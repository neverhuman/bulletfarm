[package]
name = "bullet-linux-lease"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
license = "Apache-2.0"
description = "Audited Linux file-lease boundary for Bullet recovery"

[dependencies]
libc = "=0.2.189"

[dev-dependencies]
tempfile = "=3.19.1"

[lints.rust]
unsafe_code = "deny"

[lints.clippy]
all = "deny"
