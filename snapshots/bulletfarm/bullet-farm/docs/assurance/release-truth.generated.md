<!-- Generated by: bullet-family check release --profile universal-v1 --receipts ABSOLUTE_REGISTRY --report --portable -->
<!-- Source: src/check/prerequisites.rs + src/check/truth/rows.rs + family.lock + repos.manifest.toml + docs/release.md + docs/assurance/product-gaps.md -->
<!-- Command: just release-truth -->
<!-- DO NOT EDIT BY HAND. -->

# Release truth

RELEASE DECISION: BLOCKED

| Field | Value |
| --- | --- |
| as_of | hub HEAD committer date not read; `docs/release.md` Last reviewed: 2026-08-25; no wall clock was read |
| source | `bullet-family 0.1.0` static release inventory, check report schema 3, tier RELEASE, 43 gates |
| scope | the 43 registered release gates of the four-repository Bullet Farm family, each mapped to its product gap G-id from `docs/assurance/product-gaps.md`; gap G12 combines this exact profile inventory with its selected semantic-admission machinery; nothing outside the catalog is counted |
| freshness | every projected input is identified in the Freshness section; receipt-registry path and identity are excluded and this projection is not evidence; a gate row changes only when its catalog entry, claim row, register crosswalk, or evaluation outcome changes |
| projection | PROJECTION — rendered from machine facts; this page holds no release authority |
| variant | PORTABLE — machine-local subjects, absolute paths, and mtimes are excluded; reproducible from a fresh hub-only checkout |
| profile | `universal-v1` |
| required closure | every provider, forge, and five-platform profile without implicitly admitting evolution-v1 |
| receipt admission | the selected semantic registry was evaluated before rendering, but this portable projection excludes its path and identity and is not evidence; the legacy fixed `/etc/bullet-farm/release-msrv-1-95-admission.toml` descriptor is deliberately not read |

## Repository subjects

| Repository | Commit | Tree | Checkout |
| --- | --- | --- | --- |
| bullet-farm | UNKNOWN | UNKNOWN | not read (portable variant) |
| bullet-kernel | UNKNOWN | UNKNOWN | not read (portable variant) |
| bullet-git | UNKNOWN | UNKNOWN | not read (portable variant) |
| bullet-portal | UNKNOWN | UNKNOWN | not read (portable variant) |

## Family lock

| Field | Value |
| --- | --- |
| schema | 2 (diagnostic-only; every installer path refuses it by design) |
| tag | `v0.1.0-alpha.4` |
| binds current HEADs | UNKNOWN (portable variant does not read checkouts) |

## Five truths, never conflated

| Truth | Status | Source | Scope |
| --- | --- | --- | --- |
| Mechanical gates (fast) | NOT RUN (portable variant does not read machine-local check reports) | `.bullet-family/check-fast.json` written by `bullet-family check fast --json` | fixed fast command catalog; a pass here is not release readiness |
| Mechanical gates (required) | NOT RUN (portable variant does not read machine-local check reports) | `.bullet-family/check-required.json` written by `bullet-family check required --json` | fixed required command catalog; a pass here is not release readiness |
| Evidence completeness | 0 of 43 receipted | the selected semantic registry is read for this exact profile; its identity is excluded from this projection, the legacy fixed `/etc/bullet-farm/release-msrv-1-95-admission.toml` descriptor is deliberately not read, and structural validity cannot make a gate PASS | the 43 registered release gates |
| Release review | HOLD — no receipt exists to review | this page; no reviewer record exists | human verification of receipted gates |
| Deployment match | N/A — pre-release; nothing is deployed | no deployment record exists | deployed SHA versus reviewed integration subject |
| Post-deploy survival | NOT ESTABLISHED | no deployment observation exists | required observations after a deployment |

## Gates (43 of 43 unproved)

1. **Nobody has restored a tagged backup and read the restored state back as an exact-subject release receipt.** — `release.backup-restore` BLOCKED
   - Product gap: G3, G9
   - Why it matters: A backup whose restore has never been observed protects nothing; the pointed-at bytes, not the database, are the recovery subject.
   - Acceptance: Run the tagged backup/restore suite from release bytes and register a signed receipt binding backup digest, restore epoch, and the exact four-repository subjects.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — no exact-subject backup and restore receipt is registered; exists but does not count: Kernel `798f0c8` `farm backup|restore` with an exact receipt and quarantined offline restore (COMPONENT_PROOF); restored state is not admitted for production use
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the backup/restore suite and its receipt shape are Kernel code (V1-S2); external predecessor: release bytes from signed tagged subjects (G9) and production restore admission (G3)
   - Next command: `cd ../bullet-kernel && cargo run --locked -p bullet -- farm backup --database /abs/ledger.sqlite --output /abs/snapshot.sqlite --receipt /abs/backup-receipt.json` — component backup with an exact receipt; `farm restore --backup … --receipt … --destination …` restores into quarantine only, and neither run is a tagged-bytes release receipt
   - Release-blocking: yes
2. **No checksum set is admitted for the exact artifact set selected by any release profile.** — `release.checksums` BLOCKED
   - Product gap: G9
   - Why it matters: Without checksums an installer cannot tell substituted bytes from released bytes.
   - Acceptance: Generate and re-read checksums for the requested profile's exact target set and bind the profile id, closure digest, targets, and checksums in its signed manifest; `universal-v1` must bind all five targets.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — release archive checksums are absent; exists but does not count: hub `a1b7ab3` + `ab1fd8f` build and re-read a BLAKE3 checksum manifest for one unsigned Linux x86_64 component bundle; it is not a signed checksum set admitted for any profile, and the four additional universal targets have no checksum subjects
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: checksum generation and aggregation for the exact targets selected by the requested profile are hub engineering (V1-S7); external predecessor: the requested profile's exact archives and signed manifest; `universal-v1` additionally needs its four additional target archives
   - Next command: NONE — no typed command exists yet; no profile-preserving checksum producer or admission command exists
   - Release-blocking: yes
3. **Nobody has crashed the tagged family at every SQLite/WAL/CAS/journal/generation boundary and read back either the prior or the whole next state.** — `release.fault-suite` BLOCKED
   - Product gap: G2, G3
   - Why it matters: Crash safety that was only reasoned about is not crash safety; one unobserved boundary can lose or duplicate an effect.
   - Acceptance: Run the tagged crash-boundary and recovery fault suite from release bytes and register its exact signed receipt.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the crash-boundary and recovery fault receipt is absent; exists but does not count: Kernel atomic lease/command/outbox, injected setup transaction boundaries (hub `94b6549`), and BulletGit prior-or-whole-next recovery (`274fd6d`, `f551736`) as COMPONENT_PROOF
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the crash-boundary and recovery fault suite is Kernel/BulletGit engineering (V1-S2/S3); external predecessor: the tagged release bytes the suite must run from
   - Next command: NONE — no typed command exists yet; `just model-check` covers only the two bounded formal protocols (lease/fence/reclaim, command/effect ambiguity), not the crash-boundary suite
   - Release-blocking: yes
4. **No GitHub App has integrated an exact Candidate into a protected test repository and read the result back.** — `release.forge.github-app` BLOCKED
   - Product gap: G7
   - Why it matters: GitHub is an effect adapter; until a lost response reconciles to UNKNOWN and read-back adopts the original OID, every GitHub effect is unproved.
   - Acceptance: Configure the GitHub App test repository with branch protection and register exact-subject dispatch, check, integration, read-back, and reconciliation receipts.
   - Evidence class: LIVE_PROOF
   - Current evidence: NONE — no protected GitHub App integration and reconciliation receipt is registered; exists but does not count: a specified, uncertified effect adapter (ADR 0002, ADR 0008); no App, test repository, or credential
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the typed GitHub App capability, delivery, check, integration, read-back, reconciliation, and semantic receipt-admission paths are effects/Hub engineering; external predecessor: the operator-provisioned GitHub App, branch-protected test repository, and separate delivery, attestation, integration, and observation credentials
   - Next command: NONE — no typed command exists yet; no typed GitHub App command exists
   - Release-blocking: yes
5. **No protected Jeryu integration has run with restored operator authentication and produced a read-back receipt.** — `release.forge.jeryu` BLOCKED
   - Product gap: G6
   - Why it matters: Jeryu is the source forge; an integration that never read its own result back cannot claim protected-ref safety.
   - Acceptance: With operator-restored authentication run read-only probes, then one exact protected integration with UNKNOWN/read-back reconciliation, and register the receipt; never modify the running forge.
   - Evidence class: LIVE_PROOF
   - Current evidence: NONE — no protected Jeryu integration and reconciliation receipt is registered; exists but does not count: a local bare-forge component and a typed-quarantine Jeryu adapter; no authenticated read-back
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: typed read-only probes, protected integration/read-back reconciliation, and semantic receipt admission are effects/Hub engineering; external predecessor: operator-restored scoped authentication on the unmodified pinned forge and an operator-named test repository
   - Next command: NONE — no typed command exists yet; the read-only probes that come first have no typed command yet; never modify the running forge
   - Release-blocking: yes
6. **The checked-in family.lock is still schema 2, which every installer path rejects by design.** — `release.installable-lock` BLOCKED
   - Product gap: G1
   - Why it matters: Installation from the hub cannot start until a lock carries authenticated Jeryu sources and signed exact subjects.
   - Acceptance: Publish immutable signed member tags, generate the schema-3 lock with Jeryu URL/slug, exact tree OIDs, lockfiles, and artifact digests, and commit it under a signed hub tag.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the checked-in family lock is not an installable signed schema-3 release lock; exists but does not count: schema-3 generation and strict verification code (`bullet-family lock generate|verify`), descriptor-relative setup, and the honest schema-2 refusal (`doctor` reports `UNSUPPORTED_SCHEMA`)
   - Owner: EXTERNAL (needs operator credential, signer, service, or platform) — operator publishes signed immutable member tags with an authenticated Jeryu URL/slug; the schema-3 lock can only be generated from those
   - Next command: `bullet-family lock generate --tag <prospective-version> --subjects <absolute-path>` — at the clean prospective Hub HEAD, generates the schema-3 lock from authenticated signed non-Hub tags for that version; commit the lock and sign the Hub tag last, then verify that exact tag. Until those subjects exist `bullet-family doctor --json` reports the schema-2 refusal, and that refusal is correct
   - Release-blocking: yes
7. **Nobody has run the signed prebuilt installer twice in a fresh HOME from tagged hub-only bytes.** — `release.installer-twice` BLOCKED
   - Product gap: G1
   - Why it matters: An operator-selected external executable that has not itself passed signed package admission is not an installer receipt; idempotence and exact clean member OIDs must be observed, not assumed.
   - Acceptance: From a signed prebuilt bullet-family binary and tagged bytes, run setup twice in a fresh HOME and register exact clean member OIDs, zero tracked changes, and no worktrees.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — no two-run fresh-HOME installer receipt from tagged hub-only bytes is registered; exists but does not count: two-run component fixture with local source transport and sealed Linux Cargo/Node/Bash/npm subjects (hub `94b6549`, `7efe2f3`); `scripts/setup.sh` stays a pre-admission source wrapper
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: admission of every remaining Git/helper subject, production Jeryu transport, and the exact two-run installer receipt path are Hub engineering; external predecessor: a signed prebuilt `bullet-family` from tagged bytes, authenticated Jeryu sources, protected signer roots, and two clean fresh-HOME hosts
   - Next command: NONE — no typed command exists yet; `scripts/setup.sh` delegates to an operator-selected external `bullet-family` and passes explicit Cargo/Node/npm paths, but that executable is not yet authenticated as a signed release package; running the wrapper twice is not the installer receipt
   - Release-blocking: yes
8. **No pinned Jankurai report reads 90 or better with zero caps and zero hard findings for the exact release subjects.** — `release.jankurai-90` BLOCKED
   - Product gap: G8
   - Why it matters: Hard findings and caps are release blockers; a machine-local or skip-green audit lane cannot substitute for the pinned receipt.
   - Acceptance: Run pinned Jankurai 1.6.11 with zero skips against the exact tagged subjects and register the score/cap/hard-finding receipt at 90 or better with zero caps and zero hard findings.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — Jankurai release evidence at score 90 with zero hard findings and caps is absent; exists but does not count: pinned local jankurai 1.6.11 lane that fails closed below its floor and ratchets upward only (`just audit`); no hosted checksum-pinned artifact
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: reduce hard findings and caps to zero and raise the score to 90 with the pinned local binary; external predecessor: a checksum-pinned hosted CI Jankurai artifact and the exact tagged subjects
   - Next command: `just audit` — pinned local lane with an upward-only floor; a local binary is not the CI artifact and this checkout is not the tagged subject
   - Release-blocking: yes
9. **No signed final profile manifest binds the hub tag and selected artifact closure without embedding its own digest.** — `release.manifest-non-circular` BLOCKED
   - Product gap: G9
   - Why it matters: A manifest that includes its own digest cannot be checked; a manifest without a signature cannot be trusted.
   - Acceptance: Generate the final manifest binding profile id, closure digest, hub tag, schema-3 lock, byte-sorted selected targets, and archive/SBOM/provenance digests, then sign it with the protected release key; self-hosted-v1 requires its one Ubuntu x86_64 entry and universal-v1 requires five entries.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the final non-circular signed release manifest is absent; exists but does not count: hub `release verify` checks an exact non-circular signed five-target manifest against a preassembled fixture (`352f963`); no builder or signer
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the profile-preserving manifest producer and semantic admission path are Hub release engineering; external predecessor: the requested profile's exact artifacts plus the protected release signing key and signer identity
   - Next command: NONE — no typed command exists yet; no profile-preserving manifest producer or signer exists; the current schema-2/five-target verifier is a quarantined universal-envelope component and cannot admit self-hosted-v1
   - Release-blocking: yes
10. **No signed Ubuntu 24.04 x86_64 package with the embedded Portal, services, migrations, sandbox assets, and guest image exists.** — `release.package-linux-x86_64` BLOCKED
   - Product gap: G9
   - Why it matters: The portable profile needs the concrete native archive and its supply-chain subjects; a platform condition or an internal builder test is not a package.
   - Acceptance: Build the exact tagged x86_64-unknown-linux-gnu package inside the admitted different-identity builder, semantically verify every subject, and register its signed receipt.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the signed Ubuntu 24.04 x86_64 package with embedded Portal, services, migrations, sandbox assets, and guest image is absent; exists but does not count: a quarantined internal one-target builder component exercised by focused tests; the public `release build` command refuses before parsing or output because different-identity containment is absent
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the different-identity builder boundary and package semantic verifier are Hub release engineering; external predecessor: exact signed family subjects, protected signing roots, and the admitted Linux builder environment
   - Next command: NONE — no typed command exists yet; the public `release build` command returns RELEASE_BUILD_CONTAINMENT_UNAVAILABLE before parsing or output; no admitted package-build command exists yet
   - Release-blocking: yes
11. **No release profile has a signed, lifecycle-smoked archive set for its exact selected targets; self-hosted-v1 selects one Ubuntu x86_64/systemd archive and later universal-v1 selects five targets.** — `release.package-matrix` BLOCKED
   - Product gap: G9
   - Why it matters: A release is the exact packaged bytes selected by its profile; the Vite preview lane and a separately built farmd are not package evidence.
   - Acceptance: Build and lifecycle-smoke the requested profile's exact tagged archive set with the Portal embedded, then register every archive digest; self-hosted-v1 requires one Ubuntu x86_64/systemd archive and universal-v1 requires all five targets.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the five required platform archives with the embedded Portal are absent; exists but does not count: hub `a1b7ab3` + `ab1fd8f` emit one unsigned Linux x86_64 component archive with the committed Portal embedded and all eight release binaries; it is not a signed self-hosted-v1 package, while the four additional universal-v1 targets are absent
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: profile-preserving packaging and lifecycle-smoke commands are Hub release engineering (V1-S7); external predecessor: the protected signer and exact build hosts for the selected profile; universal-v1 additionally needs its four non-first-GA target hosts
   - Next command: NONE — no typed command exists yet; no profile-preserving package producer or lifecycle-smoke admission command exists; the frozen five-target verifier cannot admit self-hosted-v1
   - Release-blocking: yes
12. **No release profile has an admitted containment or typed mutation-refusal receipt for its exact selected platform set.** — `release.platform-containment` BLOCKED
   - Product gap: G10
   - Why it matters: Linux needs a production containment receipt; any selected platform without an equivalent backend must fail closed, and that target-specific refusal is itself a receipt.
   - Acceptance: For each requested platform profile, bind the exact target and profile-closure digest to either its production containment receipt or its fail-closed mutation-refusal receipt; `universal-v1` must admit all five target bindings, while `self-hosted-v1` selects Linux x86_64 only.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — platform containment or fail-closed refusal receipts are absent; exists but does not count: Linux user+net namespace, slirp4netns, nftables, and CONNECT-proxy isolation proofs (Kernel `d388733`, `ops/ci/egress.sh`); no non-Linux backend or refusal receipt
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: profile-preserving containment certification, typed mutation-refusal, and semantic receipt-admission commands are platform/Hub engineering; external predecessor: independently controlled target hosts and the requested profile's exact packaged bytes; `universal-v1` additionally requires all five target bindings
   - Next command: NONE — no typed command exists yet; no profile-preserving containment certification command exists
   - Release-blocking: yes
13. **github-adapter-v1 has no current kind-specific semantic receipt proving GitHub App delivery, exact-SHA check, protected integration, observation, and reconciliation** — `release.profile.github-adapter-v1` BLOCKED
   - Product gap: G7
   - Why it matters: A dependency graph selects a condition; it does not establish the independently trusted, exact-subject semantics that condition names.
   - Acceptance: Register exact current-family evidence only after signer lifecycle, dependency closure, schema-3 family, policy/toolchain/environment fingerprints, trusted time, replay state, and the condition-specific semantic validator all pass.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — github-adapter-v1 has no current kind-specific semantic receipt proving GitHub App delivery, exact-SHA check, protected integration, observation, and reconciliation; exists but does not count: an explicit profile graph and bounded structural registry parser that remain fail-closed without external trust, replay state, current-family admission, and condition-specific semantics
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: condition-specific semantic validators and typed refusals are local engineering; external predecessor: independently admitted signer roots, trusted time, replay state, exact signed family subjects, credentials, services, and target platforms
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — reports this exact selected profile against its registry; structural or absent input cannot clear it
   - Release-blocking: yes
14. **gitlab-adapter-v1 has no current kind-specific semantic receipt proving GitLab.com REST v4 delivery, exact-SHA status, protected merge-request integration, observation, and reconciliation** — `release.profile.gitlab-adapter-v1` BLOCKED
   - Product gap: G16
   - Why it matters: A dependency graph selects a condition; it does not establish the independently trusted, exact-subject semantics that condition names.
   - Acceptance: Register exact current-family evidence only after signer lifecycle, dependency closure, schema-3 family, policy/toolchain/environment fingerprints, trusted time, replay state, and the condition-specific semantic validator all pass.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — gitlab-adapter-v1 has no current kind-specific semantic receipt proving GitLab.com REST v4 delivery, exact-SHA status, protected merge-request integration, observation, and reconciliation; exists but does not count: an explicit profile graph and bounded structural registry parser that remain fail-closed without external trust, replay state, current-family admission, and condition-specific semantics
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: condition-specific semantic validators and typed refusals are local engineering; external predecessor: independently admitted signer roots, trusted time, replay state, exact signed family subjects, credentials, services, and target platforms
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — reports this exact selected profile against its registry; structural or absent input cannot clear it
   - Release-blocking: yes
15. **gitlab-self-managed-v1 has no current kind-specific semantic receipt proving one exact self-managed GitLab endpoint/version and its independently certified capabilities, credentials, protection, integration, observation, and drift** — `release.profile.gitlab-self-managed-v1` BLOCKED
   - Product gap: G16
   - Why it matters: A dependency graph selects a condition; it does not establish the independently trusted, exact-subject semantics that condition names.
   - Acceptance: Register exact current-family evidence only after signer lifecycle, dependency closure, schema-3 family, policy/toolchain/environment fingerprints, trusted time, replay state, and the condition-specific semantic validator all pass.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — gitlab-self-managed-v1 has no current kind-specific semantic receipt proving one exact self-managed GitLab endpoint/version and its independently certified capabilities, credentials, protection, integration, observation, and drift; exists but does not count: an explicit profile graph and bounded structural registry parser that remain fail-closed without external trust, replay state, current-family admission, and condition-specific semantics
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: condition-specific semantic validators and typed refusals are local engineering; external predecessor: independently admitted signer roots, trusted time, replay state, exact signed family subjects, credentials, services, and target platforms
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — reports this exact selected profile against its registry; structural or absent input cannot clear it
   - Release-blocking: yes
16. **jeryu-forge-v1 has no current kind-specific semantic receipt proving pinned local Jeryu capabilities, protected integration, reconciliation, backup/restore, and drift refusal** — `release.profile.jeryu-forge-v1` BLOCKED
   - Product gap: G6
   - Why it matters: A dependency graph selects a condition; it does not establish the independently trusted, exact-subject semantics that condition names.
   - Acceptance: Register exact current-family evidence only after signer lifecycle, dependency closure, schema-3 family, policy/toolchain/environment fingerprints, trusted time, replay state, and the condition-specific semantic validator all pass.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — jeryu-forge-v1 has no current kind-specific semantic receipt proving pinned local Jeryu capabilities, protected integration, reconciliation, backup/restore, and drift refusal; exists but does not count: an explicit profile graph and bounded structural registry parser that remain fail-closed without external trust, replay state, current-family admission, and condition-specific semantics
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: condition-specific semantic validators and typed refusals are local engineering; external predecessor: independently admitted signer roots, trusted time, replay state, exact signed family subjects, credentials, services, and target platforms
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — reports this exact selected profile against its registry; structural or absent input cannot clear it
   - Release-blocking: yes
17. **platform-linux-aarch64 has no current kind-specific semantic receipt proving the Linux arm64 package and its independently certified OCI/Firecracker containment** — `release.profile.platform-linux-aarch64` BLOCKED
   - Product gap: G9, G10
   - Why it matters: A dependency graph selects a condition; it does not establish the independently trusted, exact-subject semantics that condition names.
   - Acceptance: Register exact current-family evidence only after signer lifecycle, dependency closure, schema-3 family, policy/toolchain/environment fingerprints, trusted time, replay state, and the condition-specific semantic validator all pass.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — platform-linux-aarch64 has no current kind-specific semantic receipt proving the Linux arm64 package and its independently certified OCI/Firecracker containment; exists but does not count: an explicit profile graph and bounded structural registry parser that remain fail-closed without external trust, replay state, current-family admission, and condition-specific semantics
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: condition-specific semantic validators and typed refusals are local engineering; external predecessor: independently admitted signer roots, trusted time, replay state, exact signed family subjects, credentials, services, and target platforms
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — reports this exact selected profile against its registry; structural or absent input cannot clear it
   - Release-blocking: yes
18. **platform-linux-x86_64 has no current kind-specific semantic receipt proving the Linux x86_64 package and its independently certified containment** — `release.profile.platform-linux-x86_64` BLOCKED
   - Product gap: G9, G10
   - Why it matters: A dependency graph selects a condition; it does not establish the independently trusted, exact-subject semantics that condition names.
   - Acceptance: Register exact current-family evidence only after signer lifecycle, dependency closure, schema-3 family, policy/toolchain/environment fingerprints, trusted time, replay state, and the condition-specific semantic validator all pass.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — platform-linux-x86_64 has no current kind-specific semantic receipt proving the Linux x86_64 package and its independently certified containment; exists but does not count: an explicit profile graph and bounded structural registry parser that remain fail-closed without external trust, replay state, current-family admission, and condition-specific semantics
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: condition-specific semantic validators and typed refusals are local engineering; external predecessor: independently admitted signer roots, trusted time, replay state, exact signed family subjects, credentials, services, and target platforms
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — reports this exact selected profile against its registry; structural or absent input cannot clear it
   - Release-blocking: yes
19. **platform-macos-aarch64 has no current kind-specific semantic receipt proving the macOS arm64 package and mutation refusal until an exact containment profile passes** — `release.profile.platform-macos-aarch64` BLOCKED
   - Product gap: G9, G10
   - Why it matters: A dependency graph selects a condition; it does not establish the independently trusted, exact-subject semantics that condition names.
   - Acceptance: Register exact current-family evidence only after signer lifecycle, dependency closure, schema-3 family, policy/toolchain/environment fingerprints, trusted time, replay state, and the condition-specific semantic validator all pass.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — platform-macos-aarch64 has no current kind-specific semantic receipt proving the macOS arm64 package and mutation refusal until an exact containment profile passes; exists but does not count: an explicit profile graph and bounded structural registry parser that remain fail-closed without external trust, replay state, current-family admission, and condition-specific semantics
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: condition-specific semantic validators and typed refusals are local engineering; external predecessor: independently admitted signer roots, trusted time, replay state, exact signed family subjects, credentials, services, and target platforms
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — reports this exact selected profile against its registry; structural or absent input cannot clear it
   - Release-blocking: yes
20. **platform-macos-x86_64 has no current kind-specific semantic receipt proving the macOS x86_64 package and mutation refusal until an exact containment profile passes** — `release.profile.platform-macos-x86_64` BLOCKED
   - Product gap: G9, G10
   - Why it matters: A dependency graph selects a condition; it does not establish the independently trusted, exact-subject semantics that condition names.
   - Acceptance: Register exact current-family evidence only after signer lifecycle, dependency closure, schema-3 family, policy/toolchain/environment fingerprints, trusted time, replay state, and the condition-specific semantic validator all pass.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — platform-macos-x86_64 has no current kind-specific semantic receipt proving the macOS x86_64 package and mutation refusal until an exact containment profile passes; exists but does not count: an explicit profile graph and bounded structural registry parser that remain fail-closed without external trust, replay state, current-family admission, and condition-specific semantics
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: condition-specific semantic validators and typed refusals are local engineering; external predecessor: independently admitted signer roots, trusted time, replay state, exact signed family subjects, credentials, services, and target platforms
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — reports this exact selected profile against its registry; structural or absent input cannot clear it
   - Release-blocking: yes
21. **platform-windows-x86_64 has no current kind-specific semantic receipt proving the Windows x64 package and mutation refusal until an exact containment profile passes** — `release.profile.platform-windows-x86_64` BLOCKED
   - Product gap: G9, G10
   - Why it matters: A dependency graph selects a condition; it does not establish the independently trusted, exact-subject semantics that condition names.
   - Acceptance: Register exact current-family evidence only after signer lifecycle, dependency closure, schema-3 family, policy/toolchain/environment fingerprints, trusted time, replay state, and the condition-specific semantic validator all pass.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — platform-windows-x86_64 has no current kind-specific semantic receipt proving the Windows x64 package and mutation refusal until an exact containment profile passes; exists but does not count: an explicit profile graph and bounded structural registry parser that remain fail-closed without external trust, replay state, current-family admission, and condition-specific semantics
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: condition-specific semantic validators and typed refusals are local engineering; external predecessor: independently admitted signer roots, trusted time, replay state, exact signed family subjects, credentials, services, and target platforms
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — reports this exact selected profile against its registry; structural or absent input cannot clear it
   - Release-blocking: yes
22. **provider-antigravity has no current kind-specific semantic receipt proving one exact Antigravity provider version, profile, service identity, isolation, failure, quota, and patch certification** — `release.profile.provider-antigravity` BLOCKED
   - Product gap: G5
   - Why it matters: A dependency graph selects a condition; it does not establish the independently trusted, exact-subject semantics that condition names.
   - Acceptance: Register exact current-family evidence only after signer lifecycle, dependency closure, schema-3 family, policy/toolchain/environment fingerprints, trusted time, replay state, and the condition-specific semantic validator all pass.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — provider-antigravity has no current kind-specific semantic receipt proving one exact Antigravity provider version, profile, service identity, isolation, failure, quota, and patch certification; exists but does not count: an explicit profile graph and bounded structural registry parser that remain fail-closed without external trust, replay state, current-family admission, and condition-specific semantics
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: condition-specific semantic validators and typed refusals are local engineering; external predecessor: independently admitted signer roots, trusted time, replay state, exact signed family subjects, credentials, services, and target platforms
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — reports this exact selected profile against its registry; structural or absent input cannot clear it
   - Release-blocking: yes
23. **provider-claude has no current kind-specific semantic receipt proving one exact Claude provider version, profile, service identity, isolation, failure, quota, and patch certification** — `release.profile.provider-claude` BLOCKED
   - Product gap: G5
   - Why it matters: A dependency graph selects a condition; it does not establish the independently trusted, exact-subject semantics that condition names.
   - Acceptance: Register exact current-family evidence only after signer lifecycle, dependency closure, schema-3 family, policy/toolchain/environment fingerprints, trusted time, replay state, and the condition-specific semantic validator all pass.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — provider-claude has no current kind-specific semantic receipt proving one exact Claude provider version, profile, service identity, isolation, failure, quota, and patch certification; exists but does not count: an explicit profile graph and bounded structural registry parser that remain fail-closed without external trust, replay state, current-family admission, and condition-specific semantics
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: condition-specific semantic validators and typed refusals are local engineering; external predecessor: independently admitted signer roots, trusted time, replay state, exact signed family subjects, credentials, services, and target platforms
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — reports this exact selected profile against its registry; structural or absent input cannot clear it
   - Release-blocking: yes
24. **provider-codex has no current kind-specific semantic receipt proving one exact Codex provider version, profile, service identity, isolation, failure, quota, and patch certification** — `release.profile.provider-codex` BLOCKED
   - Product gap: G5
   - Why it matters: A dependency graph selects a condition; it does not establish the independently trusted, exact-subject semantics that condition names.
   - Acceptance: Register exact current-family evidence only after signer lifecycle, dependency closure, schema-3 family, policy/toolchain/environment fingerprints, trusted time, replay state, and the condition-specific semantic validator all pass.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — provider-codex has no current kind-specific semantic receipt proving one exact Codex provider version, profile, service identity, isolation, failure, quota, and patch certification; exists but does not count: an explicit profile graph and bounded structural registry parser that remain fail-closed without external trust, replay state, current-family admission, and condition-specific semantics
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: condition-specific semantic validators and typed refusals are local engineering; external predecessor: independently admitted signer roots, trusted time, replay state, exact signed family subjects, credentials, services, and target platforms
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — reports this exact selected profile against its registry; structural or absent input cannot clear it
   - Release-blocking: yes
25. **provider-cursor has no current kind-specific semantic receipt proving one exact Cursor provider version, profile, service identity, isolation, failure, quota, and patch certification** — `release.profile.provider-cursor` BLOCKED
   - Product gap: G5
   - Why it matters: A dependency graph selects a condition; it does not establish the independently trusted, exact-subject semantics that condition names.
   - Acceptance: Register exact current-family evidence only after signer lifecycle, dependency closure, schema-3 family, policy/toolchain/environment fingerprints, trusted time, replay state, and the condition-specific semantic validator all pass.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — provider-cursor has no current kind-specific semantic receipt proving one exact Cursor provider version, profile, service identity, isolation, failure, quota, and patch certification; exists but does not count: an explicit profile graph and bounded structural registry parser that remain fail-closed without external trust, replay state, current-family admission, and condition-specific semantics
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: condition-specific semantic validators and typed refusals are local engineering; external predecessor: independently admitted signer roots, trusted time, replay state, exact signed family subjects, credentials, services, and target platforms
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — reports this exact selected profile against its registry; structural or absent input cannot clear it
   - Release-blocking: yes
26. **self-hosted-v1 has no current kind-specific semantic receipt proving Ubuntu 24.04 x86_64/systemd, a signed schema-3 family, pinned local Jeryu, Claude service identity, offline and live five-authority transactions, every selected product surface durable or typed OUT_OF_PROFILE, operations, installer, security, and supply-chain closure** — `release.profile.self-hosted-v1` BLOCKED
   - Product gap: G1, G2, G3, G5, G6, G8, G9, G10, G13, G14
   - Why it matters: A dependency graph selects a condition; it does not establish the independently trusted, exact-subject semantics that condition names.
   - Acceptance: Register exact current-family evidence only after signer lifecycle, dependency closure, schema-3 family, policy/toolchain/environment fingerprints, trusted time, replay state, and the condition-specific semantic validator all pass.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — self-hosted-v1 has no current kind-specific semantic receipt proving Ubuntu 24.04 x86_64/systemd, a signed schema-3 family, pinned local Jeryu, Claude service identity, offline and live five-authority transactions, every selected product surface durable or typed OUT_OF_PROFILE, operations, installer, security, and supply-chain closure; exists but does not count: an explicit profile graph and bounded structural registry parser that remain fail-closed without external trust, replay state, current-family admission, and condition-specific semantics
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: condition-specific semantic validators and typed refusals are local engineering; external predecessor: independently admitted signer roots, trusted time, replay state, exact signed family subjects, credentials, services, and target platforms
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — reports this exact selected profile against its registry; structural or absent input cannot clear it
   - Release-blocking: yes
27. **universal-v1 has no current kind-specific semantic receipt proving every provider, forge, and five-platform profile without implicitly admitting evolution-v1** — `release.profile.universal-v1` BLOCKED
   - Product gap: G1, G2, G3, G5, G6, G7, G8, G9, G10, G16
   - Why it matters: A dependency graph selects a condition; it does not establish the independently trusted, exact-subject semantics that condition names.
   - Acceptance: Register exact current-family evidence only after signer lifecycle, dependency closure, schema-3 family, policy/toolchain/environment fingerprints, trusted time, replay state, and the condition-specific semantic validator all pass.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — universal-v1 has no current kind-specific semantic receipt proving every provider, forge, and five-platform profile without implicitly admitting evolution-v1; exists but does not count: an explicit profile graph and bounded structural registry parser that remain fail-closed without external trust, replay state, current-family admission, and condition-specific semantics
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: condition-specific semantic validators and typed refusals are local engineering; external predecessor: independently admitted signer roots, trusted time, replay state, exact signed family subjects, credentials, services, and target platforms
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — reports this exact selected profile against its registry; structural or absent input cannot clear it
   - Release-blocking: yes
28. **No signed build provenance statement is admitted for the exact artifacts selected by any release profile.** — `release.provenance` BLOCKED
   - Product gap: G9
   - Why it matters: Without provenance a consumer cannot connect archive bytes to the hub tag, lock, and toolchains that produced them.
   - Acceptance: Produce provenance bound to the exact profile, closure digest, hub tag, lock, toolchains, and selected archive digests, sign it, and re-read it with the release verifier; `universal-v1` must bind all five targets.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — signed build provenance for the package matrix is absent; exists but does not count: the quarantined internal Linux component writes and re-reads unsigned in-toto provenance bound to its one-target component subject; `release verify` re-reads provenance inside a signed bundle; no signed selected-profile provenance is admitted, and the four additional universal target subjects are absent
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the profile-preserving provenance producer and semantic admission path are Hub release engineering; external predecessor: the protected release key, independent build-attestor identity, and requested profile's exact selected archive digests; `universal-v1` additionally needs its four additional target archives
   - Next command: NONE — no typed command exists yet; no profile-preserving provenance producer or admission command exists; the current schema-2/five-target verifier is a quarantined universal-envelope component and cannot admit self-hosted-v1
   - Release-blocking: yes
29. **No admitted Antigravity binary has produced a sealed PONG receipt through the live-conformance path; the path exists (Kernel `ba485d5`, `b4735da`, `0d848f6`; `docs/runbooks/live-conformance.md`) but every run so far refused at POLICY_LIVE_ADMISSION_DISABLED under the checked-in v1alpha1 policy, so the offline structured-output subset and the zero-spawn neutral refusal are the only Antigravity evidence.** — `release.provider.antigravity` BLOCKED
   - Product gap: G5
   - Why it matters: The bounded offline parser and the zero-spawn neutral refusal prove neither schema-3 policy/enrollment-anchor admission nor the live adapter, its credentials, quota, isolation, patch output, onboarding, or sealed receipt registration; only a semantically admitted PONG receipt under an operator-ratified policy is LIVE_PROOF for this adapter.
   - Acceptance: First land hostile-tested schema-3 policy and provider-enrollment-anchor admission, provider-specific onboarding/runtime probing, and semantic sealed-receipt registration. Then perform the operator act in `docs/runbooks/live-conformance.md` (authority keygen, a ratified policy generation with an active provider-runner key, the `AGENT_CHAT.md` ratification line), run the real-mode lane, and register the sealed exact adapter/version/profile receipt covering isolation, failure, quota, and patch conformance.
   - Evidence class: LIVE_PROOF
   - Current evidence: NONE — Antigravity release conformance evidence is absent; exists but does not count: Kernel `5badc85` bounded structured-output subset; the common policy→key→lease→admission→grant→egress→read-only-turn→canary→receipt path with the v1alpha2 loader mirror (`0d848f6`); every run refuses before spawn under the checked-in policy
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: schema-3 policy/enrollment-anchor admission, provider onboarding/runtime probing, and semantic sealed-receipt registration are local engineering; external predecessor: the operator ratifies the policy and enrollment, supplies the exact provider executable/profile/credentials, and runs the native live conformance lane
   - Next command: `cd ../bullet-kernel && BULLET_LIVE_REAL=1 BULLET_POLICY_PATH=/abs/bullet-data/policy/policy.json BULLET_LIVE_PROVIDERS=agy bash ops/ci/nightly.sh` — real mode refuses to start without an absolute operator-ratified policy path; the current loader only checks structure and does not admit the external anchor, and under the checked-in v1alpha1 policy the lane exits 78 (neutral refusal) and spawns nothing
   - Release-blocking: yes
30. **No admitted Claude binary has produced a sealed PONG receipt through the live-conformance path; the path exists (Kernel `ba485d5`, `b4735da`, `0d848f6`; `docs/runbooks/live-conformance.md`) but every run so far refused at POLICY_LIVE_ADMISSION_DISABLED under the checked-in v1alpha1 policy, so the offline stream-JSON subset, the deep fake-process proof, and the zero-spawn neutral refusal are the only Claude evidence.** — `release.provider.claude` BLOCKED
   - Product gap: G5
   - Why it matters: The bounded offline parser and the zero-spawn neutral refusal prove neither schema-3 policy/enrollment-anchor admission nor the live adapter, its credentials, quota, isolation, patch output, onboarding, or sealed receipt registration; only a semantically admitted PONG receipt under an operator-ratified policy is LIVE_PROOF for this adapter.
   - Acceptance: First land hostile-tested schema-3 policy and provider-enrollment-anchor admission, provider-specific onboarding/runtime probing, and semantic sealed-receipt registration. Then perform the operator act in `docs/runbooks/live-conformance.md` (authority keygen, a ratified policy generation with an active provider-runner key, the `AGENT_CHAT.md` ratification line), run the real-mode lane, and register the sealed exact adapter/version/profile receipt covering isolation, failure, quota, and patch conformance.
   - Evidence class: LIVE_PROOF
   - Current evidence: NONE — Claude release conformance evidence is absent; exists but does not count: Kernel `c34d578` bounded stream-JSON subset and the only deep positive fake-process proof; the common policy→key→lease→admission→grant→egress→read-only-turn→canary→receipt path with the v1alpha2 loader mirror (`0d848f6`); every run refuses before spawn under the checked-in policy
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: schema-3 policy/enrollment-anchor admission, provider onboarding/runtime probing, and semantic sealed-receipt registration are local engineering; external predecessor: the operator ratifies the policy and enrollment, supplies the exact provider executable/profile/credentials, and runs the native live conformance lane
   - Next command: `cd ../bullet-kernel && BULLET_LIVE_REAL=1 BULLET_POLICY_PATH=/abs/bullet-data/policy/policy.json BULLET_LIVE_PROVIDERS=claude bash ops/ci/nightly.sh` — real mode refuses to start without an absolute operator-ratified policy path; the current loader only checks structure and does not admit the external anchor, and under the checked-in v1alpha1 policy the lane exits 78 (neutral refusal) and spawns nothing
   - Release-blocking: yes
31. **No admitted Codex binary has produced a sealed PONG receipt through the live-conformance path; the path exists (Kernel `ba485d5`, `b4735da`, `0d848f6`; `docs/runbooks/live-conformance.md`) but every run so far refused at POLICY_LIVE_ADMISSION_DISABLED under the checked-in v1alpha1 policy, so the offline App Server JSONL subset and the zero-spawn neutral refusal are the only Codex evidence.** — `release.provider.codex` BLOCKED
   - Product gap: G5
   - Why it matters: The bounded offline parser and the zero-spawn neutral refusal prove neither schema-3 policy/enrollment-anchor admission nor the live adapter, its credentials, quota, isolation, patch output, onboarding, or sealed receipt registration; only a semantically admitted PONG receipt under an operator-ratified policy is LIVE_PROOF for this adapter.
   - Acceptance: First land hostile-tested schema-3 policy and provider-enrollment-anchor admission, provider-specific onboarding/runtime probing, and semantic sealed-receipt registration. Then perform the operator act in `docs/runbooks/live-conformance.md` (authority keygen, a ratified policy generation with an active provider-runner key, the `AGENT_CHAT.md` ratification line), run the real-mode lane, and register the sealed exact adapter/version/profile receipt covering isolation, failure, quota, and patch conformance.
   - Evidence class: LIVE_PROOF
   - Current evidence: NONE — Codex release conformance evidence is absent; exists but does not count: Kernel `ca376e4` bounded App Server JSONL subset; the common policy→key→lease→admission→grant→egress→read-only-turn→canary→receipt path with the v1alpha2 loader mirror (`0d848f6`); every run refuses before spawn under the checked-in policy
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: schema-3 policy/enrollment-anchor admission, provider onboarding/runtime probing, and semantic sealed-receipt registration are local engineering; external predecessor: the operator ratifies the policy and enrollment, supplies the exact provider executable/profile/credentials, and runs the native live conformance lane
   - Next command: `cd ../bullet-kernel && BULLET_LIVE_REAL=1 BULLET_POLICY_PATH=/abs/bullet-data/policy/policy.json BULLET_LIVE_PROVIDERS=codex bash ops/ci/nightly.sh` — real mode refuses to start without an absolute operator-ratified policy path; the current loader only checks structure and does not admit the external anchor, and under the checked-in v1alpha1 policy the lane exits 78 (neutral refusal) and spawns nothing
   - Release-blocking: yes
32. **No admitted Cursor binary has produced a sealed PONG receipt through the live-conformance path; the path exists (Kernel `ba485d5`, `b4735da`, `0d848f6`; `docs/runbooks/live-conformance.md`) but every run so far refused at POLICY_LIVE_ADMISSION_DISABLED under the checked-in v1alpha1 policy, so the offline ACP subset and the zero-spawn neutral refusal are the only Cursor evidence.** — `release.provider.cursor` BLOCKED
   - Product gap: G5
   - Why it matters: The bounded offline parser and the zero-spawn neutral refusal prove neither schema-3 policy/enrollment-anchor admission nor the live adapter, its credentials, quota, isolation, patch output, onboarding, or sealed receipt registration; only a semantically admitted PONG receipt under an operator-ratified policy is LIVE_PROOF for this adapter.
   - Acceptance: First land hostile-tested schema-3 policy and provider-enrollment-anchor admission, provider-specific onboarding/runtime probing, and semantic sealed-receipt registration. Then perform the operator act in `docs/runbooks/live-conformance.md` (authority keygen, a ratified policy generation with an active provider-runner key, the `AGENT_CHAT.md` ratification line), run the real-mode lane, and register the sealed exact adapter/version/profile receipt covering isolation, failure, quota, and patch conformance.
   - Evidence class: LIVE_PROOF
   - Current evidence: NONE — Cursor release conformance evidence is absent; exists but does not count: Kernel `ea89929` bounded ACP subset; the common policy→key→lease→admission→grant→egress→read-only-turn→canary→receipt path with the v1alpha2 loader mirror (`0d848f6`); every run refuses before spawn under the checked-in policy
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: schema-3 policy/enrollment-anchor admission, provider onboarding/runtime probing, and semantic sealed-receipt registration are local engineering; external predecessor: the operator ratifies the policy and enrollment, supplies the exact provider executable/profile/credentials, and runs the native live conformance lane
   - Next command: `cd ../bullet-kernel && BULLET_LIVE_REAL=1 BULLET_POLICY_PATH=/abs/bullet-data/policy/policy.json BULLET_LIVE_PROVIDERS=cursor bash ops/ci/nightly.sh` — real mode refuses to start without an absolute operator-ratified policy path; the current loader only checks structure and does not admit the external anchor, and under the checked-in v1alpha1 policy the lane exits 78 (neutral refusal) and spawns nothing
   - Release-blocking: yes
33. **The receipt verifier has only ever checked fixtures; no external allowed-signers policy or real signed release receipt has been provisioned.** — `release.receipt-contracts` BLOCKED
   - Product gap: G12
   - Why it matters: A verifier without a provisioned signer policy and a real receipt is a component, not release evidence.
   - Acceptance: Implement and hostile-test each selected receipt kind's semantic verifier, then provision the external signer policy, trusted-time observation, replay state, and exact tagged receipts; both this admission gate and the requested profile-condition receipt must pass, and neither can substitute for the other.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the selected receipt registry is absent; zero profile gates were cleared from it; exists but does not count: strict canonical receipt/policy verifier with exact OpenSSH signer/namespace/interval checks (hub `143f8b9`) plus the MSRV receipt-admission path (`d762f86`); only fixtures have ever been checked
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: kind-specific semantic validators and typed admission for the exact requested profile closure are Hub engineering; external predecessor: an independently provisioned allowed-signers policy, trusted time, replay/high-water state, and real signed receipts from tagged bytes
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — evaluates generic signer, trusted-time, replay, exact-family, and semantic admission for this exact selected profile
   - Release-blocking: yes
34. **No admitted Rust 1.95 build-and-test receipt is available to this profile; conformant profiles use their selected semantic registry, while legacy-v1-26 deliberately keeps this gate static.** — `release.rust-msrv-1-95` BLOCKED
   - Product gap: G9
   - Why it matters: MSRV is a release promise; a local toolchain match today is not a tagged-bytes build receipt, and a self-selected signer or a generic result digest cannot clear the gate.
   - Acceptance: Provision the selected semantic registry with role-separated source-tag, build-attestor, and trusted-time roots, then supply the receipt binding the schema-3 lock, clean signed subjects, dependency-lock digests, exact Rust 1.95 rustc/cargo bytes, and zero-skip build/test observations for all three Rust workspaces.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — an exact release build receipt for Rust 1.95 is absent; exists but does not count: the selected-registry structural admission path and local lanes that already build under rustc 1.95.0 (`rust-toolchain.toml`, `scripts/ci-doctor.sh`); legacy-v1-26 ignores registry contents and no profiled path reads the legacy fixed descriptor
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the admission path and the 1.95 lane are hub code; every local lane already runs under rustc 1.95.0; external predecessor: a selected semantic registry with three distinct admitted signer roots, an independently signed time observation, and the schema-3 lock the receipt must bind
   - Next command: `bullet-family check release --profile universal-v1 --receipts /absolute/admitted-registry --json` — reads the selected semantic registry and deliberately does not read the legacy fixed `/etc/bullet-farm/release-msrv-1-95-admission.toml` descriptor
   - Release-blocking: yes
35. **Nobody has built and tested the exact tagged family with pinned Rust 1.97.1 and registered the receipt.** — `release.rust-pinned-1-97-1` BLOCKED
   - Product gap: G9
   - Why it matters: The pinned toolchain is the second required build; one toolchain receipt never covers the other.
   - Acceptance: Build and test the exact tagged family with the admitted pinned Rust 1.97.1 toolchain under cargo --locked and register the receipt.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — an exact release build receipt for pinned Rust 1.97.1 is absent; exists but does not count: the explicit `just toolchain-pinned` Rust 1.97.1 build/test lane emits a subject-, tool-, argv-, environment-, and output-bound machine-local observation; it is unsigned diagnostic input, and no condition-specific admission path or release receipt exists
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the pinned 1.97.1 lane exists; its condition-specific semantic admission path remains hub engineering; external predecessor: the exact tagged family bytes and the same signer/time roots as the MSRV receipt
   - Next command: `just toolchain-pinned` — builds and tests this Hub under pinned Rust 1.97.1 and emits an unsigned observation only; it cannot clear the release gate
   - Release-blocking: yes
36. **No SBOM set is admitted for the exact artifacts selected by any release profile.** — `release.sbom` BLOCKED
   - Product gap: G9
   - Why it matters: Consumers cannot audit or respond to a vulnerability in bytes whose contents were never enumerated.
   - Acceptance: Generate and validate CycloneDX and SPDX SBOMs for the requested profile's exact target set and bind profile id, closure digest, targets, and SBOM digests in the signed manifest; universal requires all five targets.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — software bills of materials for the package matrix are absent; exists but does not count: hub `a1b7ab3` + `ab1fd8f` generate and re-read a CycloneDX 1.6 SBOM with typed component, package URL, and admitted-license fields for one unsigned Linux x86_64 component bundle; no signed SBOM set is admitted for any profile, and the four additional universal target subjects are absent
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: SBOM generation and validation over the requested profile's exact built archives; external predecessor: the requested profile's exact archives and signed manifest that binds each SBOM digest; `universal-v1` additionally needs its four additional target archives
   - Next command: NONE — no typed command exists yet; no profile-preserving SBOM producer or admission command exists
   - Release-blocking: yes
37. **No pinned dependency scan receipt exists for the exact release lockfiles.** — `release.scan.dependency` BLOCKED
   - Product gap: G8, G9
   - Why it matters: A scan run on a working tree at some earlier commit says nothing about the tagged lockfiles.
   - Acceptance: Run the admitted cargo-deny 0.19.8 against the exact tagged lockfiles and register its receipt.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the pinned dependency scan receipt is absent; exists but does not count: pinned cargo-deny 0.19.8 checks advisories, licenses, bans, and sources against this checkout after refreshing and age-checking RustSec; no tagged-family scan receipt exists
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the pinned four-category cargo-deny lane over this checkout; external predecessor: the exact tagged lockfiles of all three Rust workspaces
   - Next command: `just security` — runs current-tree secrets, canary, cargo-deny advisories/licenses/bans/sources, and strict workflow analysis on this checkout only; not the exact tagged family
   - Release-blocking: yes
38. **No pinned license policy scan receipt exists for the exact release artifacts.** — `release.scan.license` BLOCKED
   - Product gap: G8, G9
   - Why it matters: License violations discovered after tagging invalidate the release bytes.
   - Acceptance: Run the admitted license scanner against the exact archives and SBOMs and register its receipt.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the pinned license policy scan receipt is absent; exists but does not count: pinned cargo-deny 0.19.8 checks the configured license policy for this checkout together with advisories, bans, and sources; no exact-archive or SBOM receipt exists
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the pinned cargo-deny license-policy lane over this checkout; external predecessor: the exact tagged archives and SBOMs it must run against
   - Next command: `just security` — checks the checkout's lockfile license policy; exact tagged archives, their SBOMs, and an admitted release receipt remain
   - Release-blocking: yes
39. **No pinned secret scan receipt exists for the exact tagged trees.** — `release.scan.secret` BLOCKED
   - Product gap: G8, G9
   - Why it matters: A canary or credential in tagged bytes is unrecoverable once published.
   - Acceptance: Run the admitted gitleaks 8.21.2 against every exact tagged tree and register its receipt.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the pinned secret scan receipt is absent; exists but does not count: pinned gitleaks 8.21.2 scans the current Hub source and lockfiles before dependency resolution, and a secret-shaped canary proves genuine findings fail; no exact tagged-family receipt exists
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the pinned current-tree scan and genuine-finding canary; external predecessor: every exact tagged tree of the four repositories
   - Next command: `just security` — scans this current Hub tree and proves a canary is detected; the exact tagged trees of all four repositories remain
   - Release-blocking: yes
40. **No pinned workflow policy scan receipt exists for the exact workflow bytes.** — `release.scan.workflow` BLOCKED
   - Product gap: G8, G9
   - Why it matters: Hosted workflows are a supply-chain surface; unpinned or over-permissioned steps break provenance.
   - Acceptance: Run the admitted zizmor 1.25.2 against the exact workflow bytes and register its receipt.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the pinned workflow policy scan receipt is absent; exists but does not count: pinned zizmor 1.25.2 runs offline with ignores disabled and strict collection on this checkout; actionlint 1.7.8 and repository policy meta-tests cover the same workflow inventory in lint
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the pinned zizmor lane over this checkout; external predecessor: the exact tagged workflow bytes
   - Next command: `just security` — runs strict workflow analysis on this checkout only; not the exact tagged workflow bytes or a hosted release receipt
   - Release-blocking: yes
41. **No requested profile artifact set carries signatures from admitted protected release keys.** — `release.signatures` BLOCKED
   - Product gap: G9
   - Why it matters: An unsigned archive, checksum set, SBOM, or manifest can be replaced by anyone who can write to the download path.
   - Acceptance: Sign every archive, checksum set, SBOM, provenance statement, and final manifest selected by the requested profile, bind its closure digest and target set, and re-read them with the release verifier; universal requires the five-target set.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — verified signatures for the package matrix are absent; exists but does not count: hub `release verify` checks detached signatures and exact Ed25519 signer status inside an assembled bundle; nothing signs
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the profile-preserving signing plan, producer, and signature-admission path are Hub release engineering; external predecessor: protected release keys, a distinct signer identity, and the requested profile's exact artifact set
   - Next command: NONE — no typed command exists yet; no profile-preserving signer or signature-admission command exists; the current schema-2/five-target verifier is a quarantined universal-envelope component and cannot admit self-hosted-v1
   - Release-blocking: yes
42. **No native systemd install, upgrade, activation, rollback, uninstall, and non-destructive retention receipt exists.** — `release.systemd-v1` BLOCKED
   - Product gap: G1, G9
   - Why it matters: A signed archive is not an operable Linux release until native lifecycle transitions preserve the prior or whole next generation and read their service state back.
   - Acceptance: Run two clean Ubuntu 24.04 installs plus upgrade, activation, rollback, uninstall, retention, and disaster drills from signed package bytes and register the exact receipt.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the native systemd install, upgrade, activation, rollback, uninstall, and non-destructive retention receipt is absent; exists but does not count: release and setup runbooks plus structural bundle verification; no activation helper, generation lifecycle, clean-host drill, or signed lifecycle receipt
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the narrow activation service, generation lifecycle, rollback, and fault harness are Hub release engineering; external predecessor: two clean Ubuntu 24.04 hosts, signed package bytes, protected signing roots, and trusted time
   - Next command: NONE — no typed command exists yet; no typed native activation or lifecycle-drill command exists; source setup and archive extraction are not installers
   - Release-blocking: yes
43. **Nobody has produced a signed five-plane TRANSACTION_PROOF from `just demo`; the demo ends in self-signed component evidence.** — `release.transaction-demo` BLOCKED
   - Product gap: G2
   - Why it matters: The offline transaction is the baseline every live and release gate builds on; component evidence renamed is still not a transaction.
   - Acceptance: Run the connected tagged demo through real child boundaries and the protected local forge simulator, ending in one signed TRANSACTION_PROOF covering authority, runner death/salvage, independent verification, ambiguous-effect reconciliation, protected integration, preservation, and truthful projection.
   - Evidence class: TRANSACTION_PROOF
   - Current evidence: NONE — the exact offline five-plane transaction demo receipt is absent; exists but does not count: `just demo` runs the Kernel simulator and emits a self-signed COMPONENT_PROOF receipt; atomic lease/command/outbox, fail-closed `bullet-gitd`, the fixture E2 verifier, and Portal PENDING→UNKNOWN remain component evidence
   - Owner: LOCAL (closable offline) — Kernel + BulletGit + Portal engineering (V1-S4); the TRANSACTION_PROOF is credential-free and offline
   - Next command: `just demo` — today ends in self-signed component evidence from the simulator; a connected receipt must replace that evidence, not rename it
   - Release-blocking: yes

## Product-gap crosswalk

Every G-id of `docs/assurance/product-gaps.md` is visible here: by selected gate rows, by the ungated section below, or by this exact profile inventory. For semantic profiles G12 deliberately shows both the inventory projection and its semantic-admission gate without counting either as evidence. Titles are compiled; the crosswalk rows are read from the register and compared here.

| Gap | Title | Answered by |
| --- | --- | --- |
| G1 | Hub-only signed install | `release.installable-lock`, `release.installer-twice`, `release.systemd-v1`, `release.profile.self-hosted-v1`, `release.profile.universal-v1` |
| G2 | Connected five-plane transaction | `release.fault-suite`, `release.transaction-demo`, `release.profile.self-hosted-v1`, `release.profile.universal-v1` |
| G3 | Production Kernel write path | `release.backup-restore`, `release.fault-suite`, `release.profile.self-hosted-v1`, `release.profile.universal-v1` |
| G4 | Production BulletGit write path | ungated — release-blocking through the selected `release.transaction-demo`; see below |
| G5 | Live provider conformance | `release.provider.antigravity`, `release.provider.claude`, `release.provider.codex`, `release.provider.cursor`, `release.profile.provider-antigravity`, `release.profile.provider-claude`, `release.profile.provider-codex`, `release.profile.provider-cursor`, `release.profile.self-hosted-v1`, `release.profile.universal-v1` |
| G6 | Jeryu live effect | `release.forge.jeryu`, `release.profile.jeryu-forge-v1`, `release.profile.self-hosted-v1`, `release.profile.universal-v1` |
| G7 | GitHub live effect | `release.forge.github-app`, `release.profile.github-adapter-v1`, `release.profile.universal-v1` |
| G8 | Security release floor | `release.jankurai-90`, `release.scan.dependency`, `release.scan.license`, `release.scan.secret`, `release.scan.workflow`, `release.profile.self-hosted-v1`, `release.profile.universal-v1` |
| G9 | Signed profile-selected release | `release.backup-restore`, `release.checksums`, `release.manifest-non-circular`, `release.package-matrix`, `release.provenance`, `release.rust-msrv-1-95`, `release.rust-pinned-1-97-1`, `release.sbom`, `release.scan.dependency`, `release.scan.license`, `release.scan.secret`, `release.scan.workflow`, `release.signatures`, `release.package-linux-x86_64`, `release.systemd-v1`, `release.profile.platform-linux-aarch64`, `release.profile.platform-linux-x86_64`, `release.profile.platform-macos-aarch64`, `release.profile.platform-macos-x86_64`, `release.profile.platform-windows-x86_64`, `release.profile.self-hosted-v1`, `release.profile.universal-v1` |
| G10 | Platform containment | `release.platform-containment`, `release.profile.platform-linux-aarch64`, `release.profile.platform-linux-x86_64`, `release.profile.platform-macos-aarch64`, `release.profile.platform-macos-x86_64`, `release.profile.platform-windows-x86_64`, `release.profile.self-hosted-v1`, `release.profile.universal-v1` |
| G11 | Evolutionary runtime | NOT SELECTED BY `universal-v1` — independently owned by `release.profile.evolution-v1` |
| G12 | Family `check release` | this `universal-v1` inventory — 43 selected gates, 0 receipted; semantic admission: `release.receipt-contracts`; the requested profile-condition receipt must also pass, and neither half can substitute for the other (historical diagnostic: `bullet-family check release --profile legacy-v1-26 --receipts ABSOLUTE_REGISTRY --report --portable`) |
| G13 | Portal product surfaces | `release.profile.self-hosted-v1` |
| G14 | farmd production API | `release.profile.self-hosted-v1` |
| G15 | Cognitive persistence | NOT SELECTED BY `universal-v1` — independently owned by `release.profile.evolution-v1` |
| G16 | GitLab adapter effects | `release.profile.gitlab-adapter-v1`, `release.profile.gitlab-self-managed-v1`, `release.profile.universal-v1` |
| G17 | Distributed team mode | NOT SELECTED BY `universal-v1` — independently owned by `release.profile.team-v1` |
| G18 | Cross-repository sagas | NOT SELECTED BY `universal-v1` — independently owned by `release.profile.saga-v1` |

Agreement with `docs/assurance/product-gaps.md`: YES — all 46 crosswalk rows and the G-id list agree

## Blocking but not a release gate

Product gaps with no `release.*` id. Each row says which gate it blocks through; none can be receipted here, and each is listed so that no G-id is invisible to machine output.

1. **Public BulletGit `clone` still answers AUTHORITY_CONTRACT_UNAVAILABLE, and no immutable `bullet-wire` tag has been published for consumers to pin.** — G4 Production BulletGit write path
   - Why it matters: Without positive online authority and a published wire tag no Candidate can be written by the production path, so the five-plane transaction (G2) cannot start.
   - Acceptance: Publish the immutable `bullet-wire` tag, land Kernel online reservation/settlement and BulletGit positive authority, and read one exact Candidate back through the public path inside the G2 transaction.
   - Evidence class: TRANSACTION_PROOF
   - Current evidence: COMPONENT only — dissociate clone, hostile-git, generations, preservation, and honest cleanup UNKNOWN (BulletGit `236f4ef`); no positive online authority and no published wire tag
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: Kernel online reservation/settlement and BulletGit positive authority (V1-S3); external predecessor: operator publishes the immutable `bullet-wire` tag
   - Next command: NONE — no typed command exists yet; `just contract` proves generated wire identity, not a published tag
   - Release-blocking: yes — through G2 (`release.transaction-demo`); no receipt here can count on its own

## Excluded from this decision

- Self-signed component receipts, including the `required.demo-component` simulator lane: COMPONENT_PROOF never counts toward a transaction, live, or release gate.
- Simulator runs, fixture-only policies, and the fixture-only live-enabled v1alpha2 policy: none is an operator ratification.
- Prose: `docs/release.md`, `docs/assurance/product-gaps.md`, `docs/assurance/v1-closure-plan.md`, ADRs, changelog, and coordination chat describe evidence; they are not evidence.
- Component receipts named in `docs/release.md` and in the “exists but does not count” text above: COMPONENT_PROOF for one crate or surface, never transaction, live, or release evidence.
- The archived 2026-08-24 live demonstration receipt at the family root `.l7-bundle/`: incident evidence only (`transaction_gate_eligible: false`); it is not counted.
- Any mechanical gate result recorded against subjects other than the current HEADs (STALE above).
- Reviewer checkmarks, browser-local marks, exit codes, model statements, pushed branches, and pull requests.

## Freshness

| Input | Path | Identity | mtime (unix seconds) |
| --- | --- | --- | --- |
| family lock | `family.lock` | blake3:2b9fdced1effaaf7d0e9eefe5381b0e844bd5194fd412963f536f83bbb26d0a5 | excluded (portable variant) |
| hub manifest | `repos.manifest.toml` | blake3:46ed9d45fe7aa08d5b2d0574f176d4368d236873183f1ec77e7345cd93717159 | excluded (portable variant) |
| release index | `docs/release.md` | `Last reviewed: 2026-08-25` line only | excluded (portable variant) |
| product-gap register | `docs/assurance/product-gaps.md` | blake3:6682750e21e72d3735c1b4183a7406fb8b716b4108a9f664eae4413d666a2550 over the 46 `release.*` crosswalk rows + G-id list only | excluded (portable variant) |
| fast check report | `.bullet-family/check-fast.json` | absent | excluded (portable variant) |
| required check report | `.bullet-family/check-required.json` | absent | excluded (portable variant) |
| gate catalog | `src/check/prerequisites.rs` (compiled into `bullet-family 0.1.0`) | check report schema 3 | — |
| claim rows | `src/check/truth/rows.rs` + `rows/gates.rs` + `rows/ungated.rs` (compiled into `bullet-family 0.1.0`) | 46 gate rows + 1 ungated rows over 18 G-ids | — |
