# Gap register — canonical spec vs `neverhuman/bulletfarm` main `1ea7aa60`

Revision 2 (2026-09-18): 366 rows; every BF3-001..030 criterion (90) and every AT/HF/CF scenario (80) appears exactly once; each row has a stable `row_id`. Counts: deferred_by_plan 34, done 8, missing 256, not_applicable 1, partial 67. Validate with `python3 docs/gaps/check.py`.

Status rules: `done` only with a file:line or test on main; `partial` names the exact remaining gap; `missing` has no evidence on main; `deferred_by_plan` only for G3 (BF3-023..030). Rows the critic refuted carry a `[critic: …]` note. Machine-readable copy: `register.json`. Milestones refer to `docs/plan/close-all-gaps.md`.

## contracts-store-auth (51 rows: done 2, missing 31, partial 18)

| row_id | ID | Status | Milestone | Size | Requirement | Evidence on main | Gap | In prior plan |
|---|---|---|---|---|---|---|---|---|
| R0001 | AT-001 | missing | M1 | S | Unknown dependency: a missing ID or deleted task file does not establish completion. | none | No dependencies table or readiness predicate on main. Restorable from 9f03362:src/jobs.rs:66 (LEFT JOIN tasks; p.id IS NULL counts as unmet) and 9f03362:tests/acceptance.rs:14; add a test that omits/deletes the predecessor and asserts UNKNOWN_DEPENDENCY at admission. | Stage 3 (unknown/deleted/cancelled/invalid prerequisites never mean completed) |
| R0002 | AT-002 | missing | M1 | S | Graph cycle: cyclic requirements reject plan activation. | none | Restore 9f03362:src/domain.rs:179 cycle_in_ids and wire it into activate_plan over the full proposed graph (at 9f03362 it was tested in isolation but never called from start_work, commands.rs:196). | Stage 3 (full graph activation) |
| R0003 | AT-003 | partial | M2 | S | Same command replay: same key/body returns the same operation. | tests/core.rs:81-109 (two POSTs, same id); src/commands.rs:111 | Actor-scoped key and authorization recheck on replay are missing (see BF3-003-AC02, BF3-004-AC02); replay status is 'accepted' rather than distinguishable 'replayed' (9f03362:commands.rs:141) — optional. | Stage 2 |
| R0004 | AT-004 | done | M2 | S | Changed command body with the same key rejects without mutation. | src/commands.rs:49-50 returns CommandConflict before any insert; test src/commands.rs:123 changed_body_conflicts asserts code COMMAND_CONFLICT | none (namespace fix tracked under BF3-003-AC02) | Stage 2 |
| R0010 | AT-010 | missing | M4 | M | Activation crash: a published inactive Git revision cannot dispatch half a graph. | none | Fault-injection test after Git publish and before SQL import, then after import and before admission, asserting no job is dispatchable and re-running activation adopts the same identity. | Stage 3 (partially) |
| R0011 | AT-011 | missing | M4 | S | Task deletion: deleting a source task file cannot set MERGED or unblock a dependent. | none | Test that removing a task from bf/control or the contracts table (blocked by immutable_contract trigger) leaves dependents blocked with UNKNOWN_DEPENDENCY. | Stage 3 |
| R0056 | BF3-002-AC01 | partial | M1 | M | Reject unknown schema versions, duplicate keys, missing IDs, cycles and unresolved mandatory checks. | src/digest.rs:16-23,81-89 parse_strict_json rejects duplicate keys; src/commands.rs:18-22 rejects schema_version!=3 and empty command_id (commands only) | No task/mission/profile validators, no cycle detection, CHECK_MISSING never raised (src/error.rs:16 defined but unused). Restorable from 9f03362:src/domain.rs (validate_task :50, cycle_in_ids :179, validate_profile :212), 9f03362:schemas/task.schema.json, 9f03362:fixtures/protocol/task.json, tests 9f03362:tests/acceptance.rs:14,23,107. | Stage 1 (domain contracts and semantic validation) / Stage 2 (reject duplicate keys, unknown fields, invalid references) |
| R0057 | BF3-002-AC02 | missing | M1 | M | Task, planning, verifier and shadow job variants obey their role-specific requirements (Appendix A Job contract). | none (no jobs table or job validator on main) | Restore jobs schema CHECK constraints from 9f03362:migrations/001_core.sql:60-81 (purpose/lane/executor_kind/profile nullability, one_current_writer) and add a pure validator + tests for planning_job/verification_job/shadow variants; 9f03362 had no shadow/review variant tests. | Stage 1/Stage 3 (job identity/purpose/generation persisted before launch) |
| R0058 | BF3-002-AC03 | partial | M1 | S | Exact-byte SHA-256 digests and migration provenance cannot be silently confused with old canonical digests. | src/digest.rs:7 sha256_hex over raw bytes; src/commands.rs:30,67-69 stores body_sha of exact received bytes | No digest_scheme label stored for any contract on main (no contracts table); no importer that retains old algorithm labels. Restorable: 9f03362:migrations/001_core.sql:27 `digest_scheme CHECK(='sha256_exact_utf8_v3')` and git_oid stored beside raw_json. CF07 importer itself is BF3-021. | Stage 2 (migrate forward/import with provenance) |
| R0059 | BF3-003-AC01 | missing | M2 | S | A failed transition rolls back state and audit/outbox together. | src/commands.rs:66-75 performs two autocommit executes (commands, operations) with no transaction; migrations/001_core.sql has no events/outbox table | Wrap authorize -> version check -> mutate -> audit/event -> operation in one `c.transaction()` inside the DB worker; add events table; port 9f03362:tests/hardening.rs:269 failed_storage_rolls_back_mission_event_and_operation (trigger-injected failure leaves zero rows in every table). | Stage 2 (atomically authorize/check versions/mutate/audit/create operations); plan §1 audit names 'command/operation inserts not atomic' |
| R0060 | BF3-003-AC02 | partial | M2 | S | Same actor/key/body returns one durable operation; changed body conflicts. | src/commands.rs:33-57 replay/conflict; unit tests src/commands.rs:111 replay_same_body_is_idempotent, :123 changed_body_conflicts; tests/core.rs:30 http replay returns same id | Idempotency key is command_id alone (migrations/001_core.sql:17 PRIMARY KEY(command_id)); replay lookup :34-42 ignores actor, so another principal with the same id+body receives the first principal's operation, and a different body leaks existence. Change to PRIMARY KEY(actor_id,command_id) (9f03362:migrations/001_core.sql:127-130) and scope the replay query; add cross-actor test. | Stage 2; plan §1 audit names 'Replay lacks actor scope' |
| R0061 | BF3-003-AC03 | missing | M2 | M | Immutable contracts/candidates/evidence and job/selection relationships enforce the Appendix B structural reference. | migrations/001_core.sql defines only principals, sessions, commands, operations | Restore Appendix B tables and triggers (9f03362:migrations/001_core.sql full file, 002_workbench.sql, 003_evidence.sql) behind a schema-recognizing migration that fingerprints today's 4-table hub.sqlite and the board's bf.sqlite (src/board.rs SCHEMA) instead of only checking schema_migrations existence (src/storage.rs:93-100); add structural tests (immutable_contract, one_current_writer, evidence_binding). | Stage 2 (recognize original controller, reduced hub and coordination schemas; migrate forward) |
| R0068 | BF3-006-AC01 | missing | M4 | L | Activation protocol: validate the full proposal, persist publication intent, publish/confirm exact Git objects on refs/heads/bf/control, atomically import the validated graph and active revision, then admit jobs; a crash between Git and SQL leaves inert metadata, never a runnable partial graph; repeating the same activation adopts the same identity. | none (src/gitutil.rs:5-36 is a bare git wrapper; no plan/activation code) | Implement activate_plan with a durable publication_intents record, expected-old-ref publish, confirm-by-readback, single-transaction import, and crash-injection tests at each boundary. 9f03362 only committed a fixture `bf/control` branch (src/jobs.rs:307-315) and imported in start_work (commands.rs:196-205) with no intent record or crash test. | Stage 3 (full graph activation) — Git intent branch, publication intent and expected-old refs are not mentioned in the plan |
| R0069 | BF3-006-AC02 | missing | M4 | M | Unknown, cancelled or known-reverted prerequisites do not satisfy readiness. | none | Restore dependencies + dependency_invalidations tables and the readiness predicate (9f03362:src/jobs.rs:66: missing predecessor, revision mismatch, phase != condition, active invalidation all block) and add tests for each branch including cancelled/superseded phases. | Stage 3 (unknown/deleted/cancelled/invalid prerequisites never mean completed) |
| R0070 | BF3-006-AC03 | missing | M4 | M | Metadata-only refs use expected-old forward updates and do not trigger code deployment. | none (no update-ref usage on main or at 9f03362, which used `checkout -b`) | Add a gitutil helper `git update-ref refs/heads/bf/control <new> <old>` with an ancestry (forward-only) check, a stale-old-ref -> STALE_VERSION path, and a test; document/verify that bf/control is excluded from deploy triggers in the forge configuration. | no |
| R0151 | HF01 | missing | M1 | M | Unknown or unadmitted profile rejects launch; no default provider is silently substituted. | none (src/main.rs:264 only checks provider name; src/error.rs:14 UNKNOWN_PROFILE never raised) | Profile registry keyed by digest (9f03362:migrations/001_core.sql:45 profiles) with admission that raises UNKNOWN_PROFILE for any job whose profile_digest is not registered and admitted; 9f03362 hardcoded one PROFILE_JSON (jobs.rs:186) and never tested the negative. | Stage 3 (inputs are admitted repository/owner/task/profile/grant/limits) / Stage 5 |
| R0166 | HF16 | missing | M2 | S | A mutable trace outcome cannot modify authoritative immutable evidence or effect records. | none (no evidence/effects tables on main) | Restore evidence_no_update/no_delete and effect_identity_immutable triggers (9f03362:migrations/001_core.sql:167-170) and port 9f03362:tests/hardening.rs:482 wrong_producer_and_stale_evidence_are_rejected. | Stage 1/Stage 2 (immutable artifacts) |
| R0269 | §8.1-a | missing | M1–M3 | S | Numeric rules: JSON integers within JS safe range, money as nonnegative checked micro-unit integers with currency, checked arithmetic (reject overflow), decimal settings as strings, no float budget arithmetic. | src/digest.rs:48-58 accepts any i64/u64/f64 into serde_json::Value; Cargo.toml has no [profile.release] overflow-checks | Add a safe-integer bound (\|n\| <= 2^53-1, reject non-integer where integer required) in parse_strict_json or the typed validators; add a Micros{amount,currency} type using checked_add/checked_sub; set overflow-checks=true for release. [critic: The cited evidence is itself the refutation: src/digest.rs:48-58 accepts any i64/u64/f64 into serde_json::Value with no safe-integer bound, no integer-required check and no money type; Cargo.toml has no [profile.release] overflow-checks and no checked-arithmetic type exists anywhere in src/. The onl] | Stage 2 (reject unsafe integers) |
| R0270 | §8.1-b | partial | M1–M3 | S | Contract/profile digests are SHA-256 of exact UTF-8 bytes; duplicate keys and invalid encoding are rejected before interpretation; digest and enclosing commit stored outside the bytes. | src/digest.rs:11 require_utf8, :16-23 parse_strict_json, :84-86 duplicate key rejection; used at src/commands.rs:12-13 | No regression test on main for duplicate keys or invalid UTF-8 (9f03362:tests/acceptance.rs:107 and tests/hardening.rs:120 were pruned); outside-the-bytes storage of digest+git_oid only applies once contracts table exists. | Stage 2 (preserve exact received bytes; never hash reserialization) |
| R0271 | §8.1-c | partial | M1–M3 | S | Timestamps are UTC ISO-8601; hub-assigned committed event sequence is authoritative ordering; provider timestamps are observations. | src/commands.rs:65 and src/hub.rs:84-85 use Utc::now().to_rfc3339() | No events table or committed sequence on main (restorable 9f03362:migrations/001_core.sql:131-135 events(seq AUTOINCREMENT, producer_sequence UNIQUE)). | Stage 2 (event stream with reconnect cursor) |
| R0272 | §8.2 | missing | M1–M3 | M | Mission contract identifies owner, allowed repositories/domains, outcome/non-goals, parent requirement IDs, delivery goal, planning mode, grant reference, budget accounts, assembled acceptance obligations and notification/window policy; planning begins under the authorized mission and is funded. | none | No missions table or validator on main; 9f03362:migrations/001_core.sql:18-23 missions stored only owner/domain/goal/version/paused and contract_json='{}' (commands.rs:160). Define the mission JSON shape, validator and create_mission payload per §20.2. | Stage 1 (domain contracts) / Stage 4 (persist requirements/acceptance/base/scope/checks/full graph) |
| R0273 | §8.3 | missing | M1–M3 | M | Task contract carries id/revision/lineage/mission, one repo, owner/domain, kind/class, objective/non-goals, requirement IDs, typed prerequisite revisions, write paths/interface keys, acceptance with trusted check refs, gate/risk/route refs, granted allowance, stop conditions, context refs; code delivery_goal is pr_ready\|merged; investigations end at evidence_accepted; check IDs must resolve to protected runnable checks or a manual owner before live execution. | none | Restorable from 9f03362:src/domain.rs:50-177 validate_task (fields, kind, limits, write_paths, delivery_goal, acceptance needs check_ids or manual_owner_id) — but it never resolved check IDs against a protected check registry; add resolution + CHECK_MISSING at activation and a test. | Stage 1 (domain contracts and semantic validation) |
| R0274 | §8.4 | partial | M1–M3 | M | Scope paths: file path exact, directory ends in `/`, no implicit globs; reject absolute, `.`/`..`, backslashes, NUL/newline and ambiguous paths; compare segments not prefixes; renames need both paths; explicit rules for symlinks, gitlinks, mode changes, generated code and policy files. | src/board.rs:107-143 normalize_path (rejects absolute, backslash, NUL, empty/./.. segments, bare globs); :153-157 overlaps() is segment-wise; test src/board.rs:1100 | Applies only to board claims, not task contracts; does not reject newline (:109); accepts `dir/**`/`dir/*` as `dir/` (:115-118) and `.` as whole repo (:119-121), contrary to 'no implicit glob semantics'; no rename/symlink/gitlink/mode/generated/policy-file rules. 9f03362:src/domain.rs:20-36 validate_write_path rejected newline but also lacked the latter rules. | Stage 1 (contracts) / Stage 6 (seal rejects out-of-scope or unsafe paths) |
| R0275 | §8.5 | missing | M1–M3 | M | Execution profile is immutable, digest-referenced configuration (harness/adapter/version, one transport, model/settings, context/prompts/skills/memory, engine ref, environment/hardware, delegation ceilings); a profile is not a grant; secrets never appear in it; admission intersects current policy, capabilities, budget and sandbox certification. | none | Restore profiles table + immutability trigger (9f03362:migrations/001_core.sql:45,165) and validate_profile (9f03362:src/domain.rs:212 rejects grant_id); add full-shape validation per Appendix A profile example, a registry/admission API and secret-reference resolution from protected settings. | Stage 3 (admitted profile) / Stage 5 (pinned Codex profile) |
| R0276 | §8.6 | missing | M1–M3 | M | Candidate binds exact commit/tree/base, contract, producer and artifact manifest; a versioned current Selection determines eligibility without implying a check passed; Evidence and CompletionReceipt are append-only and never rewritten to look successful. | none | Restore candidates/selections/evidence/artifacts tables and immutability triggers from 9f03362:migrations/001_core.sql:92-111,163-168 and 003_evidence.sql binding triggers; receipt content rules are BF3-011's obligation. | Stage 1/Stage 2 (immutable artifacts) / Stage 6 |
| R0277 | §AppendixA | missing | M1–M3 | M | Seven strict v3 schema files (task, profile, job, receipt, command, job result, build checkpoint) are the shape contracts; Rust and TypeScript command/response types are generated from one source. | web/src/wire.ts:1-3 typed Command envelope (claims generation); web/src/contracts.ts hand-written; scripts/ contains only build-web and check | No schemas/ directory on main (9f03362 had only schemas/task.schema.json); scripts/generate-contracts referenced by wire.ts does not exist, so wire.ts is a stale artifact of 9f03362's ts_rs derive (commands.rs:10); no BUILD_CHECKPOINT.json on main. Add the seven schemas, the generator, and a CI check that wire.ts matches. [critic: web/src/wire.ts:1 says 'Generated from Rust; run scripts/generate-contracts' but scripts/ contains only build-web and check; wire.ts:2 lists 14 CommandKinds (run, remember_project, edit_draft, start_work, ...) while the hub's KINDS at src/commands.rs:9 is note/release/stop and rejects every wire.ts ] | Stage 2 (generate Rust/TypeScript commands and responses) / Stage 1 (BUILD_CHECKPOINT.json) |
| R0278 | §7-a | partial | M1–M3 | S | One Cargo package with a library and `bf` binary; roles serve, runner and client commands; compiled static web assets served from the hub. | Cargo.toml [[bin]] bf + src/lib.rs; build.rs embeds web assets; src/api.rs:45,167-196 serves them with CSP | No `runner` role subcommand (src/main.rs:28-115); runner is BF3-008/009 scope but the CLI shape belongs here. | Stage 3/Stage 5 (runner under trusted supervisor) |
| R0279 | §7-b | partial | M1–M3 | S | Local-disk SQLite with one bounded DB command worker, foreign keys, short transactions, busy timeout, durable acknowledged mutations; WAL enabled only after verifying the linked runtime has the documented WAL-reset fix; no SQL write transaction spans Git/model/network/compilation. | src/storage.rs:18 sync_channel(64) single worker, :49-67 saturation rejects, :80-86 pins SQLite 3.53.2 + source id, :88 busy_timeout, :90 PRAGMA foreign_keys/WAL/synchronous=FULL; tests/core.rs:21 doctor_reports_pinned_sqlite; docs/implementation-plan.md Stage 2 records the 3.53.2 WAL-reset check | none for hub.sqlite (second writable bf.sqlite tracked under §7-d; the no-I/O-in-transaction rule must be re-verified when the controller returns) [critic: The worker, pin (src/storage.rs:80-86; verified against the bundled libsqlite3-sys 0.38.2 header: 3.53.2 / 2026-06-03 source id), busy_timeout and PRAGMAs hold. 'Short transactions' and 'durable acknowledged mutations' do not: the only mutation path, src/commands.rs:66-75, issues two autocommit INSE] | Stage 2 |
| R0280 | §7-c | done | M1–M3 | S | An OS instance lock prevents two hub processes on the same store. | src/hub.rs:43-51 try_lock on hub.lock; tests/core.rs:12 second_hub_is_refused (RESOURCE_CONFLICT) | none | Stage 2 |
| R0281 | §7-d | missing | M1–M3 | L | One hub authority and one database: CLI/TUI mutations go through the authenticated hub API and never open a second writable store. | src/board.rs:166-175 Board::open writes bf.sqlite directly (synchronous=NORMAL), :692 BOARD_FILE; src/main.rs:169-244 claim/heartbeat/release/note call board directly; src/main.rs:255 prs reads bf.sqlite; hub uses hub.sqlite (src/hub.rs:54) | Implement note/release/claim/heartbeat as /v3/commands kinds (src/commands.rs:9 declares note/release/stop but returns NOT_IMPLEMENTED :59-64), migrate bf.sqlite entries/claims into the hub schema with provenance, make CLI/TUI clients of the API, retire direct writes. | Stage 2 (CLI/TUI mutations use authenticated hub API, never another writable authority; migrate coordination schema) |
| R0282 | §13-a | missing | M1–M3 | S | A consequential request records three identities: authenticated actor, initiating human and delegated capability. | migrations/001_core.sql:16-23 commands stores actor_id only | Add initiating_human_id and capability/grant_id columns to commands (and jobs/effects), populated from the session's principal and the resolved grant, never from the payload. | Stage 2 (partially: agents-for-admins distinct) |
| R0283 | §13-b | missing | M1–M3 | M | Roles administrator/engineer/observer narrowed by repository membership and domain grants; actor type independently human/agent/runner/verifier/publisher. | migrations/001_core.sql:6 principals.kind IN ('human','agent'); no memberships/repositories/domains tables | Restore principals kinds, repositories, memberships(role) and domains from 9f03362:migrations/001_core.sql:7-17 and require_membership (9f03362:src/hub.rs:287-306); remove the hardcoded 'repo-demo' membership check (9f03362:commands.rs:133). | Stage 2 |
| R0284 | §13-c | partial | M1–M3 | S | All reads, SSE subscriptions, artifact transfers, commands and operation-status responses apply scope checks; inaccessible object existence is not exposed through differentiated errors or unfiltered global counts. | src/commands.rs:101 returns AUTH_REQUIRED identically for missing and unowned operations | Replay conflict on a global command_id namespace leaks existence (BF3-003-AC02); every new read endpoint must reuse one scope predicate; add a test matrix (anonymous, revoked, other-owner, observer) per route. | Stage 2 / plan §4 Authorization negatives |
| R0285 | §13-d | missing | M1–M3 | L | A grant identifies issuer, subject, permitted actions, repositories/domains, data classification, expiry, revocation and allowance references, resolved from protected state; an approval binds exact action+subject, selection version, policy, environment, actor and expiry, and a changed subject needs a new approval. | none | Restore grants and decisions tables (9f03362:migrations/001_core.sql:46-51,143-147) and extend grants with repositories/domains/data classification/actions; implement approval binding by subject digest + selection version; no fixture 'demo-grant' with 2099 expiry (9f03362:hub.rs:146-147) may seed a live store. | Stage 3 (current grants) / Stage 4 (one approval binds exact revision) |
| R0286 | §13.1 | missing | M1–M3 | M | Capability classes: change-authority actions require a designated authenticated human; revocations apply at admission and before new privileged dispatch; a pinned old policy is not immunity; a stricter check requirement is reconciled before delivery and a broader policy does not widen a running task. | none | Grant/revocation recheck at admission and at dispatch (9f03362:src/jobs.rs:11 grant(), :234-235 dispatch recheck, :491 current()); policy-change reconciliation hooks; tests AT-009-style plus 'broader policy does not widen running task'. | Stage 3 (current grants; pause stops admission) |
| R0287 | §13-e | partial | M1–M3 | S | Browser sessions need authenticated TLS, secure cookies and CSRF/origin protections; CLI and runner credentials stay outside source repositories and candidate sandboxes. | src/api.rs:54-67 Host/Origin/Sec-Fetch-Site checks; :189-191 CSP; bearer header not cookie (no ambient CSRF); loopback-only bind src/main.rs:420; endpoint.json 0600 outside any repo :439-443 | Token placed in URL fragment (src/main.rs:358,:452) lands in browser history; remote access relies on the planned SSH forwarding rather than TLS (plan §2) — record that decision; no test for Origin/Host rejection on main (9f03362:tests/hardening.rs:95 pruned). | Stage 2 (strict Host/Origin) / plan §2 (SSH forwarding) |
| R0288 | §13-f | partial | M1–M3 | M | Local bootstrap uses a private loopback/socket endpoint and a generated local secret; `bf init` creates configuration only and confers no remote rights, funding or provider consent; an administrator can issue one-time enrollment material exchanged for a revocable repository-scoped runner identity. | src/main.rs:416-458 serve: loopback bind, generated secret, private endpoint.json; src/main.rs:374-396 connect refuses non-loopback endpoints | No `bf init`/setup subcommand; bootstrap secret is not one-time and is not exchanged for a scoped identity; no runner enrollment or revocation path. | Stage 2 (private bootstrap) / plan §2 first-launch flow; runner enrollment Stage 3/5 |
| R0289 | §13-g | missing | M1–M3 | S | Source text, issues, logs and imported Markdown are untrusted content even when they contain JSON resembling a command; provider refusals are not instructions to rotate identities or providers. | src/board.rs:728 regenerate writes AGENT_CHAT.md as a generated digest (no read-back as authority); AGENTS.md:32 policy statement | No importer exists yet; when todo/*.md or Markdown import lands (§9-c) it must produce proposals only, never grants/approvals/completion; add a negative test with command-shaped JSON in imported text. [critic: src/board.rs:728 regenerate() is a write path; the digest file IS read back (src/board.rs:677 write_digest reads the existing file to preserve text after END_MARKER; :1046 reads DIGEST_FILE for `bf board`), but nothing interprets it, and no importer, no command-shaped-JSON negative test and no provi] | Stage 2 (imported prose cannot create grants, approvals or completion evidence) |
| R0290 | §6.1 | missing | M1–M3 | M | Capabilities assist_only/verified_pr/policy_merge/production_observed are admitted per repository/task class and profile; the first code mission defaults visibly to pr_ready; a requested merged/production_observed goal cannot silently downgrade. | none | Per-repository/task-class capability admission table and a goal-downgrade guard; 9f03362:src/commands.rs:158 validated the goal enum and :168 defaulted 'pr_ready' but had no capability admission. | Stage 6 (first capability verified_pr; automatic merge deferred) / Stage 4 (default) |
| R0291 | §6.2 | partial | M1–M3 | S | `bf serve --local` runs the same authority for private repositories; a team-managed repository has exactly one configured managed authority with outbound-connected runners. | src/main.rs:107-112,416-458 `bf serve` loopback hub is the single authority for the local case | No `--local` flag naming, no managed-authority configuration for team repositories, no outbound runner; team mode is BF3-014 but the single-authority configuration belongs to auth. | plan §2 (laptop connects to independent Ubuntu hub via SSH) / G2 |
| R0292 | §9-a | missing | M1–M3 | M | One protected controller-written branch per repository, refs/heads/bf/control, holds mission files, immutable numbered plan/task revisions and adopted decisions; ordinary code stays on PR branches; metadata updates are forward-only and never deploy. | none | Define the branch layout (missions/, tasks/<id>/<rev>.json, decisions/), the writer (credential-separated, hooks disabled as src/gitutil.rs:15 already does), and read-back verification of the exact object digests recorded in contracts.git_oid. | no |
| R0293 | §9-b | missing | M1–M3 | M | Substantive revisions produce an explicit preserve/cancel/supersede mapping; active work stays on its old contract only while still permitted and never silently inherits changed scope; a current revocation takes precedence over historical authorization; immutable documents exclude their own commit hash. | none | revise command kind with old-digest check and mapping record; contracts immutable with git_oid stored outside raw_json (pattern at 9f03362:migrations/001_core.sql:24-29); full respecification flow is BF3-015. | G2 (BF3-015) per plan §5; not staged |
| R0294 | §9-c | partial | M1–M3 | M | Import existing todo/*.md preserving IDs, owners, domains, class, blockers, gate hints and provenance; unknown blockers block; cancelled/superseded tasks are not completed prerequisites; file deletion never sets merged; AGENT_CHAT.md may be a generated digest, not a coordination authority. | src/board.rs:728 regenerate() emits AGENT_CHAT.md as a digest only | No todo/*.md importer on main or at 9f03362; importer must create proposed tasks with provenance and no authority (ties to CF07/BF3-021 for digest-scheme labels). | Stage 2 (import with provenance; imported prose cannot create grants) |
| R0295 | §9.1 | missing | M1–M3 | M | A code prerequisite requires its integrated receipt plus validity at the selected current source base; known invalidations, interface/version preconditions and required sanity checks are checked; ancestry alone is insufficient; unknown applicability blocks or creates a replan decision. | none | dependency_invalidations table and preconditions_json (9f03362:migrations/001_core.sql:37-44,152-155) plus the check at admission; revert observation itself is BF3-019/CF04. | Stage 3 (dependency readiness) / G2 (CF04) |
| R0296 | §20.2-a | partial | M1–M3 | M | Command envelope: schema_version 3, command_id, kind, target_id, expected_version, kind-specific payload; creation uses null target/version; updates require a target and positive expected version; the actor is never a payload field; exactly one command endpoint. | src/commands.rs:14-29 validates schema_version, command_id, kind; src/api.rs:43 single POST /v3/commands; web/src/wire.ts:3 envelope type; web/src/App.tsx:106 sends target_id/expected_version | target_id and expected_version are never parsed or enforced (STALE_VERSION never raised); unknown envelope fields accepted; no body size bound; payload not typed per kind. Restorable: 9f03362:src/commands.rs:10-23 deny_unknown_fields typed Command, :116 64 KiB bound, :164,:180,:193 expected_version checks. | Stage 2 (POST /v3/commands: typed command, exact raw bytes, command ID, target, expected version) |
| R0297 | §20.2-b | missing | M1–M3 | L | Command kinds create_mission, activate_plan, pause/stop/cancel, take, submit_human, return, revise, resolve_decision, grant_allowance and request_merge each enforce their payload guard (exact artifacts/digests, current grant, human-only where stated). | src/commands.rs:9 KINDS = note/release/stop, all returning NOT_IMPLEMENTED (:59-64); web/src/wire.ts:2 lists the 9f03362 kind set which the hub rejects with INVALID_CONTRACT (:27-29) | Implement this area's kinds (create_mission, activate_plan, pause/stop/cancel with expected_version, revise, resolve_decision, grant_allowance) in the transactional kernel; 9f03362:src/commands.rs:144-249 covers create_mission/pause/stop/cancel/resume and denied take/submit_human/grant/resolve stubs but has no activate_plan/revise/return/request_merge. take/return are BF3-015; request_merge is BF3-012. | Stage 2/3/4 partially; take/return G2; request_merge Stage 6 |
| R0298 | §20.2-c | partial | M1–M3 | S | 202 Accepted means a durable operation record exists; replaying the same actor/key/body returns that operation; reusing the key for different content conflicts; current read authorization is rechecked before returning a historical replay. | src/api.rs:159-162 returns 202 with the operation; src/commands.rs:43-57 replay; tests/core.rs:93,109 | Actor scoping and replay authorization recheck (BF3-003-AC02, BF3-004-AC02). | Stage 2 |
| R0299 | §20.2-d | partial | M1–M3 | S | Errors use INVALID_CONTRACT, UNKNOWN_DEPENDENCY, DEPENDENCY_INVALID, UNKNOWN_PROFILE, CHECK_MISSING, STALE_VERSION, STALE_GENERATION, STALE_SELECTION, POLICY_DENIED, AUTH_REQUIRED, BUDGET_UNAVAILABLE, RESOURCE_CONFLICT, OUTCOME_UNKNOWN, STORAGE_UNAVAILABLE with the smallest useful authorized explanation and a correlation ID; not every error is retried identically. | src/error.rs:7-62 defines all 14 codes; :64-77 HTTP status mapping; src/main.rs:133-140 CLI exit codes distinguish conflict/refusal vs bad input | No correlation ID in the error body (src/error.rs:100-103); OUTCOME_UNKNOWN maps to 202 rather than an error status (:74); non-spec COMMAND_CONFLICT code for key reuse (:59) — decide RESOURCE_CONFLICT or document; client retry guidance per code absent. | no |
| R0300 | §20.3-a | partial | M1–M3 | L | Endpoints: POST /v3/commands, GET /v3/operations/{id}, /v3/missions/{id}, /v3/tasks/{id}, /v3/tasks/{id}/why, /v3/work?owner=&domain=&phase=, /v3/events?after=, /v3/runners/enroll, /v3/runners/{id}/poll, /v3/jobs/{id}/heartbeat\|events\|result, /v3/artifacts/uploads, /v3/artifacts/{issued-id}, POST /v3/hooks/forge. | src/api.rs:43-44 commands + operations only (plus non-spec /v3/bootstrap, /v3/session, /v3/doctor); web/src/App.tsx:45,48,63 calls /v3/projects,/v3/drafts,/v3/work,/v3/events which 404 via the asset fallback (:45,:173-174) | This area: scoped, typed, paginated reads for missions/{id}, tasks/{id}, tasks/{id}/why, work and events (9f03362:src/api.rs had projects/drafts/work/events). Runner/job/artifact/hook endpoints belong to BF3-007..012 but share the same auth middleware. | Stage 2 (interface table) |
| R0301 | §20.3-b | missing | M1–M3 | M | Job endpoints validate enrolled producer, assignment generation, epoch and purpose; a verifier result is accepted only for the expected check job and subject; ordinary runner credentials cannot mint verifier evidence; grant changes and human decisions may share the transport but require the distinct human path; no arbitrary shell/SQL endpoint. | src/api.rs:35-47 exposes no shell/SQL endpoint | No job endpoints, runner/verifier principals or producer binding on main; 9f03362:migrations/003_evidence.sql evidence_binding trigger and hub.epoch (src/hub.rs:55 exists on main) are the restorable pieces; enforcement of producer/generation/epoch is BF3-009/011 scope. [critic: The only clause the evidence supports is a negative: src/api.rs:35-47 exposes no shell/SQL route. Every positive obligation has no code: no job endpoints, no runner/verifier principal kinds (migrations/001_core.sql:6 allows only human/agent), no producer or purpose validation, and Hub.epoch (src/hub] | Stage 3 (runner endpoints bound to generation) / Stage 6 |
| R0302 | §20.3-c | missing | M1–M3 | M | SSE uses committed sequence cursors and a permission-filtered snapshot on compaction/reconnect; expired or unauthorized subscriptions end or refresh; provider timestamps are not ordering authority. | none (no /v3/events route; web/src/App.tsx:63 expects one) | Events table with committed seq, GET /v3/events?after= streaming with per-actor scope filter, snapshot on cursor gap, subscription termination on session expiry/revocation, bounded buffering; 9f03362:tests/hardening.rs:625 slow-stream bound test is the restorable negative. | Stage 2 (event stream: bounded updates, reconnect cursor, snapshot recovery) |

## operator-surface+contracts-store-auth (7 rows: missing 6, partial 1)

| row_id | ID | Status | Milestone | Size | Requirement | Evidence on main | Gap | In prior plan |
|---|---|---|---|---|---|---|---|---|
| R0005 | AT-005 | missing | M3 | M | Observer cannot spend; a runner cannot impersonate an owner or a verifier. | none (single 'owner' principal, no roles administrator/engineer/observer, no runner/verifier actor types on main) | Needs role + actor-type model (spec §13) and per-kind authorization; verifier evidence acceptance bound to enrolled producer is BF3-011's side. Test shape restorable from 9f03362:tests/hardening.rs observer_cannot_activate_or_dispatch_task_repository. \| Restore memberships/roles (9f03362:migrations/001_core.sql:12-16, hub.rs:287 require_membership) and port 9f03362:tests/hardening.rs:511 observer_cannot_activate_or_dispatch_task_repository; add runner-impersonation test once job endpoints exist. | Stage 2 (Authorization negatives: cross-owner reads) |
| R0006 | AT-006 | missing | M3 | M | Unauthorized event/artifact reads fail even when the caller knows the hash/ID, without leaking existence. | none (no /v3/events, /v3/artifacts or /v3/tasks routes on main; src/api.rs:34-47) | Implement scope-checked event and artifact routes returning non-disclosing 404/403; test a second principal reading a known artifact id. \| Implement scope-filtered GET /v3/events?after= and GET /v3/artifacts/{issued-id} where content hash is identity not permission; test that a second principal with the artifact hash/event id is refused without differentiated error. | Stage 2 ("Owner-scoped, typed, paginated reads") |
| R0062 | BF3-004-AC01 | missing | M3 | M | An agent principal initiated by an admin cannot exercise interactive human-only authority (grant changes, impersonation, required approvals, policy promotion). | src/main.rs:245-253 (`bf stop` refuses unless identity::current().provider=="human"); src/identity.rs:69-88 (declared --as name never hides observed provider); tests/cli_wiring.rs:97 stop_refuses_pid_one; migrations/001_core.sql:4-8 principals.kind CHECK(human\|agent) exists but is never consulted | Hub has one principal 'owner' (001_core.sql:31) and no capability class on command kinds; commands.rs:11-82 never inspects principal kind; the only guard is a /proc ancestry heuristic in the CLI (bypassable via setsid/undetected wrapper). Need agent/human principal kinds, a human-only kind set (take, submit_human, return, revise, resolve_decision, grant_allowance, request_merge) rejected with POLICY_DENIED for kind='agent', and tests. Restorable pattern from 9f03362:tests/acceptance.rs at005_cf06_agent_cannot_take / hf17_agent_cannot_grant. \| Restore active_actor kind check and human-only gate in the kernel (9f03362:src/commands.rs:132-134, hub.rs:279), add agent/runner/verifier/publisher principal kinds, and port 9f03362:tests/acceptance.rs:71 at005_cf06_agent_cannot_take and :87 hf17_agent_cannot_grant. | Stage 2 ("Agents acting for admins remain distinct from human-only authority") |
| R0063 | BF3-004-AC02 | partial | M3 | M | Repository event/artifact reads and historical operation replays recheck current access before returning. | src/commands.rs:84-103 (GET operation filtered by actor_id, AUTH_REQUIRED otherwise); src/hub.rs:93-104 lookup_session rejects revoked/expired/inactive; src/commands.rs:33-57 replay returns stored op only for same command_id+digest; tests/core.rs:30 http_auth_and_command_replay | No repository membership/scope model: replay and reads are scoped only to the principal id, not to current membership or grant; no event/artifact routes exist to recheck. Add memberships + scope check inside the replay path and on every read route. Restorable from 9f03362:tests/hardening.rs replay_rechecks_current_principal_and_membership, cross_owner_target_and_operations_are_denied. \| Replay path src/commands.rs:34-57 rechecks nothing (no actor filter, no target authorization); 'current access' is only session validity because there are no memberships/roles; no event/artifact read endpoints exist to scope. Restorable: 9f03362:src/commands.rs:136-142 authorize_target/authorize_result on replay; tests 9f03362:tests/hardening.rs:181,197. | Stage 2 ("historical replay rechecks current access"; Authorization negatives table) |
| R0064 | BF3-004-AC03 | missing | M3 | L | Local bootstrap is private (loopback/socket + generated local secret, not an unauthenticated listener). | src/main.rs:416-424 (serve refuses non-IPv4-loopback bind), :428 (random bootstrap secret), :434-447 (endpoint.json written 0600); src/api.rs:95-101 (bootstrap compares secret digest); src/api.rs:49-75 (Host/Origin/sec-fetch-site checks); src/hub.rs:37-53 (data dir 0700, single-instance lock); tests/core.rs:47-57 (401 without bearer), tests/core.rs:12 second_hub_is_refused | none for the private pilot; team/TLS bootstrap (`bf connect`) is a separate row. \| Add runner principal kind, enrollment code table, POST /v3/runners/enroll exchanging a single-use code for a revocable scoped token, revocation, and `bf runner enroll/start`. No prior implementation at 9f03362 either. \| No test asserts a body owner_id/actor field is ignored or rejected, and no command kind on main has an owner payload to test against. Add a negative test once create_mission exists. Restorable from 9f03362:tests/acceptance.rs body_owner_does_not_authenticate. \| No POST /v3/runners/enroll or runner principal kind (001_core.sql:6); bootstrap secret is reusable for the hub's lifetime, not one-time enrollment material; session token delivered via URL fragment (src/main.rs:358,:452); no test that body owner fields are ignored (9f03362:tests/acceptance.rs:154 body_owner_does_not_authenticate, commands.rs:157). | Stage 2 ("Private bootstrap, expiring revocable sessions, strict Host/Origin") |
| R0148 | CF06 | missing | M3 | M | An agent initiated by an administrator cannot approve itself; initiating owner is not authenticated human action. | none (no delegated-capability or initiating-human identity is recorded on sessions or commands; migrations/001_core.sql:9-30) | Record authenticated actor, initiating human and delegated capability on sessions/commands; approvals require actor kind human via interactive channel. Test with agent session created for an admin. \| Record initiating human and delegated capability separately from the authenticated actor and gate grant/approval kinds on the authenticated actor's kind, never on the initiator; port 9f03362:tests/acceptance.rs:71 at005_cf06_agent_cannot_take and add a grant-change variant. | Stage 2 |
| R0167 | HF17 | missing | M3 | S | An agent using the channel/command endpoint to self-approve a human-only decision is rejected for the agent principal. | none (resolve_decision/grant_allowance are not accepted kinds: src/commands.rs:9) | Covered by BF3-004-AC01 implementation plus a test posting grant_allowance/resolve_decision with an agent session and asserting POLICY_DENIED. \| resolve_decision/grant_allowance must check actor kind == human and designated role inside the kernel transaction; port 9f03362:tests/acceptance.rs:87 hf17_agent_cannot_grant. | Stage 2 |

## control-plane+contracts-store-auth (7 rows: missing 7)

| row_id | ID | Status | Milestone | Size | Requirement | Evidence on main | Gap | In prior plan |
|---|---|---|---|---|---|---|---|---|
| R0007 | AT-007 | missing | M4 | M | Concurrent admissions preserve bounded allocations (budget race). | none — src/storage.rs:18-60 serializes all writes through one worker, which is the primitive, but nothing admits jobs | Admission inside one Database::call transaction plus a multi-thread test (pattern exists at 9f03362:tests/hardening.rs:493 concurrent_duplicate_submissions_activate_once) asserting holds never exceed the limit \| Multi-client concurrent admission test against one account; the single DB worker (src/storage.rs) serializes writes but nothing proves conservation. | Stage 3 / §4 'Jobs: partial reservation' |
| R0008 | AT-008 | missing | M4 | S | Unreported cost stays reserved/uncertain and is never treated as zero. | none | usage_observations with 'unknown' state and reporting that never converts missing telemetry to 0; restorable base 9f03362:src/jobs.rs:527 (usage_observations insert) but that fixture recorded 0 as 'reported' \| Same as BF3-005-AC03; test that a job lost mid-run leaves its hold 'uncertain' across restart. | Stage 3 |
| R0009 | AT-009 | missing | M4 | M | An expired or revoked grant is not overridden by older policy at admission or in flight. | none — no grants table on main; sessions expire (src/hub.rs:96-108) but sessions are not grants | grants with expiry/revocation/invocation allowance checked at admission and re-checked at result collection; restorable from 9f03362:src/jobs.rs:11-17 grant(), :491-499 current(), tests/hardening.rs:244 revoked_expired_exhausted_grants_refuse_before_job_intent \| Restore grant expiry/revocation checks at admission and dispatch and port 9f03362:tests/hardening.rs:244 revoked_expired_exhausted_grants_refuse_before_job_intent. | Stage 3 ('current grants') |
| R0065 | BF3-005-AC01 | missing | M4 | L | Concurrent admission conserves all applicable account holds without double-counting hierarchical limits. | none — migrations/001_core.sql defines only principals/sessions/commands/operations; no budget tables or admission path on main | Add budget accounts/holds and an admission transaction on the single-writer Database (src/storage.rs:18) that checks every applicable scope (org/project, owner/domain, mission, lineage, window, upstream pool) as one constraint set; restorable in part from 9f03362:src/jobs.rs:18-25 (available()) and migrations/001_core.sql:52,112-119, but that version has one hard-coded 'demo-usd' account and no hierarchy \| Restore budget_accounts/budget_holds/completion_holds (9f03362:migrations/001_core.sql:52-55,112-120) and available() (9f03362:src/jobs.rs:18-25, single 'demo-usd'); add hierarchical accounts (mission/owner/repo) and a concurrent-admission test through the API. | Stage 3 (budget holds only; hierarchical limits not named) |
| R0066 | BF3-005-AC02 | missing | M4 | M | Writer use cannot consume the reserved verifier/reviewer envelope. | none | Separate completion_holds from writer budget_holds and refuse writer top-ups from the completion reserve; restorable from 9f03362:src/jobs.rs:97-135 (completion hold top-up, writer hold separate) — still needs a negative test proving a writer request cannot draw the reserve \| Restore completion_holds creation at implementation admission (9f03362:src/jobs.rs:137-138) and transfer to verify jobs (:397-402); port acceptance check writer_cannot_spend_completion (9f03362:tests/acceptance.rs:138). | Stage 3 (protected verification/review reserve) |
| R0067 | BF3-005-AC03 | missing | M4 | M | Unknown usage stays exposed; actual overruns are recorded in full and further admission is held. | none | Ledger states held/settled/uncertain plus an admission freeze when actual exceeds limit; restart path marking holds 'uncertain' is restorable from 9f03362:src/jobs.rs:458-467 and tests/hardening.rs:455 restart_keeps_unknown_occupancy_and_cost_reserved; overrun-beyond-limit recording and freeze are new \| Restore usage_observations with confidence='unknown' (9f03362:migrations/001_core.sql:121-126), recover() marking holds uncertain (9f03362:src/jobs.rs:458-463), actual_micros overrun recording that blocks new admission; port 9f03362:tests/hardening.rs:455. | Stage 3 (partial: 'unknown usage visible' default; overrun freeze not named) |
| R0147 | CF05 | missing | M4 | M | Mandatory completion reserve cannot fund extra writing; check/review jobs use its transferred hold without double billing. | none | Hold transfer at seal (restorable 9f03362:src/jobs.rs:388-402: completion_holds → 'assigned', verify job created with that hold) plus a test that a writer asking for the reserve is refused and the verify job bills once \| Same mechanism as BF3-005-AC02 plus a double-billing assertion (hold transferred, not duplicated) on the verify job. | Stage 3 |

## execution-and-verification (65 rows: deferred_by_plan 4, missing 58, not_applicable 1, partial 2)

| row_id | ID | Status | Milestone | Size | Requirement | Evidence on main | Gap | In prior plan |
|---|---|---|---|---|---|---|---|---|
| R0012 | AT-012 | missing | M4 | S | Truncated frame/final result cannot become accepted work end to end: job ends interrupted/failed with no candidate or evidence. | none on main (9f03362 tests/acceptance.rs:101 covered parsing only) | End-to-end fixture: fake adapter emits truncated final frame, assert job interrupted and zero candidates. | Stage 3 |
| R0013 | AT-013 | missing | M5 | M | Two attempts and a human clone cannot modify each other. | none | Per-job private clone jobs/{id}/{incarnation} with --no-local --no-hardlinks restorable from 9f03362:src/jobs.rs:187,336-347; add a test with two concurrent attempts plus a human clone. | Stage 5 ('private filesystem/Git'); §5 default 'private clones/no worktrees' |
| R0014 | AT-014 | missing | M5 | L | Generated code cannot read forge/reporting/release or host credentials. | none (gitutil.rs env_clear protects trusted git only) | Credential-separated supervisor: subscription auth held by the controller, candidate commands run with no auth files/env/FDs; tests that reads of ~/.codex, gh config, hub.sqlite and endpoint.json fail. | Stage 5 ('Trusted controller may access subscription auth, candidate commands cannot') |
| R0021 | AT-021 | missing | M6 | S | Verifier and delivery continue after the author lease ends. | none | Sealed-to-verify-to-publish chain restorable from 9f03362:src/jobs.rs:395-402; test with the author job terminated before verify starts. | Stage 3 / Stage 6 |
| R0026 | AT-026 | missing | M6 | M | A no-prompt profile does not allow wider scope or egress. | src/main.rs:100-103 defines `--yolo` on the stub with no policy behind it | Sandbox/network policy enforced outside the provider's own permission prompts; test egress and out-of-scope writes are blocked under the no-prompt profile; bind or drop `--yolo` (§10.1 forbids a universal yes). | Stage 5 |
| R0027 | AT-027 | missing | M7 | M | Provider auth/quota failure parks the job or takes an authorized backoff/fallback; no identity evasion via account pools or key fallback. | src/main.rs:321-327 doctor probes only `--version` and `gh auth status` | Classify auth/quota frames into an auth_required hold, forbid fallback accounts (Stage 5 rule), recorded fixtures. | Stage 5 (Codex only); second family after release |
| R0030 | AT-030 | missing | M6 | S | A writer summary or PASS cannot create trusted verification. | src/board.rs:869-887 proof_body records self-run command output as a note, not evidence | Evidence insert only from the verifier role via receipt-v3; test that a forged PASS in a result frame is ignored; keep `release --proof` labeled diagnostic. | Stage 6 |
| R0031 | AT-031 | missing | M6 | S | A same-name check from the wrong identity is rejected. | none | evidence.producer must equal the gate manifest's expected producer; restorable from 9f03362:migrations/003_evidence.sql and the hardening test. | Stage 6 ('wrong issuer') |
| R0033 | AT-033 | missing | M6 | M | An unchanged bug (no-op candidate) does not pass solely on an old broad green suite. | none | Require a targeted regression that fails on base and passes on the candidate (Stage 7 acceptance) plus a non-empty in-scope diff (9f03362:src/jobs.rs:361-366 diff check). | Stage 6 / Stage 7 |
| R0034 | AT-034 | missing | M6 | M | Missing or skipped required tests do not equal pass. | none | Required-suite identity manifest with discovered/executed/skipped counts checked; corpus version at 9f03362:src/verification.rs:30-52; generalize to cargo test/vitest/browser harness output. | Stage 6 ('expected discovery, required cases/assertions executing') |
| R0044 | AT-044 | missing | M6 | S | Incomplete artifact bytes never satisfy complete evidence. | none | artifacts(complete, digest, byte_count) with revalidation before use, restorable from 9f03362:src/delivery.rs:73-77 and tests/hardening.rs missing_artifact_blocks_publication_after_pass. | Stage 6 ('missing bytes') |
| R0071 | BF3-007-AC01 | missing | M4 | M | Malformed or truncated final frames cannot become success. | none | LF-framed JSONL with bounded buffer, strict JSON, complete final frame with matching job/generation required; seed only at 9f03362:src/hub.rs:263 parse_result_frame and tests/acceptance.rs at012_truncated_frame (parse-level, no stream framing). | Stage 3 / Stage 5 ('framing' qualification) |
| R0072 | BF3-007-AC02 | missing | M4 | M | Admission, execution end, candidate output and physical cleanup are recorded as distinct durable events. | none | Restorable from 9f03362:src/jobs.rs:196 job_admitted, :247 launch_intent, :393 candidate insert, :528 termination_observed; cleanup must be split from execution end (fixture finish() at :510-513 conflated them). | Stage 3 |
| R0073 | BF3-007-AC03 | missing | M4 | S | All fixture paths run without model credentials or network access. | scripts/check:2 and .github/workflows/ci.yml:8-9 carry no provider secrets, but no fixture execution path exists to run (tests/cli_skeleton.rs:35 asserts `demo` stays hidden) | Restore fake provider/forge journeys (9f03362:src/fixtures.rs, tests/acceptance.rs demo_basic_reaches_review_ready) and assert env_clear/no listener in the test. | Stage 1 / Stage 3 |
| R0074 | BF3-008-AC01 | missing | M5 | XL | Jobs cannot read/write other owners' workspaces, host credentials, hub state or privileged sockets. | src/runner.rs:18 'process control, not a live OS sandbox'; only src/gitutil.rs:8-12 env_clear for trusted git | OCI/bwrap profile under a trusted supervisor (private fs/git/tmp/caches, no $HOME, no hub.sqlite/endpoint.json, no sockets, network off) with explicit tests for paths/env/FDs//proc/sockets; 9f03362:src/qualification.rs:36-58 bwrap probe failed on this host so a compatible host is required. | Stage 5 |
| R0075 | BF3-008-AC02 | missing | M5 | L | Cleanup targets the exact recorded sandbox incarnation; unconfirmed processes remain occupied and capacity is reclaimed only on a trusted stopped receipt (§14.3); retention/cleanup requires exact job/workspace identity (§16.4). | src/runner.rs:223 returns OUTCOME_UNKNOWN when the target survives KILL (honest idiom) but there is no incarnation or occupancy record | launch_intents.incarnation and occupancy='unconfirmed' on restart restorable from 9f03362:src/jobs.rs:187,458-468; kill by cgroup/namespace incarnation rather than pgid; cleanup gated on job identity and disposition. | Stage 3 ('Stop remains Stopping until trusted termination proof'), Stage 5 ('stop descendants by incarnation; restart finds survivors') |
| R0076 | BF3-008-AC03 | missing | M5 | L | Native daemons and children cannot escape the registered job lifetime or shared write restrictions. | src/runner.rs:31,50-55 kill by process group only; :61 comment assumes fixture tools do not daemonize, so a setsid child escapes | cgroup/pid-namespace scoped lifetime (bwrap --die-with-parent/--new-session or systemd scope), one-writer checkout, native delegation disabled in the shipping profile. | Stage 5 |
| R0080 | BF3-010-AC01 | missing | M6 | L | Certify one live executor with exactly one selected transport (Codex `exec --json` fallback per plan); no TUI/menu/keystroke automation. | src/main.rs:259-272 `bf run` exits 3 'not implemented yet'; tests/cli_skeleton.rs:57 | Codex adapter behind the seam, pinned binary/version (plan notes 0.154.0 to 0.155.0 drift), no resume or API-key fallback, conformance fixtures with recorded JSONL. | Stage 5 |
| R0081 | BF3-010-AC02 | missing | M6 | M | Fresh session identity is proved for each job (new invocation, no inherited context), including a Prime session-veto response where applicable. | none | Assert a new thread/session id in exec --json output differs from any prior, reject resume flags, record identity in the launch receipt. | Stage 5 ('freshness') |
| R0082 | BF3-010-AC03 | missing | M6 | L | Cancellation, auth/quota, nested usage and required credential boundaries pass the selected profile fixtures. | src/runner.rs:13-86 has a cancel flag and 30 s deadline for trusted tools only | Per-job cancel handle returning confirmed_stopped\|unconfirmed, recorded auth/quota fixtures that park the job, usage-artifact parsing, credential boundary tests. | Stage 5 |
| R0083 | BF3-011-AC01 | missing | M6 | L | Author PASS, missing discovery, wrong producer or changed subject cannot satisfy checks. | none on main; src/board.rs:794-880 `bf release --proof` runs an author-chosen command and records exit/sha as a board note (diagnostic, must never be read as evidence) | Verifier job plus evidence table with producer FK and subject digest; corpus-only version restorable from 9f03362:src/verification.rs:10-57 and tests/hardening.rs early_exit_and_forged_completion_cannot_pass / wrong_producer_and_stale_evidence_are_rejected; must generalize beyond the embedded corpus. | Stage 6 |
| R0084 | BF3-011-AC02 | missing | M6 | M | Verification runs under its own identity and deadline and survives author exit. | none | Distinct verifier principal and separate deadline restorable from 9f03362:src/jobs.rs:186,437; execution must be asynchronous so author exit is actually exercised. | Stage 6 ('under separate authority') |
| R0085 | BF3-011-AC03 | missing | M6 | M | Prepublication and CI-only obligations are distinguished so publication is neither blocked circularly nor called fully verified too early. | none | Gate manifest location local\|ci\|both; task stays checking after the draft until CI-only evidence arrives (9f03362:src/delivery.rs:136 jumped to review_ready on fixture pass). | Stage 6 |
| R0098 | BF3-016-AC01 | missing | M7 | L | The same task restarts from an exact checkpoint with a fresh eligible second-provider context (Claude headless against the Codex fallback). | none | Checkpoint artifact (commit/tree plus decisions), second adapter behind the seam, restart fixture. | no (§5 'after release: qualify Cursor/Claude') |
| R0099 | BF3-016-AC02 | missing | M7 | M | Data policy, startup hooks, permission handling, cancellation and accounting remain explicit for the second family. | none | Per-profile declaration plus fixtures (hooks/MCP loading off, permission mode, cancel, usage). | no (after release) |
| R0100 | BF3-016-AC03 | missing | M7 | S | A second harness name using the same provider family does not satisfy the gate; the actual model family is recorded. | src/identity.rs:13-27 classifies by CLI brand (claude/codex/cursor/grok), not model family | CapabilityReceipt records the family from the transport's own model id; the gate compares families. | §5 note ('two CLI brands with one family do not satisfy gate') |
| R0122 | BF3-024-AC01 | deferred_by_plan | M8 | L | Grok Build freshness, permission, cancel, auth/quota and output tests pass through one supported headless/ACP route with planning kept in BulletFarm. | none | G3 adapter after the common boundary works. | §5 G3 ('qualify Grok Build separately') |
| R0123 | BF3-024-AC02 | deferred_by_plan | M8 | S | Grok Bot and unidentified `grok` executables are not treated as the Grok Build executor. | src/identity.rs:14 and src/agents.rs:101 label anything with `/.grok/` or comm==grok as 'grok' for discovery only | Executor identity must come from the certified binary/version pin, never process-name discovery. | §5 G3 |
| R0124 | BF3-024-AC03 | deferred_by_plan | M8 | S | No provider-specific scheduler or menu automation is added for Grok Build. | none | Design constraint to assert in the adapter's conformance tests. | §5 G3 |
| R0143 | CF01 | missing | M5 | M | Sealed writer still physically alive: independent checking proceeds on the sealed bytes while the old runner occupancy stays allocated/unconfirmed until termination is proved. | none | Sealed authority plus immutable-bundle verify job restorable from 9f03362:src/jobs.rs:377-402; needs an asynchronous fake adapter that stays alive after sealing and an assertion that occupancy != stopped while verify runs. | Stage 3 |
| R0144 | CF02 | missing | M6 | M | A CI-only check that requires a draft PR: prepublication gates permit exact draft publication; the task stays checking until the trusted CI result; no circular wait or premature review-ready. | none | Same as BF3-011-AC03 plus publication-stage ordering with the forge adapter and a fixture that exercises the ordering. | Stage 6 |
| R0152 | HF02 | missing | M6 | M | An effective model differing from the admitted job profile blocks or requires an explicitly authorized new profile; a live job is never relabeled; imports preserve provenance. | none | Compare observed model/version from exec --json/usage against the profile digest at collect_result and hold on mismatch. | Stage 5 ('explicit model/profile identity') |
| R0153 | HF03 | missing | M6 | S | An RPC session switch reporting success with cancelled=true leaves the fresh-context requirement unsatisfied; no implementation starts on inherited context. | none | Prime veto is N/A for the pinned Codex transport; Codex analogue: any resumed or inherited thread fails start_fresh. | Stage 5 ('freshness') |
| R0154 | HF04 | missing | M6 | S | Prompt accepted then execution fails: acknowledge admission only; later failure is not task completion. | none | Launch-intent states pending/dispatching/acknowledged/stopped restorable from 9f03362:src/jobs.rs:246,285,513. | Stage 3 ('launch intent') |
| R0155 | HF05 | missing | M6 | S | A spawn returning a handle without a child result infers no result or acceptance. | none | RunHandle carries no outcome; only collect_result can propose; add a fixture test. | Stage 3 |
| R0156 | HF06 | missing | M5 | S | A native child switching to an ineligible provider is denied or fails lane certification; the parent data policy is not widened. | none | Disable native delegation in the shipping profile and test that spawn attempts are blocked and recorded; if ever enabled, record the actual child model. | Stage 5 ('native delegation ... disabled') |
| R0159 | HF09 | missing | M5 | S | Children sharing a writable candidate checkout disable shipping delegation unless the one-writer boundary is actually enforced. | none | Same enforcement as HF06 plus a test that a child cannot obtain write access to the candidate tree. | Stage 5 |
| R0161 | HF11 | missing | M6 | S | An attempt-local lesson attempting to change a verifier or grant is rejected as an authority change and preserved only as an untrusted proposal. | none | result-v3 carries no authority fields; any embedded proposal is stored as an artifact with an untrusted label and surfaced for review. | no (§2 'imported prose cannot create grants' is nearest) |
| R0303 | §12.1-seam | missing | M4–M6 | M | Implement the adapter seam probe/start_fresh/read_events/cancel/collect_result as the single boundary every executor (fake, Codex, Claude, Grok) sits behind; the adapter never schedules another owner's work, grants permissions, decides acceptance or publishes to the forge. | none (src/lib.rs has no adapter module; 9f03362:src/jobs.rs:262-267 dispatched purpose-specific fixture fns without a trait) | Define the trait, RunHandle, normalized event and result types; port plan/implement/verify fixture behaviours behind it (restorable from 9f03362:src/jobs.rs plan_fixture/implement_fixture/verify_fixture); the seam itself is new code. | Stage 3 (adapters use production job paths; seam not named) |
| R0304 | §12.1-probe | missing | M4–M6 | M | probe(profile) returns a CapabilityReceipt (transport, model family, optional input/permission callbacks, sandbox class, version); a profile needing an unadvertised capability is ineligible at admission. | src/main.rs:283-306 and src/hub.rs:145-160 only capture `--version`; hub.rs:124 reports live_executor: null | Capability receipt type persisted per profile digest and checked at admission; restorable pieces in 9f03362:src/qualification.rs:10-65 (codex version/login/bwrap probe). | Stage 5 (partial: 'qualified profile') |
| R0305 | §12.1-events | missing | M4–M6 | M | Every normalized event names job ID, assignment generation and producer-local sequence; the hub derives the authenticated runner and global sequence; bounded raw protocol diagnostics are kept separately. | none (migrations/001_core.sql has no jobs or events tables) | Events schema with (job_id, generation, producer_seq) and runner endpoint validation; 9f03362:migrations/001_core.sql:131 events table lacked generation/producer sequence. | Stage 2 (runner endpoints bound to generation) |
| R0306 | §12.1-proposal | missing | M4–M6 | M | Result frames are proposals until the server validates role, subject and current authority; process exit, success:true or a final answer can never set merged, verified or observed_healthy. | none | Server-side validation path; merged/verified/healthy derived only from evidence/effect tables (9f03362:src/delivery.rs:145-162 eligible() shows the derived-predicate style). | Stage 3 / Stage 6 |
| R0307 | §12.1a-schema | missing | M4–M6 | M | Ship schemas/job_result.schema.json registered as result-v3: outcome enum candidate_proposed\|artifact_proposed\|blocked\|failed\|interrupted, candidate object required iff candidate_proposed, blocked requires code+explanation, no authoritative PASS/MERGED/actor-role/grant fields. | none (no schemas/ directory on main; 9f03362 had only schemas/task.schema.json) | Write the schema, generate the Rust type, validate every collect_result payload against it with a fixture corpus. | no (Stage 2 generates command/response types only) |
| R0308 | §12.1a-purpose | missing | M4–M6 | M | Hub validates payload against job purpose (planner returns plan artifact, reviewer returns finding artifact, implementer may propose code) and a verifier reports through receipt-v3 under its distinct enrolled role. | none | Purpose dispatch and verifier principal-kind check restorable from 9f03362:src/jobs.rs:262-267 and :437-438; receipt-v3 schema is new. | Stage 3 / Stage 6 |
| R0309 | §12.1a-meter | missing | M4–M6 | S | Usage/meter artifacts are authenticated observations whose basis the budget controller settles; a missing usage artifact is unknown, not zero. | none | usage_observations plus 'uncertain' holds on restart restorable from 9f03362:src/jobs.rs:463,527; overlaps BF3-005-AC03. | Stage 3 (§5 default 'unknown usage/effects visible') |
| R0310 | §12.2 | missing | M4–M6 | M | Persist three independent job dimensions: lifecycle (prepared..unknown), logical writing authority (none/active/sealed/revoked, one active shipping writer per task) and physical occupancy (allocated/running/stopped/unconfirmed); unconfirmed is never free capacity. | none | Restorable from 9f03362:migrations/001_core.sql:60 jobs table, src/jobs.rs:167-181 capacity check and tests/hardening.rs restart_keeps_unknown_occupancy_and_cost_reserved. | Stage 3 |
| R0311 | BF3-007-fakes | missing | M4 | M | Deterministic fake provider and durable fake forge with `bf demo --fixture basic` and `bf demo --fixture interrupted_publish` producing deterministic receipts labeled fake (§24.1). | docs/first_win.md:1 still instructs `bf demo --fixture basic`; src/main.rs:28-115 has no demo verb and tests/cli_skeleton.rs:35 forbids it | Restore from 9f03362:src/fixtures.rs:8-32, src/forge.rs, src/delivery.rs:27-64 (five fault points), DemoReceipt with fake:true; re-expose the verb or correct the doc. | Stage 1 ('persistent fake forge'), Stage 3 ('demos retain distinct run directories') |
| R0312 | §19.1-codex | missing | M4–M6 | M | Codex profile: `codex exec --json` with schema-constrained final output, a new invocation per task (never resume the planner), least-permitted execution scope stated explicitly, model credentials isolated from repository-controlled processes; App Server only as a replacement transport, never a second one. | none (docs/implementation-plan.md Stage 5 commits to this) | Profile JSON plus conformance fixtures; overlaps BF3-010-AC01 and AT-014. | Stage 5 |
| R0313 | §19.2-prime | not_applicable | M4–M6 | L | Prime RPC certification obligations: LF-framed JSONL with request IDs, new_session cancelled-flag check, per-root job namespace, no shared user daemon, client disconnect is not a stopped job. | docs/implementation-plan.md §1 selects 'one pinned Codex subscription transport first'; Prime is absent from the plan | Becomes required only if Prime is later selected as an executor; record that decision in the build checkpoint. | no |
| R0314 | §19.2-children | missing | M4–M6 | M | Native children are disabled for the initial shipping profile unless count, depth, model eligibility, usage and writable behaviour are enforceable; nested accounting distinguishes inclusive from exclusive root totals; child spawn/answers are observations, not completion. | none | Profile flag enforced by the sandbox plus usage-artifact fields for inclusive/exclusive totals. | Stage 5 (delegation disabled; accounting not named) |
| R0315 | §19.3 | deferred_by_plan | M4–M6 | M | Evaluate OpenJarvis engine/core reuse with a minimal consumer (footprint, explicit endpoint, stream/error handling, bounded blocking, cancel) before adoption; a local profile records model/quantization/engine/hardware/limits. | none | BF3-028 (G3) spike; no MVP prerequisite. | §5 G3 list |
| R0316 | §15-manifest | missing | M4–M6 | M | A gate manifest records stage, location (local\|ci\|both), protected recipe digest, required suite identities, execution environment and expected producer, reusing the real CI definitions rather than an imitation. | none (scripts/check is the only recipe and no manifest references it) | Manifest object with digest of scripts/check and the CI job identity; 9f03362 evidence carried recipe_digest/required_tests only. | Stage 6 (partial) |
| R0317 | §15.1-clean | missing | M4–M6 | L | A verifier checks out the exact candidate in a clean constrained job; recipe and fixtures are outside writer control; candidate code and tests run without the trusted reporter's credential; the reporter authenticates job identity, subject, exit/results, suite discovery and artifact completeness before recording evidence. | none | Skeleton restorable from 9f03362:src/jobs.rs:405-431 (clone from bundle, detach commit) and verification.rs python -I -S env_clear; needs the BF3-008 sandbox with the reporter outside it (plan: external browser harness for BulletFarm itself). | Stage 6 |
| R0318 | §15.1-binding | missing | M4–M6 | M | Evidence binds task/contract digest, candidate commit/tree, selection version, base or integration subject, check/fixture/environment/toolchain identities, actual suites, result, producer and artifacts; investigations bind hypothesis and evidence artifact instead of a Git candidate. | none | 9f03362:src/jobs.rs:441 evidence JSON had job/generation/candidate/subject/inputs/producer but no toolchain/environment identities or investigation form. | Stage 6 (partial) |
| R0319 | §15.1-invalidate | missing | M4–M6 | M | Changed candidate content invalidates its evidence; changed gate, environment, selection or integration inputs require the applicable recheck; earlier evidence is kept as historical; required profiles are rerun rather than proofs reused. | none | Key evidence by candidate_id + selection version + gate/environment digest (9f03362 StaleSelection at src/jobs.rs:436 covered selection only) and add a historical flag. | Stage 6 (§4 'stale Candidate' negative) |
| R0320 | §15.1-reruns | missing | M4–M6 | S | Keep first results and all reruns; flaky quarantine requires protected policy, an owner and replacement coverage; never sample until green or let the author decide its own failure is harmless. | none | Append-only evidence, quarantine policy record with owner, reruns only via a protected decision. | no |
| R0321 | §15.2 | missing | M4–M6 | M | Independent review: a reviewer receives requirements, exact diff, source and authenticated checks in a fresh bounded context; findings cite clauses/locations and a concrete test; model review is advisory and cannot replace required human/Code Owner review; the risk floor rises mechanically for auth, permissions, destructive data, public contracts and release infrastructure regardless of label. | docs/implementation-plan.md §4 requires different-vendor exact-head review as a process obligation only | Review job purpose with finding artifact plus a changed-surface risk classifier that sets the minimum review obligation. | no (process rule only) |
| R0322 | §11-diagnostic | missing | M4–M6 | L | Run the gate-first diagnostic: about ten historical behavioral fixes with recorded selection criteria, a passing good baseline, each defect reintroduced without deleting its detection test, and cases classified detected/missed/invalid/flaky/not-applicable. | none (9f03362 had one synthetic case: tests/acceptance.rs at032_gate_oracle) | Select ~10 fixes from neverhuman/bulletfarm history, script inject/run/classify against scripts/check, record results in the checkpoint. | no (Stage 7's single regression is not this) |
| R0323 | §11-negatives | missing | M4–M6 | M | Add a no-op candidate, a plausible constant result, missing test discovery, skipped mandatory cases and weakened-acceptance attempts to the diagnostic; a whole-PR revert that removes the oracle is not a valid detection test. | none | Overlaps AT-033/AT-034; add constant-result and weakened-acceptance cases to the diagnostic corpus. | Stage 6 (§4 verification negatives, partial) |
| R0324 | §11-admission | missing | M4–M6 | M | Admission records specify task classes, risk ceilings, profile/gate revisions, manual review obligations, permitted delivery boundary, residual gaps and approving principal; a legacy repository may allow supervised proposals while autonomous merge stays disabled. | none | Admission record per repository checked at job admission; first record for neverhuman/bulletfarm with boundary verified_pr. | no (Stage 6 names the `verified_pr` capability only) |
| R0325 | §11-block | missing | M4–M6 | S | Missing infrastructure or configuration is a typed onboarding block (check_missing hold), never permission to fabricate a pass; the first live task needs a meaningful independent acceptance path. | src/error.rs:16 defines CHECK_MISSING but nothing raises it | Raise at admission when the gate manifest, recipe or fixtures are absent and surface the hold reason. | Stage 6 (implicit) |
| R0326 | §19.1-claude | missing | M4–M6 | M | Claude headless profile: `-p` with structured output, one selected profile tested (repository hooks/MCP discovery and bare-mode authentication differences made explicit), native shutdown observed then reinforced by the recorded sandbox boundary. | none | Profile JSON plus conformance fixtures mirroring the Codex set. | no (after release) |
| R0327 | §20.1-run | missing | M4–M6 | S | `bf run` submits an outcome for in-policy planning and implementation under a selected standing grant through the hub command path (§10.1, §20.1); it cannot manufacture a grant. | src/main.rs:94-103,259-272 defines `bf run <provider> <prompt> [--yolo]` (headless dispatch in cwd) and exits 3 'not implemented yet (plan PR 9: bf run)'; tests/cli_skeleton.rs:57 | Shape diverges from the spec (provider/prompt/--yolo vs goal/--goal under grant); re-point to POST /v3/commands kind=run once the controller exists and drop the universal-yes flag. | Stage 2 (CLI mutations via hub API) / Stage 4 |
| R0328 | §12.2-supervisor | partial | M4–M6 | M | Every machine job has bounded output, a deadline, a durable start intent and a cancellation path (§12.2; §16.4 bounded frames). | src/runner.rs:19-86 (1 MiB cap :95, 30 s deadline :44, cancel flag :21,45, pgid kill :50); test runner::tests::stop_observes_termination; sole caller src/gitutil.rs:25 | Only trusted git runs through it; the fixed 30 s deadline and thread-local cancel flag do not fit jobs; no per-job handle, incarnation or durable start intent; not a sandbox (comment :18). | Stage 3 / Stage 5 |
| R0329 | §20.1-stop | partial | M4–M6 | M | `bf stop` requests active termination and remains Stopping until trusted termination proof; capacity is not reclaimed while unconfirmed (§14.3). | src/runner.rs:185-224 TERM then KILL on the pgid with self/ancestor/pgid guards and OUTCOME_UNKNOWN when still alive; tests/cli_wiring.rs:114 stop_ends_a_process_group_and_records_it; runner::tests::stop_refuses_before_signalling; human-only guard src/main.rs:246 | Waits only for the target PID (runner.rs:219), not the whole group or descendants; keyed by pid not incarnation; records to the board (src/live.rs:118-140) rather than job occupancy; spec stop is per mission, not per pid. | Stage 3 ('Stop remains Stopping'), Stage 5 |

## delivery-and-forge (44 rows: deferred_by_plan 5, done 1, missing 30, partial 8)

| row_id | ID | Status | Milestone | Size | Requirement | Evidence on main | Gap | In prior plan |
|---|---|---|---|---|---|---|---|---|
| R0015 | AT-015 | missing | M6 | S | Malicious import: Git hooks/helpers/filters and traversal cannot execute in the privileged publisher. | none | Scenario test: candidate carrying post-checkout/pre-push hooks, a clean/smudge filter, a credential helper in .git/config and a ../ or symlink path is imported; assert none executed and the traversal candidate is refused. tests/cli_prs.rs:42 shim_path shows the shim pattern to reuse. | Stage 6 |
| R0023 | AT-023 | missing | M6 | S | Changed selection: an old green candidate cannot publish after it has been replaced. | none | Selection-version check in the dispatch transaction and withdrawal of the old effect's eligibility when a repair/new candidate is admitted (§14.2). Test: seal candidate A, verify, seal B, assert A's effect is blocked. | Stage 3 / Stage 6 |
| R0024 | AT-024 | missing | M6 | S | Current grant at delivery: a long CI run does not extend an expired publication authority. | none | Grant expiry/revocation must be re-read in the dispatch transaction, not at seal time. Test: grant expires while the check job runs; effect stays pending/blocked with a visible why. | Stage 6 |
| R0035 | AT-035 | missing | M6 | S | Lost PR response: reconcile exactly one remote PR before retry; not-found may remain unknown. | none | Scenario test against a gh shim that swallows the create response: assert one PR adopted, no duplicate, and a delayed-visibility variant stays outcome_unknown. | Stage 6 |
| R0036 | AT-036 | missing | M6 | M | Cancel while an effect is in flight: new dispatch is blocked; the old outcome is reconciled from the remote, not declared undone. | none | Restorable (fixture only) from 9f03362:tests/hardening.rs remote_observation_survives_revocation_without_readiness and cancel_cannot_resume_or_publish. Need: cancel after state='dispatching' marks the effect potentially in flight; reconciliation records the observed remote PR as confirmed without granting readiness; no new dispatch for the cancelled task. | Stage 3 (cancellation fences new work, reconciles in-flight effects) |
| R0037 | AT-037 | missing | M6 | S | Human changes the PR: never overwrite, force-push or reopen a human-closed PR; unexpected branch changes become explicit reconciliation decisions. | none | Restorable (fixture) from 9f03362:tests/hardening.rs human_closed_remote_is_never_reopened. Real adapter: detect closed state / diverged head on read-back, block the effect, open a decision; publisher pushes without --force. GitHub protection observed 2026-09-18 blocks force-push only on main, not on PR branches, so the rule must be enforced in the publisher. | Stage 6 (hold on human edits/closure) |
| R0041 | AT-041 | partial | M7 | M | Integration drift: a moving target requires current combined-subject verification. | docs/migration/README.md (PR A: require GitHub Actions `check`, up-to-date branch, linear history, admin enforcement); live protection required_status_checks.strict=true + required_linear_history observed 2026-09-18 | Forge-level only: GitHub refuses the merge until the branch is up to date, so `check` ran on the current composition, but the product does not observe or record this, .github/workflows/ci.yml:3-6 has no merge_group trigger, and no test covers base movement between check and merge. | no |
| R0042 | AT-042 | missing | M7 | L | Assembled behavior: all task checks green cannot override a failed mission-level scenario. | none | Mission acceptance job that runs on the assembled result (all component PRs merged / combined tree) and whose fail blocks mission completion regardless of per-task evidence; evidence binds the assembled subject. Not in any plan stage. | no |
| R0050 | AT-050 | deferred_by_plan | M8 | S | Release substitution: a different artifact or environment invalidates the old approval. | none | Scenario test once the release adapter exists. | no (G3) |
| R0086 | BF3-012-AC01 | partial | M6 | M | Candidate config, hooks, helpers and filters cannot execute inside the privileged importer that brings candidate objects into the publisher clone. | src/gitutil.rs:5-23 (env_clear, GIT_CONFIG_NOSYSTEM, GIT_CONFIG_GLOBAL=/dev/null, core.hooksPath=/dev/null, protocol.ext.allow=never) | The hardened git wrapper exists but is only used for read-only rev-parse/remote get-url (src/prs.rs:85); there is no importer at all (no fetch of a candidate bundle/objects into a publisher-owned clone). Repo-local .git/config is still honored (filter.*, credential.*, core.sshCommand, remote helpers, includeIf); no symlink/path-traversal refusal on the candidate tree; no negative test. Build the import path (bundle/fetch into a fresh publisher clone with GIT_CONFIG_PARAMETERS neutralizing filter/credential/sshCommand/remote-helper config, safe.directory, no checkout of candidate tree) plus negative fixtures. | Stage 6 |
| R0087 | BF3-012-AC02 | missing | M6 | L | A lost PR-create response adopts the matching remote PR (read back exact branch/head and stable task/revision marker) or stays outcome_unknown before any retry; quick not-found may remain inconclusive. | none (src/error.rs:32-33 only defines the OUTCOME_UNKNOWN error code; nothing produces it) | No effects, no publisher, no GitHub write path on main. Restorable from 9f03362:src/delivery.rs (reconcile_effects: adopt-by-logical-key against FakeForge, five fixture fault points) and 9f03362:tests/hardening.rs publication_recovers_each_durable_boundary_in_fresh_hub — fixture-only. For real GitHub: after a lost reply read back refs/heads/<branch> and `gh pr list --head` plus a task/revision marker in the PR body; treat a fast not-found as inconclusive for a bounded visibility window; retry only when target semantics establish safety; otherwise keep outcome_unknown with a named operator action visible in the UI. | Stage 6 |
| R0088 | BF3-012-AC03 | missing | M6 | M | Publication requires the current selection/grant and the observed remote head; human edits or closure of the PR are never overwritten. | none (src/error.rs:22-23 STALE_SELECTION code is unused) | Restorable from 9f03362:src/delivery.rs eligible() (selection version, grant revoked/expiry, active reservation, producer-bound evidence) and its human_closed/head/base mismatch -> state='blocked'. Missing on main entirely; for GitHub compare observed head/base/state to the effect payload inside the dispatch transaction and before every follow-up write, block on divergence and raise an explicit reconciliation decision. | Stage 6 |
| R0107 | BF3-019-AC01 | missing | M7 | L | Wrong issuer, moved integration target, or incompatible individually-green changes cannot use stale evidence for integration. | none (src/prs.rs:223-236 counts statusCheckRollup entries without checking the issuing app or the base they ran against) | Integration effect must require evidence bound to (head sha, base = current main head, issuer = GitHub Actions app id pinned in protection, required suite ids); base movement invalidates it; tests for wrong issuer, moved target and two individually-green branches whose combination fails. The live forge already pins context `check` to app_id 15368 (observed via gh api 2026-09-18, not in repo). | no (Stage 6 defers automatic merge) |
| R0108 | BF3-019-AC02 | partial | M7 | S | Unsupported merge capabilities stay accurately disabled and visible as PR-ready/manual integration; no homemade merge queue is introduced. | AGENTS.md 'Initial publication capability is `verified_pr`; automatic merge requires later qualification'; docs/implementation-plan.md Stage 6 'First capability `verified_pr`; automatic merge deferred'; no merge code exists under src/ | Only prose. Add a per-repository capability record (assist_only / verified_pr / policy_merge) derived from observed forge protection (today: no required reviews, no merge-queue ruleset, allow_auto_merge=false) and surface 'PR-ready, manual integration' in the Changes view and `bf doctor`. | Stage 6 |
| R0109 | BF3-019-AC03 | missing | M7 | M | Known reverted prerequisites hold dependents and force re-evaluation of explicit preconditions on the current base, even when the original merge commits remain ancestors; the old receipt is preserved. | none (src/error.rs:14-15 DEPENDENCY_INVALID code is unused) | Restorable from 9f03362:migrations/001_core.sql:152-155 (dependency_invalidations) and 9f03362:src/jobs.rs:66 readiness query that treats an active invalidation as unmet. Missing everywhere: invalidation event producers (observed revert on main, withdrawn evidence, declared breaking interface), holding dependents, and re-evaluation of task-specific preconditions on the current base. | Stage 3 (partial: invalid prerequisites never mean completed) |
| R0125 | BF3-025-AC01 | deferred_by_plan | M8 | L | Artifact or environment substitution invalidates a release approval. | none | G3 package; requires the release adapter (build one immutable artifact from admitted source, verify staging, authorize exact artifact/environment/configuration, promote the same artifact). Approval must bind artifact digest + environment + configuration digest. | no (plan §5 lists BF3-025 under G3 after release) |
| R0126 | BF3-025-AC02 | missing | M8 | S | No coding job gains signing or production credentials. | none (src/main.rs:259-271 `bf run` is a stub, so no coding job exists yet to leak to) | Runner profile environment allowlist that excludes gh/publisher tokens, signing keys and production secrets, with a negative test enumerating env/FDs/mounts. This is a Stage 5 isolation obligation independent of the release adapter. | Stage 5 (no publisher secrets in runner); BF3-025 itself G3 |
| R0127 | BF3-025-AC03 | deferred_by_plan | M8 | L | Observed healthy is distinct from trigger and from deployment; irreversible data migrations require an explicit forward/restore strategy and owner; multi-repository release compatibility is a manifest, not atomicity. | none | Release effect states trigger -> deployed -> observed_healthy(interval) as separate observations; migration strategy/owner fields on the release mission; tested standing rollback policy for reversible rollouts. | no (G3) |
| R0146 | CF04 | missing | M7 | S | Prerequisite merged then known-reverted: historical receipt remains; dependent is held or fails its precondition although the old merge stays in ancestry. | none | Scenario test: record merge receipt, push a revert to main, register the invalidation, assert dependent readiness fails while the receipt row is unchanged. | Stage 3 (partial) |
| R0244 | §16 effect record | missing | M6–M7 | M | Every consequential remote write has a stable logical key, immutable request digest, exact subject, current authority reference, expected remote state and an effect record with states pending -> dispatching -> confirmed \| outcome_unknown -> confirmed \| not_applied \| blocked. | none (migrations/001_core.sql defines only principals, sessions, commands, operations) | Restorable from 9f03362:migrations/001_core.sql:136-142 (effects: logical_key UNIQUE, request_digest, task/candidate/selection_version, grant_id, publisher_id, state CHECK with the six states, payload_json, receipt_json). Add as migration 002 with expected_remote_state and publisher epoch columns and a transition function that permits only the spec edges. | Stage 1 (publication intents) / Stage 6 |
| R0245 | §16 dispatch authorization | missing | M6–M7 | M | The local transaction that revalidates present authority and marks `dispatching` is the dispatch-authorization point; cancellation before it prevents dispatch, after it treats the effect as in flight; a best-effort abort is not proof the remote action did not happen. | none | Restorable from 9f03362:src/delivery.rs (eligible() + UPDATE effects SET state='dispatching' in one transaction, then remote call, then receipt). Needs the same shape on main with cancel ordering tests on both sides of the mark. | Stage 3 / Stage 6 |
| R0246 | §16 dispatch preconditions | missing | M6–M7 | M | Before privileged dispatch require current publisher/epoch, live task/domain grant, current revision and selection, applicable stage evidence, designated approvals, retained reservation, expected remote refs and no unresolved conflicting effect; ended author leases are not required, expired grants still block. | none | 9f03362 eligible() covered selection, grant, reservation and producer-bound evidence only. Add publisher epoch, current revision, designated approvals, observed remote refs equal to expected, and a conflicting-effect check on the same logical key. | Stage 6 |
| R0247 | §16 serialize per PR | missing | M6–M7 | S | Effects affecting one logical PR/change are serialized. | none | 9f03362 only had UNIQUE(logical_key) (one effect per key). Need: an in-flight (dispatching/outcome_unknown) effect on a logical key blocks any new effect on that key until settled. | Stage 6 |
| R0248 | §16.1 other effect kinds | missing | M6–M7 | M | The lost-response rule applies equally to merging, check publication and release triggers; a local idempotency key does not make a remote API exactly-once; already-dispatched effects may complete after cancellation and must be recorded as observed with only authorized follow-up. | none | Effect kinds beyond pr_create (merge, check/status publication, release trigger) must share the record/reconcile path with kind-specific read-back. Plan covers PR creation only; merge and release effects are not in any stage. | Stage 6 (PR create only); merge/release: no |
| R0249 | §16.2 hub restart | partial | M6–M7 | M | At restart acquire the single-instance lock, inspect retained authority and unsettled effects, reconnect/inspect runner incarnations, fence expired work and reconcile before any conflicting write; do not restart every task blindly. | src/hub.rs:44-51 (hub.lock try_lock, ResourceConflict when another hub runs); src/hub.rs:55 fresh boot epoch | Lock and epoch exist; nothing else does because there are no jobs or effects on main. Restorable from 9f03362:src/jobs.rs recover() (jobs -> unknown/unconfirmed, writer authority revoked, holds -> uncertain, effects dispatching -> outcome_unknown, then reconcile_effects) and test restart_keeps_unknown_occupancy_and_cost_reserved. Must run before the API accepts mutations. | Stage 2 / Stage 3 |
| R0250 | §16.2 incarnation rules | missing | M6–M7 | M | A verifier crash gets a new check-job assignment and a late old result cannot supersede it; the runner records sandbox/process identity before acknowledging; duplicate start deliveries return the existing job; ambiguous spawn yields quarantine, never a second accepted process. | none (src/main.rs:259-271 `bf run` is a stub) | Owned mainly by the isolation/recovery area (BF3-008/009/011); restorable in part from 9f03362:tests/hardening.rs stopped_prepared_verifier_can_retry_and_preserves_candidate. Listed here because §16.2 is in scope; delivery only needs the resulting job generation to bind effects. | Stage 3 / Stage 5 |
| R0251 | §16.3 rehearsed runbook | missing | M6–M7 | S | Manual restoration steps are acceptable for the single-hub release only when the procedure is explicit and rehearsed; no active-active, zero-downtime or recovery-of-uncaptured-changes claims. | none (docs/ has no restore runbook; docs/migration/README.md covers repository consolidation only) | Write docs/restore.md with the ordered steps, the outside-DB rotation points and the rehearsal record (date, snapshot id, outcome); link from doctor output. | no |
| R0252 | §16.4 artifacts | missing | M6–M7 | M | Bounded artifacts are written to issued temporary locations, validated (size/hash/path), durably finalized, then complete metadata is committed; missing or truncated referenced artifacts are unavailable, not green evidence. | none (no artifacts table in migrations/001_core.sql) | Restorable from 9f03362:migrations/001_core.sql:103-106 (artifacts: digest, byte_count, complete) and 9f03362:src/delivery.rs artifact_valid (re-reads bytes, checks size and sha256 before dispatch) plus test missing_artifact_blocks_publication_after_pass. Add tmp -> fsync -> rename finalization and path validation. | Stage 1 / Stage 6 |
| R0253 | §16.4 upload receipts | missing | M6–M7 | S | Upload receipts derive the producer's authority; worker-provided URLs are never fetch instructions for the privileged hub. | none | Runner artifact endpoint (plan Stage 2 table) must accept bytes bound to job generation and issue the receipt; the hub never fetches a URL supplied in a result frame. One negative test. | Stage 2 (runner artifacts endpoint) / Stage 6 |
| R0254 | §16.4 backup | missing | M6–M7 | M | Back up a consistent database snapshot plus the artifacts required by retained evidence. | none (src/storage.rs:76-99 opens WAL, applies 001 only; no backup path) | `bf backup` using the SQLite backup API (or VACUUM INTO) under the bounded worker, then copying artifacts referenced by retained evidence with a manifest of hashes; restore drill consumes it (AT-045). | Stage 2 (back up consistently) |
| R0255 | §16.4 escaping and bounds | partial | M6–M7 | S | Logs and remote Markdown/HTML are escaped and terminal control sequences sanitized; requests, events, frames, artifacts and browser buffers are bounded; slow UI clients cannot stall lease renewal, cancellation or evidence recording. | src/tui/text.rs:7-20 terminal_text strips C0/C1, ESC sequences and bidi overrides; src/live.rs:33-39 TAIL_BYTES/TAIL_MESSAGES/TAIL_CHARS bounds; tests/tui.rs, src/live.rs:572 tail_lines_keep_the_last_forty_and_truncate_each | No remote Markdown escaping because there is no publisher writing PR bodies/comments yet; src/api.rs has no request-body size limit on POST /v3/commands; no event stream on main so slow-consumer disconnect is untested (9f03362:tests/hardening.rs slow_streams_have_a_lifetime_bound_and_control_remains_available is restorable). | Stage 2 (bounded event stream) / plan §4 Responsiveness |
| R0256 | §15.4 cross-repo manifest | missing | M6–M7 | M | Cross-repository missions identify each exact component and compatibility scenario; no atomic multi-repository merge or deployment is claimed; numerical tasks bind frozen data, tolerances, limits and independent comparison. | none | Assembled compatibility manifest (exact component revisions) as mission output; single-repo release may explicitly state the limitation. Low priority until a second repository exists. | no |
| R0257 | §15.3 merge queue | missing | M6–M7 | M | Use the existing protected native merge queue (or an implemented equivalent passing the same behavioral checks); required Actions checks need the merge_group event; if the forge cannot enforce the subject/protection, autonomous merge stays disabled. | none (.github/workflows/ci.yml:3-6 triggers on push/pull_request only; repo rulesets observed 2026-09-18 contain only the tag-immutability ruleset) | Enable a GitHub merge-queue ruleset on main, add `merge_group:` to ci.yml, verify `check` reports on the merge group, then certify before `policy_merge` is admitted. Until then AC02's manual-integration statement stands. | no (Stage 6 defers merge) |
| R0258 | §15.3 stable PR | missing | M6–M7 | M | One stable current PR per coherent task revision with exact candidate history underneath; repairs add forward commits from the observed remote head; a local rebase never authorizes force-pushing remote history. | none | Logical key = task + revision (9f03362 used `pr:{task}:1`); repair publication fetches the observed remote head, appends commits and pushes without force; test that a diverged remote head blocks rather than overwrites. | Stage 6 (one stable PR identity per revision) |
| R0259 | §15.3 attribution | missing | M6–M7 | M | Task-to-candidate-to-merge attribution is mandatory; preserve the original tested objects and record how the certified merge method maps to final content and lineage. | none (AGENTS.md `bf-author`/`bf-claim` PR-body lines and `gh pr merge --rebase` are a manual convention, not a machine record) | Merge receipt row: PR number, tested head sha, merge method, resulting main commit(s), candidate id, task/revision; recorded by the merge effect or by observing a human merge. Needed even while merge is manual so CF04/dependencies can reference a receipt. | no |
| R0260 | §23.2 export | missing | M6–M7 | M | `bf export` produces a versioned manifest with immutable contracts/decisions/profiles, permitted gate references, ownership history, outcome/cost JSONL, evidence manifests and retained artifacts, exact hashes and explicit missing/withheld records; never usable secrets or reactivatable grants/leases. | none (src/main.rs Command enum has no export) | New CLI/API command writing a manifest + JSONL + artifact directory with sha256 per file, secrets stripped, grants/leases/sessions exported as history only. | no |
| R0261 | §23.2 import adapter | missing | M6–M7 | L | V3 imports A–J content only through a named source-format adapter that preserves raw source bytes and original digest semantics, maps Attempt/ModelJob records to purpose-tagged Job history and resolves J's ExecutionBinding into a direct profile reference after verification; never relabel a v1 payload as v3. | none | Adapter trait keyed by source format; raw bytes and digest scheme stored beside mapped rows; explicit rejection of unknown formats; mapping validation report. | no |
| R0262 | §23.2 import paused | missing | M6–M7 | M | Import begins paused/proposed with new authority: stop the old allocator, reconcile existing PRs, resolve owners and missing dependencies, issue new grants, activate only after validation. | none | Import command lands rows in proposed state, marks all imported grants/leases inactive, runs PR reconciliation against the forge, and requires an explicit activation command; observation labels for external human-controlled work. | Stage 2 (partial: import paused, revoke obsolete sessions) |
| R0263 | §21 existing forge protections | partial | M6–M7 | S | Initially use the forge's existing APIs and protections rather than new enforcement. | src/prs.rs:39-40,161-163 (gh pr list with statusCheckRollup/reviews/mergeStateStatus); docs/migration/README.md PR A protection requirements; live main protection observed 2026-09-18: required check `check` (strict), enforce_admins, linear history, no force-push/deletion | The forge has no required pull-request review and no merge queue, so 'independent review' is only the AGENTS.md comment convention from the shared `neverhuman` identity; the product does not persist or reason over the observed protection state. | plan §4 (CI/review gates) |
| R0264 | §21 interoperability non-goals | done | M6–M7 | S | Preserve ordinary Git/PR interoperability; no BulletGit replacement, new object store, custom merge queue, GitHub UI clone or deployment engine is a prerequisite; unsupported automatic merge stays accurately disabled rather than hidden behind a client-side workaround. | src/prs.rs:161-163 (plain `gh pr list`); AGENTS.md 'Do not: add a scheduler, a second database, a workflow DSL, or a chat bus'; no merge/queue code under src/ | none today; keep it so when the publisher lands (no custom queue, gh/REST only). | Stage 6 |
| R0265 | §21 forge-native improvement | deferred_by_plan | M6–M7 | XL | Later select one measured forge improvement (native task/candidate/evidence metadata, visible registered change intent, or server-enforced current subject/fence) and test the actual implementation. | none | BF3-030 (G3) after BF3-019/022. | no (G3) |
| R0266 | §21 channels | deferred_by_plan | M6–M7 | L | Slack (verified signatures/replay, identity mapping, decisions bound to exact subjects), Grok Bot and SMS thin clients cannot introduce a second task database or broader authority than the web client. | none | Owned by BF3-026/027 (G3, another area); listed only because §21 is in scope. | no (G3) |
| R0267 | §15 review-ready visibility | partial | M6–M7 | M | At review-ready the declared automated preparation obligations are complete and unresolved human obligations are visible; a PR's existence does not make a task review-ready. | src/prs.rs:248-268 (`missing`: CI failing/pending, REVIEW: approve <head-sha>, mergeStateStatus, draft); tests/cli_prs.rs:196 missing_lists_each_unmet_merge_precondition; tests/tui.rs:102 PrRow in the TUI snapshot | Projection only: not bound to a task, candidate, effect or evidence row; issuer of checks not verified; readiness for a task cannot be derived from it. Stage 6 needs the task to stay Checking until required checks and independent review are recorded as evidence. | Stage 6 |
| R0268 | §15.2 independent review binding | partial | M6–M7 | S | A required independent review of the exact head by a different vendor is an advisory artifact whose workflow obligation must be satisfied; it is not a native approval or a trusted test. | src/prs.rs:286-297 approves() binds `REVIEW: approve <sha>` to the head sha (>=7 hex prefix); AGENTS.md review protocol (different vendor, `gh pr review --comment`, reviewer merges) | No check that the reviewer differs from the author/vendor or is not the shared identity; 7-char prefix accepts ambiguity; the forge itself requires no review (protection lacks required_pull_request_reviews) so the obligation is unenforced outside the comment convention. | plan §4 |

## control-plane (57 rows: missing 44, partial 13)

| row_id | ID | Status | Milestone | Size | Requirement | Evidence on main | Gap | In prior plan |
|---|---|---|---|---|---|---|---|---|
| R0016 | AT-016 | missing | M5 | S | Exactly one current shipping writer is admitted per task under a claim race. | none | jobs table with partial unique index on (task_id) WHERE writer_authority='active' plus a race test; restorable 9f03362:migrations/001_core.sql:81 one_current_writer and src/jobs.rs:77-82 | Stage 3 |
| R0017 | AT-017 | partial | M5 | M | A conflicting resource claim acquires none of the requested incompatible set. | src/board.rs:283-320: any overlapping path returns ResourceConflict before the single insert; disjoint paths in another repo proceed (test src/board.rs:1114) | Only path scopes; no interface/resource_key kinds; not evaluated in the same transaction as WIP, capacity and budget holds; not bound to a job or task | Stage 3 / §2 ('multi-resource acquisition atomic') |
| R0018 | AT-018 | missing | M5 | M | A duplicate spawn/replay launches one accepted incarnation or quarantines the ambiguity. | none — nothing spawns (src/main.rs:270) | Launch intent state machine (pending/dispatching/acknowledged/unknown/stopped) keyed by unique incarnation; restorable 9f03362:migrations/002_workbench.sql launch_intents; unknown state must block a second launch | Stage 3 |
| R0019 | AT-019 | missing | M5 | M | An old worker's result or heartbeat cannot advance current state (stale generation). | none — src/board.rs:362-402 heartbeat extends any active claim for any caller, no holder or generation check | Heartbeat/result endpoints bound to job + assignment generation + authenticated runner; reject mismatches | Stage 3 (§4 'stale generation'); runner endpoints in Stage 2 table |
| R0020 | AT-020 | missing | M5 | M | A late heartbeat response from a suspended runner does not incorrectly extend authority. | none — src/board.rs:362-402 uses caller-supplied wall-clock `now`; no monotonic elapsed or acknowledged-renewal model; no suspend/resume or clock-change tests | Server-side lease with monotonic elapsed, renewal acknowledgement carrying generation, and tests for suspend/resume and clock jumps (§14.2) | Stage 3 (stale generation only; suspend/clock tests not named) |
| R0022 | AT-022 | missing | M5 | M | Ending author authority retains the pending-change reservation; the physical slot is free only after confirmed cleanup. | none — src/board.rs:256 drops claims silently once expires_at passes | Reservation lifetime tied to task state (checking/review/integration) not TTL; occupancy freed only on stopped receipt; restorable base 9f03362:src/jobs.rs:390-395 and recover() | Stage 3 |
| R0025 | AT-025 | missing | M7 | M | Routine plan handoff gives the implementation a fresh session with no native plan menu. | none — bf run unimplemented (src/main.rs:270) | Implementation dispatch that builds context from accepted requirements/source/decisions/checks/remaining limits, not the planner conversation, and a test that no session resume flag is passed | Stage 4 + Stage 5 |
| R0028 | AT-028 | partial | M7 | S | A quiet but approved build is not killed solely for silence. | src/runner.rs:40-50 terminates only on the wall deadline or the cancel flag, never on output silence | Deadline is a hard-coded 30 s (src/runner.rs:44) that would kill a legitimate long build; needs the admitted per-job deadline and an explicit test | no (Stage 5 mentions bounded wall time) |
| R0029 | AT-029 | partial | M7 | M | Budget, lease and cancel deadlines are enforced despite any advisory watcher. | src/runner.rs:44-60 enforces the wall deadline and cancel flag with a process-group KILL; test src/runner.rs:230 stop_observes_termination | No budget or lease deadline exists; the enforced deadline is a constant rather than the job's persisted deadline_at | Stage 3 (deadline persisted) + Stage 5 |
| R0040 | AT-040 | missing | M7 | M | A shared lane needs an explicit grant before performing owned cross-domain work (owner partition). | none | Test with two owners and a shared lane refusing without a cross-domain grant | no |
| R0043 | AT-043 | missing | M7 | M | A report window cutoff preserves work; a hard deadline uses the normal checkpoint/stop path. | none | Window boundary handling distinct from job deadline; test that cutoff does not interrupt a running job | no |
| R0047 | AT-047 | missing | M7 | S | Reports label unknown human handling time and task-selection confounding explicitly. | none — no reporting on main | report.rs output with explicit 'unknown' handling time and confounding labels; Stage 7 records interventions for one task but produces no labeled report | Stage 7 (partial: records interventions/elapsed for the pilot) |
| R0077 | BF3-009-AC01 | partial | M5 | L | Acquire all conflicting resources or none; a replay cannot spawn a second accepted incarnation. | src/board.rs:267-350 claim() checks every requested path against active claims inside one IMMEDIATE transaction (src/board.rs:183-188) and inserts only if no overlap; test src/board.rs:1114 claim_conflict_idempotent_release; command replay idempotent src/commands.rs:39-56, test :111 | Claims are coordination records keyed by declared identity, not job admission reservations; no launch_intents/incarnation, no spawn, no quarantine of ambiguous spawns; restorable base 9f03362:migrations/002_workbench.sql launch_intents (unique incarnation) and src/jobs.rs:186-187 | Stage 3 ('Ambiguous spawns stay reserved/quarantined; retries cannot create another accepted incarnation') |
| R0078 | BF3-009-AC02 | missing | M5 | M | Author sealing/revocation does not release pending-change ownership or unconfirmed physical capacity. | none — no writer authority or occupancy state on main | writer_authority and occupancy columns with seal keeping change_reservations active and occupancy 'unconfirmed' until a stopped receipt; restorable from 9f03362:src/jobs.rs:390-395 (sealed, reservation retained) and :458-467 recover(); test restart_keeps_unknown_occupancy_and_cost_reserved | Stage 3 ('Separate write permission, physical occupancy, selection, pending-change ownership') |
| R0079 | BF3-009-AC03 | missing | M5 | M | Late planner/reviewer/verifier results cannot replace newer job assignments. | none — src/error.rs:21 StaleGeneration exists but is never raised | Result collection must check job id + assignment_generation + deadline and reject stale frames as observations only; restorable from 9f03362:src/jobs.rs:501-507 current() and :430 StaleSelection check | Stage 3 / §4 'Jobs: stale generation' |
| R0092 | BF3-014-AC01 | missing | M7 | L | Cross-domain work requires applicable grants; a shared lane cannot steal owned tasks. | none — no owners/domains/grants on main, and src/board.rs:404-432 release() plus src/live.rs:320 let any identity end another agent's claim | Owner/domain model, membership-checked admission (base 9f03362:src/hub.rs:287 require_membership), holder check on release/heartbeat, and a shared-lane grant requirement (src/control/owners.rs) | no (G2 listed in §5 table only) |
| R0093 | BF3-014-AC02 | partial | M7 | M | Overlapping work waits with an accountable reason while disjoint work proceeds. | src/board.rs:283-320 overlap refusal names holder, claim id, expiry and reason; disjoint or other-repo claims proceed (test src/board.rs:1114) | Refusal is an exit code, not a held task with hold_reason=resource_conflict that is re-admitted when the reservation ends; no scheduler queue | no (G2) |
| R0094 | BF3-014-AC03 | partial | M7 | M | External PR observations reconcile with controller state; unregistered unpushed work is explicitly outside coverage. | src/prs.rs:136 list() reads open PRs through gh on a timer (src/live.rs:169 refresh_prs with a stale marker); src/agents.rs:49 observes unmanaged sessions | No authenticated webhook path, no reconciliation of observed PRs against reservations/task publication state, no explicit 'outside coverage' declaration for unpushed edits (src/forge/observations.rs) | no (G2); §2 observation only |
| R0101 | BF3-017-AC01 | missing | M7 | M | Planning before a task exists uses a mission-bound job under the same spending controls. | none — src/commands.rs:9 KINDS=note/release/stop and :61 every command returns NOT_IMPLEMENTED; web/src/App.tsx submits 'run'/'edit_draft'/'start_work' the hub rejects | Restore mission/draft/plan job admission from 9f03362:src/jobs.rs:27-51 admit_plan (grant + budget + plan job) and the drafts/conversation tables (migrations/002_workbench.sql) | Stage 4 ('Planning is a bounded mission job with common accounting/recovery') |
| R0102 | BF3-017-AC02 | missing | M7 | L | Fast/Standard/Deep planning flows are bounded and every mandatory requirement maps to an acceptance criterion. | none — 9f03362 had a fixed fixture plan only | foreman.rs with three bounded modes, requirement→acceptance coverage validation before activation; plan commits to Fast only | Stage 4 (Fast only; Standard/Deep not planned) |
| R0103 | BF3-017-AC03 | partial | M7 | L | Private memory cannot enter a wider audience automatically; agents cannot self-approve escalations. | src/main.rs:244-250 bf stop is refused when the caller's process ancestry is an agent (human-only) | No context/memory scoping (src/control/context.rs), no decision records with required human actor kind (base 9f03362:migrations/001_core.sql:143 decisions, src/commands.rs:132-134 human-only check), no audience labels on planner output | Stage 4 (fresh context excludes planner conversation; HF12 not named) |
| R0110 | BF3-020-AC01 | missing | M7 | M | Inclusive root/child meters are not double-charged and missing usage is never zero. | none | usage_observations with reporting basis (inclusive/disjoint) and settlement that picks one basis; base 9f03362:src/jobs.rs:527 records basis for a fixture 0 only | no |
| R0111 | BF3-020-AC02 | missing | M7 | M | Window end preserves useful work within the existing bounded drain authority. | none — no attention windows or drain state on main | Window definitions, report cutoff, and drain that lets running jobs checkpoint/stop normally without new admission | no |
| R0112 | BF3-020-AC03 | missing | M7 | L | Review/verification pressure reduces new writing; routing uses eligible calibrated profiles rather than a cheap-call quota. | none | Queue-pressure rule in the scheduler and routing.rs with eligibility-before-price selection; single-profile plan (Stage 5) leaves routing untested | no |
| R0145 | CF03 | missing | M5 | M | Reassigned verification has a new job generation; an old verifier result cannot overwrite current evidence selection; re-execution is accounted. | none | Verify jobs with assignment_generation, selection_version binding and cost accounting on re-execution; restorable from 9f03362:src/jobs.rs:405-447, migrations/003_evidence.sql evidence_binding trigger and tests/hardening.rs:679 stopped_prepared_verifier_can_retry_and_preserves_candidate | Stage 3 + Stage 6 |
| R0150 | CF08 | missing | M7 | S | When root-inclusive and child usage are reported together, the reporting basis chooses inclusive root or disjoint leaves so consumption is neither charged twice nor omitted. | none | Same mechanism as BF3-020-AC01 plus a test with both reports present | no |
| R0157 | HF07 | partial | M5 | L | A child or daemon that remains after the client exits stays under root sandbox supervision; the deadline stops/fences all managed descendants. | src/runner.rs:31 process_group(0); :44-60 30 s wall deadline then kill -KILL -pgid; :185-223 stop() TERM→KILL waiting for target pid, returns OutcomeUnknown 'still alive' rather than claiming success; test src/runner.rs:230 stop_observes_termination | Process-group only (a setsid daemon escapes), target checked by pid disappearance not whole tree, no incarnation-scoped cgroup/container, no restart-time survivor search before freeing capacity; deadline is a constant, not the job's admitted deadline | Stage 5 ('Stop descendants/daemons by incarnation, not parent PID. Restart finds survivors before freeing capacity') |
| R0158 | HF08 | missing | M5 | M | A native session that auto-resumes after lease loss cannot have a current result or effect accepted and cannot access another task's namespace. | none | Generation-checked result acceptance (BF3-009-AC03) plus per-job private workspace so a resumed session has no successor checkout; test with a resumed fake adapter | Stage 3 (stale generation) + Stage 5 (isolation) |
| R0160 | HF10 | missing | M7 | M | Late native child/refinement usage is attributed to the original work and profile; uncertain exposure is not released as zero. | none | Late usage ingestion keyed by original job/lineage/profile with holds kept 'uncertain' until settled | no |
| R0162 | HF12 | missing | M7 | M | Private memory proposed for team output is subject to current source and destination permissions; no automatic scope promotion. | none | Memory/context items carry a scope; promotion requires a human decision; negative test | no |
| R0171 | §18.2 | missing | M4–M5 | M | Before a write attempt reserve execution exposure plus the configured minimum verification/review envelope at task/mission scope; on failed verification retain actual costs and recompute before repair; a task that cannot fund its completion path is held (budget_unavailable) until an authorized owner extends allowance or reduces scope via a new contract. | none — src/error.rs:29 has BudgetUnavailable but nothing raises it | Task-scope completion reserve, recompute-after-failure, hold_reason=budget_unavailable and an authenticated grant_allowance command (web/src/wire.ts:2 still lists 'grant_allowance' with no server handler) | Stage 3 (reserve); allowance extension not named |
| R0172 | §18.3 | missing | M4–M5 | L | Ledger stores integer micro-units with explicit currency, applies limits at org/project, owner/domain, mission, lineage, window and upstream pool, settles each meter delta once, and records the reporting basis (inclusive root vs disjoint leaves) so nested usage is neither double-counted nor omitted. | none | New ledger schema and settlement; 9f03362:migrations/001_core.sql:52,116-126 has micro-unit accounts and a usage_observations basis column for one account only; multi-scope limits and once-only settlement are new | no |
| R0173 | §18.3 | missing | M4–M5 | M | Profiles with less precise metering must display residual exposure and remain ineligible where stricter control is required; a local timeout cannot stop already-accepted remote billing and that exposure is retained. | none | Profile metering-precision attribute, eligibility filter, and retained-exposure record on cancel/timeout (src/runner.rs:44-60 kills on deadline but records nothing about upstream billing) | no |
| R0174 | §18.4 | missing | M4–M5 | M | Default three write-enabled invocations per original task including initial implementation, repair and escalation; a fresh context or a new revision does not reset the allowance; a read-only format repair cannot become an uncounted writing repair. | none — bf run unimplemented (src/main.rs:270) | lifetime_invocations counter on the task lineage checked at admission; restorable from 9f03362:src/jobs.rs:83-95 and tests/hardening.rs:553 stopped_prepared_work_can_retry_within_cumulative_limits; the plan words it 'per task revision' (Stage 3) which conflicts with 'per original task' and AT-039 — count must survive respecification | Stage 3 (wording deviates: per revision vs per original task) |
| R0175 | §18.4 | missing | M4–M5 | M | At most two transport retries, only when the previous outcome is known and the operation is retry-safe; unknown external operations are not retry-safe by default; native retries are accounted. | none | Retry policy on launch/result/effect paths with outcome-known predicate and retry counter; no code on main touches retries beyond the SQLITE_BUSY loop in src/board.rs:183-202 | Stage 3 ('bounded transport retries only when safely repeatable') |
| R0176 | §18.4 | missing | M4–M5 | M | Repeated identical failure fingerprints prompt diagnosis/replanning instead of automatic repetition. | none | Failure fingerprint on job results and an admission rule that routes a repeat to a diagnosis/replan decision rather than another write invocation | no |
| R0177 | §18.4 | partial | M4–M5 | M | Reserve interactive headroom separately from autonomous allocations; unmanaged external sessions make capacity partially observed and the UI must display that limitation rather than claim omniscient remaining quota. | src/agents.rs:49 discover() lists unmanaged Claude/Codex/Cursor/Grok sessions on the host; src/tui/ui.rs renders them | No headroom reservation, no quota/capacity model, and no statement in TUI/web that observed sessions are outside accounting | §2 product journey (observation only) |
| R0178 | §14.1 | missing | M4–M5 | L | Atomic admission in one short transaction: verify active task revision/expected version, dependency conditions and invalidations, current grants and risk, eligible profile/runner, path/interface reservations, owner/team WIP and physical capacity, implementation allowance and all budget holds; acquire all reservations or none; increment writer generation, create job, append event and launch intent, commit before starting; no transaction spans a model call, Git process or network. | none on main | Restorable core from 9f03362:src/jobs.rs:52-155 admit_implementation (grant, pause, dependencies, one writer, invocations, budget, path reservations, completion hold, insert_job, writer_generation++); missing there: expected-version check, profile/runner eligibility, WIP/capacity limits, risk, interface reservations, per-repository serialized check | Stage 3 ('Before launch persist job identity/purpose/generation/deadline, complete reservations, budget holds…') |
| R0179 | §14.1 | partial | M4–M5 | M | Scopes are explicit repo-relative file names or directory prefixes matched by path component; reject traversal, absolute paths, NULs and unsafe encoding; renames need both source and destination; gitlinks, symlinks, executable-mode changes, protected instructions, CI files and lockfiles get explicit risk/scope treatment. | src/board.rs:107-151 normalize_path rejects absolute, '..', NUL, backslash, glob-in-file; :153 overlaps() is component-wise; tests src/board.rs:1095, :1105 | No rename rule, no risk classification for gitlinks/symlinks/mode/CI/lockfiles/protected instructions, no interface scopes, no UTF-8/encoding check; validation lives in the board not the admission path | Stage 6 (seal-time 'unsafe paths' only; risk classes not named) |
| R0180 | §14.2 | partial | M4–M5 | M | The execution lease identifies authority epoch, task/revision, job, assignment generation, writer generation, runner and deadline; heartbeat/lease defaults are tunable; use monotonic elapsed time and acknowledged renewals. | src/board.rs:46 MAX_TTL_MINUTES=180, :362 heartbeat extends a claim; src/hub.rs:55 boot epoch string exists | No lease object bound to job/generation/runner/epoch; the epoch is never compared; heartbeat has no holder check (src/board.rs:362-402) and is wall-clock only; no 10 s/60 s defaults or stop margin | Stage 3 (partial); runner heartbeat endpoint in Stage 2 table |
| R0181 | §14.2 | missing | M4–M5 | L | At result collection freeze writes or take an immutable snapshot, verify current job authority, full actual diff scope and artifact completeness, then transactionally record the candidate, advance selection version, seal the writer, retain the task-owned reservation and create the verification job intent. | none | Restorable from 9f03362:src/jobs.rs:380-403 (bundle digest, candidate + selection version, writer 'sealed', verify intent in one tx); full-diff scope check must be added | Stage 6 (seal) + Stage 3 |
| R0182 | §14.2 | missing | M4–M5 | M | A sealed job may replay the identical result idempotently but cannot submit changed code; a repair needs a new generation and private workspace; before admitting the repair withdraw the old candidate's publication eligibility and reconcile in-flight effects; keep prior code and failures. | none | Idempotent result replay keyed by digest, repair admission that increments writer generation and revokes old candidate eligibility; 9f03362 had generation increments but no withdrawal step | Stage 3 (partial: 'retries cannot create another accepted incarnation') |
| R0183 | §14.2 | missing | M4–M5 | M | A change reservation remains while checking, under review or pending integration and is released/transferred only after actual merge, explicit authorized withdrawal/supersession or reconciled abandonment; an old open PR does not leave conflict consideration because a timeout elapsed. | none — src/board.rs:256 expires claims by TTL; src/board.rs:404-432 release() and src/live.rs:320 expire_claim() let any identity end another's claim | Task-state-bound reservation lifetime, holder/owner authorization on release, and an accountable stale-reservation decision instead of silent expiry; the plan (§2) still describes claims with 'expiry' and should be reconciled with this rule | §2 / Stage 3 (partial; plan keeps expiry semantics) |
| R0184 | §14.3 | partial | M4–M5 | L | After seal/revoke, request and observe cleanup of the registered process tree/container; reclaim physical capacity only on a trusted stopped receipt or independently established sandbox destruction; unreachable occupancy stays reserved on that runner; a successor may run elsewhere only if authority, workspace, cost exposure and team limits permit; the dashboard reports the unconfirmed process. | src/runner.rs:185-223 stop() waits for observed termination and returns OutcomeUnknown 'still alive' instead of success; src/live.rs:118 records the stop on the board | No occupancy state, no runner capacity accounting, no successor admission rule, no 'unconfirmed' rendering in TUI/web (web/src/App.tsx:134 renders occupancy from a route the hub does not serve) | Stage 3 ('Stop remains Stopping until trusted termination proof') + Stage 5 |
| R0185 | §14.3 | missing | M4–M5 | M | A stale process that survives a partition cannot submit a current candidate, access its successor's writable checkout or publish privileged changes; instantaneous global termination is not promised and accepted remote billing is retained exposure. | none — no candidate submission path exists | Generation-checked submission (BF3-009-AC03), per-job workspaces, publisher credential separation, and retained-exposure ledger entry on unconfirmed stop | Stage 3 + Stage 5 |
| R0186 | §12 | missing | M4–M5 | M | One durable Job envelope for plan/implement/investigate/review/verify/shadow carrying schema_version, ID, purpose, lane, executor kind, mission, optional task/revision, input digest, base/candidate, profile, grant, budget hold, runner, dispatch generation, authority epoch and deadline; write-enabled implementation also carries the task writer generation. | none — src/board.rs:38-41 `jobs` table holds only provider/cwd/argv/pid/pgid/log/exit and is unused because bf run is unimplemented (src/main.rs:270) | Restorable from 9f03362:migrations/001_core.sql:60-81 (jobs with purpose/lane/executor_kind/generations/epoch/deadline and CHECK constraints per purpose) | Stage 3 ('persist job identity/purpose/generation/deadline') |
| R0187 | §12 | missing | M4–M5 | M | Lanes shipping/shadow/wildcard are policy tags over one machinery; read-only planning/review never acquires the writer lease; the verify executor is a registered trusted check runner, not a model; a human contribution records an exact commit and owner without a provider session or heartbeat. | none | Lane column + CHECKs (9f03362:migrations/001_core.sql:63,75-78), verifier principal kind, human_contributions table (9f03362 line 88) and the 'submit_human' command (web/src/wire.ts:2 lists it with no handler) | Stage 3 (partial; lanes and human contribution not named) |
| R0188 | §12.1a | missing | M4–M5 | M | Hub treats result frames as proposals: validate role, subject, current authority and purpose; a process exit, success:true or final answer cannot set merged/verified/observed_healthy; events name job ID, assignment generation and producer sequence while the hub derives runner and global sequence; a missing usage artifact is unknown, not zero. | none — no result or event ingestion on main (src/api.rs:37-48 routes) | Runner endpoints (poll/heartbeat/events/artifacts/results bound to generation, per plan Stage 2 table), result-v3 validation, purpose-vs-artifact check; shared with the adapter area (BF3-007) | Stage 2 (runner endpoints) + Stage 3 |
| R0189 | §12.2 | missing | M4–M5 | M | Track three separate dimensions per job: lifecycle (prepared/starting/running/stopping/succeeded/failed/interrupted/unknown), logical writing authority (none/active/sealed/revoked, one active shipping writer per task) and physical occupancy (allocated/running/stopped/unconfirmed). | none on the hub; web/src/App.tsx:134 renders lifecycle/occupancy fields from /v3/drafts/{id}, a route src/api.rs:37-48 does not serve | Restorable from 9f03362:migrations/001_core.sql:60-81 columns and CHECKs; restore the detail route or remove the dangling client fields | Stage 3 |
| R0190 | §12.2 | partial | M4–M5 | L | Every machine job including planners, reviewers and verifiers has a durable start intent, exact runner incarnation, deadline, bounded output, terminal receipt and recovery path; late results may be retained as observations but cannot replace a newer job's authoritative result. | src/runner.rs:44 fixed 30 s wall deadline; src/runner.rs:87-99 output bounded to 1 MiB per stream | No durable start intent, incarnation, terminal receipt or restart recovery (restorable 9f03362:migrations/002_workbench.sql launch_intents, src/jobs.rs:458-467 recover()); deadline must come from the admitted job | Stage 3 |
| R0191 | §12.3 | missing | M4–M5 | M | Persist the task state machine (proposed→ready→working→checking→review_ready→integrating→merged; investigation→evidence_accepted; failed/cancelled/superseded) with an orthogonal hold_reason (auth_required, dependency_invalid, budget_unavailable, review_required, resource_conflict, check_missing, outcome_unknown) carrying owner and next action; a failed attempt does not fail the task and a retry needs a current authorized decision. | none — src/error.rs:9-39 carries the matching error codes but no task or hold is persisted | tasks table with phase/hold_reason/version (base 9f03362:migrations/001_core.sql:30-36) and a retry_task command requiring human authority (web/src/wire.ts:2 lists retry_task with no handler) | Stage 3 (dependency readiness; hold_reason not named) |
| R0192 | §12.3 | missing | M4–M5 | S | Publication is a separate observed attribute; review_ready means automated preparation and required automated review are complete; merge_eligible is a derived predicate never model-authored; the mission's declared boundary (pr_ready/merged/production_observed/evidence_accepted) cannot silently downgrade. | none | Derived merge_eligible view and mission delivery_goal column with downgrade refusal; shared with the delivery area | Stage 6 (publication attribute; derived predicate not named) |
| R0193 | §14.4 | missing | M4–M5 | M | Persist a fair rotation cursor across eligible owners/domains; within a lane order by explicit priority, dependency-unblocking value, age and stable ID. | none — 9f03362:src/jobs.rs:232 ordered only verify-first then rowid | scheduler.rs with persisted cursor and deterministic ordering plus a rotation test | no |
| R0194 | §14.4 | missing | M4–M5 | M | Enforce tunable limits (four team writers, two per owner, two verifier slots, one native integration path per repository, eight pending managed PRs per repository) and prepare roughly two waves of executable tasks. | none | Limit table checked inside admission (§14.1) with per-owner/team/repository counters | no |
| R0195 | §14.4 | missing | M4–M5 | M | Protect interactive capacity and explicitly allocated owner budgets; owner preference chooses among eligible profiles; idle capacity is never permission to use another person's credentials. | none — plan defaults to one managed account, so the rule is untested | Owner-bound credential/profile eligibility in routing and a negative test that an idle second owner's profile is not selected | no |
| R0196 | §18.1 | missing | M4–M5 | L | Choose eligible profiles by data permission, current authority, needed tools, risk, context, owner preference, health and capability before price; then an explicit task-class route (economical/standard/frontier); no family is permanently writer or judge; no cheap-call quota. | none — src/hub.rs:113-124 doctor() only probes CLI versions | Profile registry with eligibility attributes and routing.rs selection; plan pins one Codex profile (Stage 5) and defers routing | no |
| R0197 | §18.1 | missing | M4–M5 | M | Report accepted surviving parent outcomes, total monetary/compute cost, active human handling and elapsed time separately; a blended metric discloses its conversion; zero accepted outcomes makes cost per outcome undefined, not zero; monitoring makes no model calls. | none for reporting; src/live.rs:260 snapshot() reads /proc and SQLite only (no model calls) for the TUI | report.rs over the ledger and task outcomes with undefined-not-zero semantics; Stage 7 records usage/elapsed/interventions for a single pilot task only | Stage 7 (partial) |

## contracts-store-auth+assurance-and-ops (4 rows: missing 3, partial 1)

| row_id | ID | Status | Milestone | Size | Requirement | Evidence on main | Gap | In prior plan |
|---|---|---|---|---|---|---|---|---|
| R0032 | AT-032 | missing | M1 | S | Gate negative case: trusted oracle retained; known-good passes and injected behavior fails. | none | Same as BF3-001-AC01: restore fixtures/gates/dedup and at032_gate_oracle from 9f03362 and run it in CI. \| No test on main names or exercises AT-032. Restorable from 9f03362:tests/acceptance.rs:113. | Stage 1 |
| R0053 | BF3-001-AC01 | missing | M1 | S | A known-good gate fixture passes and an injected behavioral defect fails without removing its oracle. | none (no fixtures/ or docs/onboarding/ on main; `find fixtures` empty) | Restorable from 9f03362:fixtures/gates/dedup/{check.py,good.py,wrong.py,src/dedup.py} + 9f03362:src/fixtures.rs (gate_at032) + 9f03362:tests/acceptance.rs:113 at032_gate_oracle; re-land under fixtures/gates/ with the test wired into scripts/check. \| No gate fixture on main. Restorable from 9f03362:fixtures/gates/dedup/{check.py,good.py,wrong.py,src/dedup.py} plus 9f03362:tests/acceptance.rs:113 at032_gate_oracle; needs a Rust test on main that runs the oracle against good and wrong. | Stage 1 (recover 9f03362 components, qualification records) |
| R0054 | BF3-001-AC02 | missing | M1 | S | Missing ownership, protection, credentials or checks remain unresolved and produce no live spending or mutation. | src/main.rs:259-272 `bf run` returns not-implemented (exit 3, tests/cli_skeleton.rs:57); src/main.rs:309-330 doctor is read-only; AGENTS.md:18-20 HOLD | No durable record of which repos lack ownership/protection/credentials/checks and no code path that surfaces them as typed blockers (9f03362:src/hub.rs:197 had a live_blockers list); readiness must be a stored per-repository/task-class fact, not prose. [critic: src/main.rs:269-271 returns 'not implemented yet' and src/main.rs:309-330 doctor is read-only, so 'no live spending' holds only because no code can spend. The AC requires the missing ownership/protection/credentials/checks to 'remain unresolved' as recorded facts; main has no repository, owner, prot] \| Unresolved ownership/protection/credential/check state is not recorded as typed blocks; nothing structural prevents a future adapter from dispatching without them. | Stage 1/Stage 3 (no default demo owner/grants; HOLD) |
| R0055 | BF3-001-AC03 | partial | M1 | S | Record which existing modules can be reused and which task classes remain assisted-only. | docs/implementation-plan.md §1 audit table (Observed state / Consequence) | No assisted-only task-class record and no per-module reuse decision under docs/onboarding/ as the package's source scope requires; the audit table names symptoms, not classes. \| No docs/onboarding/ record naming reusable modules versus assisted-only task classes; the plan's audit is against base 0cc243a, not current main. | Stage 0/1 (audit) |

## operator-surface (58 rows: done 2, missing 43, partial 13)

| row_id | ID | Status | Milestone | Size | Requirement | Evidence on main | Gap | In prior plan |
|---|---|---|---|---|---|---|---|---|
| R0038 | AT-038 | missing | M7 | M | A new human clone uses a known exact checkpoint, and missing unsaved work from an unreachable runner is disclosed rather than claimed recovered losslessly. | none | Takeover response must carry checkpoint identity plus an explicit `uncaptured_work: unknown` disclosure when the runner is unreachable; test with a runner that never acknowledges the stop. | no |
| R0039 | AT-039 | missing | M7 | S | An authorized new revision may progress while total money/invocations are preserved across revisions. | none | Test: revise, observe zero local attempts, confirm lifetime totals unchanged and progress refused until grant_allowance. | no |
| R0089 | BF3-013-AC01 | missing | M6 | M | Fixture demo (`bf demo --fixture basic`) requires no keys and clearly labels its fake executor/forge. | none on main — `demo` verb absent and tests/cli_skeleton.rs:35 asserts it is hidden; docs/first_win.md:1-3 still instructs `bf demo --fixture basic` and "Try the demo" (stale) | Restore from 9f03362:src/fixtures.rs (run_fixture, receipt with fake:true and limitations) and 9f03362:src/main.rs Demo verb; fix docs/first_win.md to match main until then. | Stage 3 ("demos retain distinct run directories"; §4 says fixture demo cannot satisfy release) |
| R0090 | BF3-013-AC02 | missing | M6 | L | A live demonstration records exact versions, checks, costs and unresolved limits instead of inheriting fixture certification. | none (README.md:29-33: live execution not qualified; no evidence/ or BUILD_CHECKPOINT.json on main) | Stage 7 dogfood record: exact binary/source/approvals/checks/usage/uncertainty/elapsed/PR URL, stored as evidence separate from fixture receipts. | Stage 7 (record exact versions/source/approvals/checks/usage/uncertainty) |
| R0091 | BF3-013-AC03 | missing | M6 | L | The author can stop while verification/delivery continue; replay and interruption do not duplicate logical publication. | none on main (no jobs/publication model; src/runner.rs:185-224 stops host process groups only) | Controller behaviour from BF3-009/011/012 plus an operator-visible stop that leaves verifier/publisher jobs running; tests restorable from 9f03362:tests/hardening.rs publication_recovers_each_durable_boundary_in_fresh_hub. | Stage 3 (Stop remains Stopping until termination proof) / Stage 6 (one stable PR identity per revision) |
| R0095 | BF3-015-AC01 | missing | M7 | L | On takeover the old writer is fenced, pending publication eligibility withdrawn, the change reservation retained, a bounded checkpoint/stop requested, in-flight effects reconciled, and the latest known exact checkpoint (or clean base in a different human clone) is offered; a potentially still-written directory is never transferred. | none (no jobs/writer generations/reservations on main; 9f03362 take was a POLICY_DENIED stub) | Implement take transaction: bump writer generation, mark old job fenced, keep pending-change reservation, request stop with checkpoint, record checkpoint artifact, hand a clone-instruction to the human. Needs BF3-009 claims/generations and BF3-012 reservation model. | no (G2 in §5 table only) |
| R0096 | BF3-015-AC02 | missing | M7 | M | Human submission (`submit-human`) and return (`return`) run the identical independent checks as agent candidates. | none (no verification on main; 9f03362 had fixture-only verification) | submit_human adopts an exact commit as a sealed candidate under current scope and enqueues the same verifier jobs; return records checkpoint/decisions/unresolved failures and admits a bounded agent job only when authorized. | no |
| R0097 | BF3-015-AC03 | missing | M7 | M | A new contract revision may show a zero revision-local attempt count but receives additional allowance only through an explicit authorized extension; lifetime generations, costs, failure evidence and parent requirements remain. | none (no contracts/revisions/attempt accounting on main) | Revision lineage with lifetime counters, `revise` guard (no implicit allowance), `grant_allowance` human-only path; AT-039 test. | no |
| R0104 | BF3-018-AC01 | partial | M7 | M | UI and CLI agree on committed state; stale commands and unauthorized reads fail server-side. | src/commands.rs:33-57 replay/conflict; src/api.rs:49-75,82-95 auth on every route; tests/core.rs:30 | No expected_version/STALE_VERSION enforcement for any kind (commands.rs:11-31 ignores target_id/expected_version); CLI and UI do not read the same projections because none exist. Restore 9f03362 command_inner version checks and add a test posting an old expected_version from both clients. | Stage 2 (reject stale versions) / Stage 4 (duplicate acceptance → same operation or precise stale conflict) |
| R0105 | BF3-018-AC02 | partial | M7 | M | Slow clients and large output cannot stall control or results; reconnect repairs state via cursor + permission-filtered snapshot. | web/src/App.tsx:60-88 (client SSE loop with 256 KiB buffer bound, reconnect and resnapshot); src/api.rs:29,83-85 (32 request slots, reject on saturation); src/storage.rs:18,57-64 (bounded DB queue) | No /v3/events on main; no stream permit limit, no committed sequence cursor, no compaction snapshot. Restore 9f03362 events() (streams semaphore, snapshot on disconnect) and add sequence cursors; test slow_streams_have_a_lifetime_bound_and_control_remains_available. | Stage 2 ("Event stream: bounded updates, reconnect cursor, snapshot recovery") / §4 Responsiveness |
| R0106 | BF3-018-AC03 | partial | M7 | M | Keyboard-accessible status and why remain useful without any model provider. | src/tui/mod.rs:160-199 full keyboard model with textual states, works with no provider (tests/tui.rs, tests/cli_wiring.rs:74); web/src/App.tsx:59 | Status/why do not exist for tasks (see §20.1-status/why); web list has no keyboard navigation beyond native buttons. | Stage 4 |
| R0169 | HF19 | missing | M7 | M | Under a slow UI plus profile-event flood, buffering is bounded and status, cancellation, durable results and leases retain priority. | none on main (only generic request-slot bound src/api.rs:83-85; no event producers exist) | Separate stream permits from control-request slots, drop/compact profile events per subscriber, and test that /v3/work and stop succeed while 16+ slow subscribers are held open under a flood. | §4 Responsiveness negatives (slow clients, floods) |
| R0198 | §20.1-init | missing | M3/M7 | S | `bf init` creates local configuration with live dispatch off and creates no remote rights. | none (verb absent: src/main.rs:29-115; tests/cli_skeleton.rs:16-29 lists agents/board/claim/heartbeat/release/note/stop/prs/run/doctor/serve/web only) | Add `init` writing config in BF_DATA_DIR with live=false; idempotent; no network. | no |
| R0199 | §20.1-serve | partial | M3/M7 | S | `bf serve --local` runs the single hub/API/static web on a private local endpoint. | src/main.rs:106-112,416-458 (`bf serve --bind` loopback-only, single instance, embedded assets served by src/api.rs:167-201); tests/core.rs:12 | Flag is `--bind`, not `--local`; no team mode alternative. Rename/alias `--local` and keep loopback default. Cosmetic once the hub is real. | Stage 2 (one hub authority) |
| R0200 | §20.1-connect | missing | M3/M7 | M | `bf connect <team-endpoint>` connects a client to a remote hub with authenticated transport. | none (src/main.rs:374-396 connect() only reads local endpoint.json and refuses non-127.0.0.1 URLs) | Plan §2 chooses private SSH forwarding to an Ubuntu hub; implement `connect` that stores a saved SSH host, forwards the loopback port, reuses host-key verification and never manages bearer tokens by hand. | §2 product journey (saved SSH host); no stage assigns it |
| R0201 | §20.1-runner | missing | M3/M7 | M | `bf runner enroll` / `bf runner start` enroll and run an explicitly authorized runner. | none (verb absent: src/main.rs:29-115) | CLI wrapper over BF3-004-AC03b enrollment plus a poll loop hitting /v3/runners/{id}/poll; blocked on runner endpoints (BF3-009 area). | Stage 2 (runner endpoints) — CLI verb not named |
| R0202 | §20.1-doctor | done | M3/M7 | S | `bf doctor` is a read-only health/capability report that works without model calls. | src/main.rs:308-330 (no DB lock, probes CLI --version only, reports data dir, endpoint, SQLite, node/npm pins, gh auth, identity); tests/cli_skeleton.rs:39 doctor_reports_sqlite_and_pins; tests/core.rs:20 doctor_reports_pinned_sqlite | Report content will need grants/HOLD/qualified-profile fields later (hub.rs:114-128 still says "Core hub only" and returns no `provider` object although web/src/App.tsx:117 reads d.provider). Not a blocker for the obligation as written. | Stage 2/§2 (separate provider install/auth/qualification/eligibility) |
| R0203 | §20.1-submit | missing | M3/M7 | S | `bf submit <task.json>` submits an exact task contract for admission. | none (verb absent; no task contract validator on main — schemas/ and src/domain.rs exist only at 9f03362) | Add verb posting create_mission/submit through POST /v3/commands with the raw file bytes; depends on BF3-002 validators and BF3-006 activation. | no (Stage 4 covers browser goal submission only) |
| R0204 | §20.1-plan | missing | M3/M7 | S | `bf plan "Explore ..."` requests a draft only and never starts implementation. | none (verb absent: src/main.rs:29-115) | CLI verb that posts a plan-only mission command; server must refuse activation from this path. Planning job itself is BF3-017. | Stage 4 (browser planning, Fast mode) — CLI verb not named |
| R0205 | §20.1-run | missing | M3/M7 | M | `bf run "Implement ..." --goal pr_ready` requests in-policy planning and implementation under a selected standing grant and cannot manufacture one. | src/main.rs:94-103,259-272 (`bf run <provider> <prompt> [--yolo]` is a stub exiting 3 with a different signature); tests/cli_skeleton.rs:57 unimplemented_verbs_exit_three_and_name_their_pr | Replace the stub with the spec signature (goal text + --goal), route through /v3/commands with grant selection server-side; drop the `--yolo` flag (universal-yes automation is forbidden by §10). Live execution gated by HOLD/BF3-010. | Stage 4 (goal submission does not authorize implementation) + Stage 5; CLI verb not named |
| R0206 | §20.1-chat | missing | M3/M7 | M | `bf chat <mission>` discusses/steers a mission through typed proposals. | none | Depends on Foreman (BF3-017) and conversation persistence (9f03362 had a `conversation` table, migrations/002_workbench.sql). Add CLI verb once the mission conversation route exists. | Stage 4 (owner-bound conversations) — CLI verb not named |
| R0207 | §20.1-status | missing | M3/M7 | M | `bf status --mine / --team` returns deterministic mission/task state with zero model calls. | none (bare `bf`, `bf agents`, `bf board` show host sessions/claims, not mission or task state: src/main.rs:163-171, src/tui/model.rs:10-39) | Add GET /v3/work?owner=&domain=&phase= projection and a `status` verb rendering it (--mine/--team filters); table must carry phase, next step, blocker, PR, evidence, cost, freshness. 9f03362 had /v3/work + task_projection to restore. | Stage 4 ("Status/Why/accounts/monitoring make zero model calls"); CLI verb not named |
| R0208 | §20.1-why | missing | M3/M7 | M | `bf why <task>` gives the exact blocker, subject and next action deterministically. | none (only analog is PR `missing` preconditions in src/prs.rs:248-268 shown by the TUI, src/tui/model.rs:505-511 — for GitHub PRs, not tasks) | Add GET /v3/tasks/{id}/why producing reason/next action from committed state, plus CLI verb. 9f03362 task_projection.why is a restorable starting point (9f03362:src/jobs.rs:199-205). | Stage 4 (Why zero model calls); CLI verb not named |
| R0209 | §20.1-take | missing | M3/M7 | L | `bf take <task>` creates a durable takeover operation under the person's current domain authority (checkpoint_preference payload). | none (kind not accepted: src/commands.rs:9; 9f03362:src/commands.rs:219 was an explicit POLICY_DENIED stub) | Whole BF3-015 mechanism (see BF3-015-AC01) plus CLI verb and `take` command guard (current human owner/delegate, checkpoint_preference last_verified\|last_durable). | no (G2 listed in §5 table only) |
| R0210 | §20.1-submit-human | missing | M3/M7 | M | `bf submit-human <task> --commit <oid>` adopts exact submitted content under current scope and runs the same independent checks. | none | CLI verb + submit_human command (exact candidate artifact + contract digest guard) feeding BF3-011 verification; see BF3-015-AC02. | no |
| R0211 | §20.1-return | missing | M3/M7 | M | `bf return <task> --checkpoint <artifact-id>` records the human's checkpoint, decisions and unresolved failures and admits a new bounded agent job when authorized, without needing the person's home directory or chat history. | none | CLI verb + `return` command (exact checkpoint + handoff artifact, current owner and remaining allowance) and artifact upload path for the checkpoint. | no |
| R0212 | §20.1-revise | missing | M3/M7 | S | `bf revise <task> --file <task.json>` submits a new contract revision against the old digest with no implicit allowance increase. | none | CLI verb + `revise` command guard (new contract artifact/revision, old digest) wired to BF3-015-AC03 accounting. | no |
| R0213 | §20.1-decide | missing | M3/M7 | M | `bf decide <decision> --option <id>` resolves a named decision for its exact subject digest by the designated actor class. | none (resolve_decision only exists as a TypeScript literal in web/src/wire.ts:2) | Needs a decisions table/route (spec §17.1 escalation fields), resolve_decision guard (option + subject digest, designated actor class not just owner id), CLI verb and decision card in the web (§20.4-decision-card). | no |
| R0214 | §20.1-pause-stop-cancel | missing | M3/M7 | M | `bf pause <mission>` stops new dispatch/privileged sends while admitted jobs drain; `bf stop` additionally requests termination (resume is a separate decision); `bf cancel <task-or-mission>` revokes further work/delivery and reconciles dispatched effects; none promises a remote undo. | src/main.rs:90-91,245-253 (`bf stop <pid>` kills a host process group — different semantics); src/commands.rs:9 accepts kind 'stop' but returns NOT_IMPLEMENTED (:59-64) | Implement pause/stop/cancel/resume on missions with expected_version check and control_state, plus CLI verbs taking a mission/task id. Restorable from 9f03362:src/commands.rs:220-231 and tests stop_before_dispatch_releases_holds_and_rejects_stale_control / cancel_cannot_resume_or_publish. Keep the host-process `stop <pid>` under a different name. | Stage 3 (Pause stops admission; Stop remains Stopping until termination proof; cancel fences and reconciles); CLI verbs not named |
| R0215 | §20.1-report | missing | M3/M7 | M | `bf report --window <id>` produces the model-free window report (completed outcomes, review-ready vs merged, missing checks, failures, pending reservations, decisions/owners, known/estimated/unknown spend, unconfirmed processes, next actions). | none (closest is the generated AGENT_CHAT.md digest of claims/notes: src/board.rs:582-634) | Depends on attention windows and accounting (BF3-020). Add report projection route and CLI verb; narration optional and must not invent counts. | no (BF3-020 not staged) |
| R0216 | §20.1-export | missing | M3/M7 | S | `bf export --out <directory>` writes a portable, non-live export of definitions and evidence. | none | CLI verb over BF3-021 export format; grants/leases must not be exported as live. | no (§5 G2 mentions restore/export; no stage) |
| R0217 | §20.2-one-endpoint | partial | M3/M7 | L | CLI, web and later channels submit mutations through one command endpoint (POST /v3/commands) and do not implement separate business rules or open a second writable authority. | src/api.rs:43 (/v3/commands), src/commands.rs:11-82 (strict JSON, exact-bytes digest idempotency; tests commands.rs:111,123); web/src/App.tsx:97,105-108 posts commands with schema_version 3/command_id/kind/target_id/expected_version | All CLI board verbs write ~/.bf/bf.sqlite directly (src/board.rs:711-725, cli_* at :924-1049) and the TUI mutates the board and signals processes directly (src/live.rs:118-140,304-332), bypassing the hub; hub ignores target_id/expected_version and only knows note/release/stop. Move claim/heartbeat/release/note/stop behind /v3/commands with actor from the session, make the TUI a client of the hub API, and enforce target/expected_version per kind. | Stage 2 ("CLI/TUI mutations use authenticated hub API, never another writable authority") + §5 after-release "finish TUI on shared API" |
| R0218 | §10-deterministic-reads | partial | M3/M7 | M | Status, readiness, grants, cost and process health are answered by deterministic reads; the Foreman runs only on user input, planning, unresolved uncertainty or requested narration, with no permanent token-consuming supervisor. | src/agents.rs:49-96 + src/tui/model.rs:255-283 (process health for host sessions with no model calls); src/main.rs:308-330 doctor; web/src/App.tsx:59 ("Status uses no model calls") | Only process health exists; readiness, grants and cost have no store or read path; no Foreman at all. Add the projections behind status/why (rows §20.1-status/why) and keep model calls out of them. | Stage 4 (zero model calls for Status/Why/accounts/monitoring) |
| R0219 | §10-plan-run-semantics | missing | M3/M7 | M | `bf plan` requests a draft only; `bf run` requests in-policy planning + implementation under a selected grant; once a conforming plan is activated, runnable tasks start fresh automatically. | none (src/main.rs:259-272 run stub; no plan verb) | Server-side guard distinguishing draft-only from activation, and automatic dispatch after activation (BF3-006/009). CLI rows §20.1-plan/run. | Stage 4 (structural admission before Start work; goal submission does not authorize implementation) |
| R0220 | §10-no-menu-automation | partial | M3/M7 | S | No native interactive plan mode for approved work, no menu indexing, arrow-key automation or universal `yes`. | src/main.rs:94-103 (`bf run --yolo`: "Allow the provider to run without permission prompts") — flag exists on a stub that never executes; nothing else automates menus | Remove `--yolo` from the CLI surface and encode the no-prompt policy in the execution profile (BF3-010) instead of a user flag. | Stage 5 (Codex `exec --json`, no menu/TUI automation) |
| R0221 | §10-typed-routing | missing | M3/M7 | M | Typed questions, findings and decisions route only to relevant jobs/owners; no broadcast transcript. | none for missions (board addressed notes `--to <agent>` in src/board.rs:434-477 are developer coordination, and the regenerated AGENT_CHAT.md digest is itself a broadcast) | Decision/finding records with owner and audience, surfaced only to the viewer they concern (feeds §20.4-decision-card and §17.1 escalation). | no |
| R0222 | §17-provisional-work | missing | M3/M7 | M | A person may work provisionally in a separate clone while an old remote effect is unknown, but no conflicting publication or unsafe reservation release occurs until that effect is reconciled. | none | Reservation stays held during OUTCOME_UNKNOWN; submit_human/publish blocked with a specific reason until reconciliation (ties to BF3-012 AT-035/036). | no |
| R0223 | §17-no-home-access | missing | M3/M7 | S | `submit-human` and `return` never require access to the person's home directory or private chat history. | none (commands absent); note src/live.rs:338-354 reads ~/.claude transcripts for the TUI, which must not be reused for takeover intake | Design the two commands to take only a commit oid / artifact id; add a test that they succeed with HOME unreadable. | no |
| R0224 | §17.1-escalation-contents | missing | M3/M7 | M | Each escalation states owner, age, requirement, exact failed criteria, attempted routes, cumulative costs/confidence, latest salvageable work and the smallest next decision. | none | Escalation/decision record type + projection route; produced by controller on hold/fail (BF3-009/011) and consumed by web decision cards and `bf decide`. | no |
| R0225 | §17.1-owner-options | missing | M3/M7 | M | The owner can respecify, decompose, implement, reassign, grant a justified extension or withdraw; repeated infrastructure problems are grouped rather than one prompt per worker. | none | Decision options enumerated on the escalation record; grouping key for infra incidents; UI/CLI expose them. | no |
| R0226 | §17.1-no-free-attempts | missing | M3/M7 | M | A whitespace edit, task split or model-written new ID cannot create free attempts; children inherit parent requirement accounting and require explicit allocations. | none | Lineage keyed by parent requirement, not by contract digest; child allocations explicit; negative tests for whitespace/rename/split. | no |
| R0227 | §17.1-adhoc-registration | missing | M3/M7 | M | Ad-hoc human work can be registered before managed delivery (late registration supplies attribution, not permission) and urgent independent work may take its own protected PR. | none (board claims in src/board.rs:267-349 register paths for developer coordination, not tasks/PRs) | `adopt`-style registration command binding a branch/PR to a task for attribution only; no retroactive grants. | no |
| R0228 | §17.2-window-snapshot | missing | M3/M7 | M | A window names admission interval, spending allocation, capacity profile, report cutoff and optional drain deadline; at its boundary an honest snapshot is produced without discarding useful patches or releasing PR reservations. | none | BF3-020 window model; operator-surface part is exposing the window and its cutoff in status/report. | no (BF3-020 not staged) |
| R0229 | §17.2-report-contents | missing | M3/M7 | M | Reports work without a model and list completed parent outcomes, review-ready and merged changes separately, missing checks, failures, pending reservations, decisions/owners, known/estimated/unknown spend, unconfirmed processes and exact next actions; narration cannot invent counts. | none | Frozen report projection over committed state (see §20.1-report). | no |
| R0230 | §17.2-attention-and-privacy | missing | M3/M7 | M | Quiet hours apply to informational notices with a separate urgent path; human active-time is voluntary/self-reported or explicitly approved and labelled; no keystroke surveillance; team sees authorized facts while raw traces have narrower audiences. | none (src/live.rs:214-249 shows every discovered session's transcript tail to whoever runs the TUI — no audience control) | Notification policy and audience scoping on trace/transcript reads once the hub serves them; keep transcript tails behind the owner's own session. | no |
| R0231 | §20.4-filters | missing | M3/M7 | M | The single React workbench has Mine, Domain and Team filters over the same data. | none (web/src/App.tsx has a project selector :121 and mission filter :34 only; /v3/work?owner=&domain=&phase= absent) | Add owner/domain/team-scoped work query and three filter controls bound to it. | no (§2 names Work/Agents/Inbox/Changes views; Mine/Domain/Team not mentioned) |
| R0232 | §20.4-foreman-input | partial | M3/M7 | S | A compact Foreman input on the workbench. | web/src/App.tsx:123-125 goal textarea + "Review plan" (Ctrl/Cmd+Enter) posting `run`/`edit_draft` commands | Server side does not accept these kinds on main (src/commands.rs:9), so the input cannot produce a draft; Foreman planning is BF3-017. | Stage 4 |
| R0233 | §20.4-work-table-drawer | partial | M3/M7 | M | A work table plus a task/mission drawer; logs are drill-down content, not the home screen. | web/src/App.tsx:114 (Work section as cards with title/phase/why/PR), :134 (jobs/conversation section), :136 (raw JSON under <details>) | Backing routes absent on main; rows lack owner/domain, next step, blocker owner, evidence, cost, freshness (see §19.2(1) row); cards instead of a semantic table; no per-task drawer with requirements/checks/limits/controls. | Stage 4 / §2 (drawer holds requirements/activity/changes/checks/limits/controls) |
| R0234 | §20.4-summary-cards | missing | M3/M7 | M | Show outcomes delivered, decisions needing the viewer, review-ready changes, active work, holds/unknowns and spend/exposure. | none (App.tsx shows connection status and a message line only) | Deterministic count/status cards from committed state; no model may write a green status. | no |
| R0235 | §20.4-decision-card | missing | M3/M7 | M | Every decision card states the exact question, why it matters, recommended action, alternatives, owner, affected version, relevant evidence and remaining allowance. | none | Decision card component bound to escalation records; resolve via resolve_decision with subject digest. | no |
| R0236 | §20.4-source-and-time | partial | M3/M7 | S | Every significant state shows its source and observation time; stale observations and disabled capabilities are explicit. | src/tui/model.rs:295-302 ("gh unavailable: PRs stale since …"), src/tui/model.rs:276-280 (snapshot time in status line), tests/cli_polish.rs:112; web/src/App.tsx:128 (Accounts card states "Qualified: No", live connection unavailable) | Web work items carry no source/observed_at; add observed_at + producer to projections and render them; mark disabled capabilities (HOLD, unqualified profile) on the page. | Stage 2 ("Bounded projections with freshness") |
| R0237 | §20.4-no-synthetic-percent | done | M3/M7 | S | No completion percentages derived from tokens or task count. | web/src/App.tsx and src/tui/model.rs render phases/counts only; no percent computation anywhere (grep of web/src and src/tui) | Keep it that way when summary cards land; add a review checklist item. | §2 ("Unknown stays Unknown") |
| R0238 | §20.4-model-free-page | partial | M3/M7 | M | The page remains useful when all models fail: inspect state, determine ownership, see pending operations and request pause/stop. | web/src/App.tsx:131 pause/resume/stop/cancel buttons; :126-127 pending request retry/dismiss; :59 no model calls for status; bare `bf` TUI works fully offline (src/tui/mod.rs:249-261, tests/tui.rs:145) | On main the browser cannot load state at all (routes missing) and the hub returns NOT_IMPLEMENTED for stop; ownership is not shown. Restore reads and implement pause/stop server-side. | Stage 4 ("model-free pause" in 9f03362 browser spec) / §4 Browser negatives |
| R0239 | §20.4-accessibility | partial | M3/M7 | M | Keyboard navigation, readable contrast, semantic tables and no status conveyed by color alone. | web/src/style.css:1 (:focus-visible outline, narrow-width layout, light ink on dark bg); web/src/App.tsx:120-126 (labels, role=status aria-live, aria-labelled sections, aria-current); src/tui/ui.rs:54-62 NO_COLOR palette + src/tui/model.rs:327-333 textual states; tests/tui.rs:145 asserts monochrome rendering | Web has no semantic <table> for work (cards), no keyboard shortcuts for list navigation, no vitest/browser assertion of focus/contrast (9f03362 web/browser/workbench.spec.ts tested visible focus and narrow layout; absent on main). Add table markup, roving focus, and restore the browser spec under scripts/check. | Stage 7 (keyboard operation/announcements) / §2 release requirements |
| R0240 | §20.4-usability-test | missing | M3/M7 | S | Usability tests ask an engineer to locate the next actionable blocker without opening raw vendor logs. | none (no usability protocol or recorded session in docs/) | Write the protocol, run it on the restored workbench, record the result as evidence. | no |
| R0241 | §19.2(1)-row-definition | missing | M3/M7 | M | Work table rows show mission outcome and delivery boundary, owner/domain, phase and current step, actual next blocker and decision owner, selected candidate/PR, local/CI/integration evidence, actual/held/unknown cost, provider health and observation freshness (column list from provenance (1) §19.2; governed by canonical §20.4). | web/src/contracts.ts:4 WorkItem = {cursor, task_id, mission_id, version, title, phase, why, pr} only; no serving route on main | Extend the work projection with owner/domain, next_step, blocker_owner, candidate, evidence summary, cost {actual,held,unknown}, provider_health, observed_at, and render them. | Stage 2 (bounded projections) partially; column set not specified |
| R0242 | §24.1-demo-interrupted | missing | M3/M7 | M | `bf demo --fixture interrupted_publish` runs with fakes, no network, and deterministic receipts. | none on main | Restore fixture fault injection (9f03362 fixture_faults table, demo_interrupted_publish_reconciles_once test) once publication intents exist. | Stage 3 ("same controller handles normal/interrupted/cancelled/ambiguous fixture journeys") |
| R0243 | §13-session-revocation | partial | M3/M7 | S | Browser/CLI sessions are expiring and revocable, and revocation ends reads and SSE subscriptions. | src/hub.rs:66-91 (8 h expiry), :93-104 (revoked/expired rejected on every call), :106-112 revoke_session; src/api.rs:41 DELETE /v3/session; web/src/App.tsx:72 handles `disconnected` frame | No SSE on main to terminate; no test that a revoked token gets 401 on a subsequent read (9f03362 revoked_session_is_not_recovered_by_fallback). Add the test and the stream termination once events exist. | Stage 2 (expiring revocable sessions; revoked sessions negative) |

## delivery-and-forge+assurance-and-ops (7 rows: missing 7)

| row_id | ID | Status | Milestone | Size | Requirement | Evidence on main | Gap | In prior plan |
|---|---|---|---|---|---|---|---|---|
| R0045 | AT-045 | missing | M7 | M | Restore rollback: old publisher quiesced/revoked and external authority rotated before new work. | none | Drill/test: restore an older snapshot containing a dispatching effect and an unexpired grant; assert the hub opens paused, refuses dispatch until the rotation checklist is recorded, and reconciles the effect from the remote rather than re-creating. \| No test; requires BF3-021-AC01. | Stage 2 (partial) |
| R0046 | AT-046 | missing | M7 | M | Export/import: definitions and evidence survive a round trip; live grants and leases do not reactivate. | none | Round-trip test on a populated fixture hub: contracts, decisions, evidence and artifacts equal by hash; every grant/lease/session inactive after import. \| No test; requires export and import. | Stage 2 (partial) |
| R0113 | BF3-021-AC01 | missing | M7 | L | Restore starts paused, with the old publisher quiesced or its remote credentials revoked and external authority rotated through a mechanism outside the restored database, old runner sessions invalidated, artifacts validated and unsettled effects/open PRs reconciled before new work is authorized. | none (src/hub.rs:106-112 revoke_session revokes a single token; no restore mode) | Add a restore mode flag persisted outside the DB (data dir marker) that keeps admission paused; a checklist the operator confirms (gh token revoked/rotated, publisher epoch rotated), bulk session revocation on open, artifact validation pass, and effect/PR reconciliation before `resume` is accepted. A new epoch alone must not be treated as sufficient. \| No restore procedure, publisher or external authority on main. | Stage 2 (partial: revoke obsolete sessions, keep restored execution paused) |
| R0114 | BF3-021-AC02 | missing | M7 | M | Missing artifacts stay unavailable; cleanup/retention requires exact job/workspace identity, known disposition, policy and absence of active references; age alone is not deletion permission; failed candidate/checkpoint preserved before destructive reset. | none | Artifact half restorable from 9f03362 (see §16.4 artifacts). Retention job: select by exact job/workspace id + disposition + policy + no referencing evidence/effect/reservation; refuse age-only requests; capture failed candidate bundle before workspace reset; tests for live and unrelated work surviving cleanup. \| No artifact store or cleanup on main. Restorable partial: 9f03362:tests/hardening.rs:610 missing_artifact_blocks_publication_after_pass. | no (Stage 1 restores artifacts only; no retention stage) |
| R0115 | BF3-021-AC03 | missing | M7 | L | Old contracts and profile bindings import with provenance but without live grants or leases; export is portable. | none (docs/migration/source-import.json documents a source-tree consolidation with provenance, not task-data import; src/storage.rs:93-99 only tests for the schema_migrations table) | Restorable in part from 9f03362:tests/hardening.rs migration_preserves_old_records_and_does_not_enable_live (schema-001 rows kept, sessions revoked, grants forced fixture_only). Needs `bf export` and `bf import` (see §23.2 rows) with provenance columns and no active grant/lease/session after import. \| Needs export (§23.2-export) and import adapter (§23.2-import-adapter) plus a round-trip test. | Stage 2 (partial: migrate forward with provenance, revoke sessions, import paused); export: no |
| R0149 | CF07 | missing | M7 | M | An old digest or binding is never silently relabeled v3: import retains source bytes and digest scheme, validates the mapping, creates new definitions/proposed history and no active authority. | none | Scenario test with a v1 semantic-JSON digest and a J ExecutionBinding: assert stored raw bytes and scheme tag, a validated profile reference, and rejection when the relationship cannot be verified. \| No test; requires import adapter with digest-scheme tagging. | no |
| R0170 | HF20 | missing | M7 | S | Importing a profile export that carries old grants restores definitions/history only; new paused authority and fresh admission are required. | none | Scenario test: export with an unexpired grant; import; assert no eligible grant, hub paused, and a first admission attempt is refused until a new grant is issued. \| No test on main. Restorable partial: 9f03362:tests/hardening.rs:428. | Stage 2 (partial) |

## assurance-and-ops (49 rows: deferred_by_plan 8, done 3, missing 27, partial 11)

| row_id | ID | Status | Milestone | Size | Requirement | Evidence on main | Gap | In prior plan |
|---|---|---|---|---|---|---|---|---|
| R0048 | AT-048 | deferred_by_plan | M8 | S | Shadow isolation: shadow candidate cannot acquire shipping selection or effect. | none | G3 scenario; test after BF3-023 exists. | §5 G3 row |
| R0049 | AT-049 | deferred_by_plan | M8 | S | Shadow survival: unshipped shadow has no production-survival observation. | none | G3 scenario; reporting must label simulation-against-snapshot as a different metric. | §5 G3 row |
| R0052 | AT-052 | missing | M7 | S | Blind reconstruction: fresh agent uses kit alone; missing architectural decisions recorded. | none | Scenario receipt of BF3-022-AC02; nothing recorded. | §5 G2 row |
| R0116 | BF3-022-AC01 | missing | M7 | XL | Two owners/runners/provider families exercise overlap, takeover, long checks, expired grants and interruption. | none | Requires the whole G2 chain (BF3-014..021) and a second qualified provider family; no tests/acceptance/ on main. | §5 G2 row + after-release paragraph (qualify Cursor); no stage |
| R0117 | BF3-022-AC02 | missing | M7 | XL | A fresh agent reconstructs the fake-backed reference from the kit alone and records every missing assumption. | none | Kit incomplete (§24.1-kit) and reconstruction never performed; needs an independent reviewer to execute acceptance cases and record assumptions in docs/pilot/. | §5 G2 row (independent rebuild) |
| R0118 | BF3-022-AC03 | missing | M7 | L | Run the §25.4 pilot (~30 representative tasks, two owners, two provider families, frozen acceptance, three-way comparison) and report failures, complete cost, human handling and uncertain observations; no paper score substitutes for runtime evidence. | none | No docs/pilot/ or pilot design on main; product gate and non-compensable failure list not yet an executable checklist. | §5 after-release paragraph (representative team pilot) |
| R0119 | BF3-023-AC01 | deferred_by_plan | M8 | L | Shadow evaluations have no shadow selection, shipping claim or publication authority. | none | G3; no evaluations module on main; requires BF3-022. | §5 G3 row |
| R0120 | BF3-023-AC02 | deferred_by_plan | M8 | M | Fixed or explicitly adaptive initial state, gate/holdout and stopping criteria remain declared for every comparison. | none | G3; evaluation contract (§22.2 declaration fields) not modelled. | §5 G3 row |
| R0121 | BF3-023-AC03 | deferred_by_plan | M8 | L | Shared lessons require current owner approval, canary and rollback; running jobs remain pinned to their profile. | none | G3; requires versioned profiles and pinned job profile identity from BF3-010/HF02. | §5 G3 row |
| R0163 | HF13 | deferred_by_plan | M8 | S | A profile change that wins by changing acceptance tests invalidates the evaluation; trusted gate/holdout remains fixed. | none | G3 scenario. | §5 G3 row |
| R0164 | HF14 | deferred_by_plan | M8 | S | Online refinement treated as a frozen evaluation is rejected, or labelled as an adaptation treatment with initial/delta state. | none | G3 scenario. | §5 G3 row |
| R0168 | HF18 | deferred_by_plan | M8 | M | Rolling back a bad profile while attempts run changes only future admission selection; running subjects and accounting stay pinned. | none | G3 scenario; needs pinned job profile identity (HF02) and versioned profile selection. | §5 G3 row |
| R0330 | §7.2-workload | missing | M0/M7 | M | Declare and script the frozen benchmark workload: 10,000 historical tasks, four simulated runners, 20 status reads + 10 durable small mutations per second, warm-up then >=60 s steady state, slow-client and event-flood variants. | none | No benchmark script on main (scripts/ has only check and build-web). Restorable from 9f03362:scripts/benchmark.py (10 s workload, 200 reads/100 commands) but it targets /v3/work and create_mission routes that do not exist on main and must be extended to >=60 s with flood/slow-client variants. | §4 (frozen release-build performance workload; no stage number) |
| R0331 | §7.2-targets | missing | M0/M7 | S | Measure and record status/Why p95 <100 ms, durable small command p95 <250 ms, idle hub RSS <150 MiB, editor p95 <100 ms and zero model calls, with distributions, failures, workload identity, hardware and cold/warm startup. | none | No performance evidence on main. 9f03362:evidence/performance.json (status p95 2.5 ms, command p95 15.2 ms, RSS 9.9 MiB) is historical for pruned code and cannot certify current main. | §4 |
| R0332 | §7.2-separate-timing | missing | M0/M7 | M | Report remote network, model startup, clone/snapshot preparation, inference, tool execution, verification and integration time separately per job. | none | No job model on main (migrations/001_core.sql has only principals/sessions/commands/operations); timing phases need job/effect records from BF3-008/009/011/020. | no |
| R0333 | §7.2-bounded-queues | partial | M0/M7 | M | Bound every queue and protect heartbeat, cancellation and result commits from a slow UI or huge log stream. | src/api.rs:29 Semaphore(32) request slots with try_acquire rejection at src/api.rs:83; src/storage.rs:18 sync_channel(64) DB queue rejects on saturation | No event stream, runner heartbeat/result or cancel endpoints exist, so there is no priority lane for control traffic; HF19 (slow UI + flood) has no test. Restorable reference: 9f03362:tests/hardening.rs:625 slow_streams_have_a_lifetime_bound_and_control_remains_available. | Stage 2 (event stream row) and §4 Responsiveness |
| R0334 | §7.2-status-no-model | partial | M0/M7 | S | Status/Why/browsing make zero model calls and keep Git, probes, builds and network outside HTTP handlers and SQL transactions. | src/api.rs:35-46 routes reference no model client; src/hub.rs:114-123 doctor() spawns claude/codex/cursor-agent/grok --version inside the /v3/doctor request (src/api.rs:130) | No status/Why route exists to measure; provider probes run synchronously inside a request handler contrary to plan §4. Move probes to a cached background observation and add a test asserting no subprocess/model call on status paths. | Stage 4 exit + §4 |
| R0335 | §11-gate-diagnostic | missing | M0/M7 | M | Run the gate-first diagnostic on ~10 historical behavioral fixes: record selection criteria, keep the accepted oracle, establish a passing baseline, reintroduce each defect, and classify detected/missed/invalid injection/flaky/not-applicable. | none | No fixtures/gates/ or diagnostic record on main. 9f03362:fixtures/gates/dedup (good.py/wrong.py/check.py) is one synthetic case, not ten historical fixes, and has no classification table. | no (Stage 1 only mentions restoring qualification records) |
| R0336 | §11-negative-cases | missing | M0/M7 | M | Add negative cases: no-op candidate for new behavior, plausible constant result, missing test discovery, skipped mandatory cases, attempts to weaken acceptance; a whole-PR revert that removes the oracle is not a valid detection test. | none | No verifier on main. Restorable partial: 9f03362:tests/hardening.rs:411 early_exit_and_forged_completion_cannot_pass (early-exit/forged only); no-op, constant-result, skipped-case and weakened-acceptance cases were never written. | Stage 6 (verification negatives table in §4, partial) |
| R0337 | §11-admission-record | missing | M0/M7 | M | Persist an admission record per enabled capability: task classes, risk ceilings, profile/gate revisions, manual review obligations, permitted delivery boundary, residual gaps and approving principal. | none | No admission/grant tables in migrations/001_core.sql; nothing records which task classes are supervised-proposal-only versus autonomous. | Stage 3 (admitted repository/owner/task/profile/grant/limits, partial) |
| R0338 | §11-typed-onboarding-block | partial | M0/M7 | S | Missing infrastructure/configuration is a typed onboarding block, not a fabricated pass; the first live task needs a meaningful independent acceptance path before its capability is enabled. | src/main.rs:309 doctor reports data dir, gh auth, provider and toolchain gaps read-only; src/main.rs:259-271 `bf run` refuses with exit 3 (tests/cli_skeleton.rs:57) | Gaps are printed, not recorded as typed blocks bound to a repository/task; no independent acceptance path is defined for the first live task. | Stage 3 |
| R0339 | §22.1-experiment-budget-zero | missing | M0/M7 | S | Unconfigured experiment spending is zero, not an implicit percentage of the team's money. | none | No budget model on main; when BF3-005 budgets land, the experiment pool default must be 0 with a test. | no |
| R0340 | §22.4-replacement-review | missing | M0/M7 | S | Configure the named service owner's periodic review (maintenance burden, gate adequacy, human handling, accepted-outcome cost, simpler-system test using fixed cases and a portable export). | none | No ops runbook or cadence configured; depends on `bf export` for the replacement test. | no |
| R0341 | §23.1-inspect-before-replace | partial | M0/M7 | S | Inspect the actual repository, gates, current implementation, ownership, data policy, isolation and forge before replacing code; preserve conforming behavior; resolve the minimum first-task check contract and actual owner/grant/budget. | docs/implementation-plan.md:57-70 audit table; PR #1 merged 2026-09-18T20:19Z preserving all six histories (branch archive/history, 1527 archive tags) | Gate/ownership/data-policy/isolation/forge inspection is not recorded as an onboarding record; first-task check contract and actual owner/grant/budget remain unresolved. | Stage 0 (done) / Stage 1 |
| R0342 | §23.1-pins | partial | M0/M7 | S | Pin actual Rust/TypeScript toolchains, lockfiles, harness executable digests and effective profiles during the build. | rust-toolchain.toml:2 (1.97.1); .github/workflows/ci.yml:22; scripts/check:5-10 node 22.23.2/npm 10.9.8; scripts/check:16-19 --locked; src/storage.rs:80-86 SQLite 3.53.2 + source id | No harness executable digest (Codex CLI binary SHA-256) or effective-profile pin recorded; docs/implementation-plan.md:205 notes 0.154.0 -> 0.155.0 drift unresolved. | Stage 5 |
| R0343 | §23.1-canary-upgrades | missing | M0/M7 | M | Canary upgrades against protocol, permission, cancellation, accounting and negative fixtures; never change semantics mid-job; roll back by selecting a previously certified version for new jobs. | none | No versioned profile/executor records or canary procedure on main. | no |
| R0344 | §23.1-watch-signals | missing | M0/M7 | M | Surface unknown-effect age, physical unconfirmed occupancy, mandatory-check gaps, review pressure, dependency invalidations, scope contention, actual/estimated cost and escalation age. | none | No job/effect/cost model on main to derive these signals from; add to the status projection once BF3-020 accounting exists. | no |
| R0345 | §23.2-export | missing | M0/M7 | L | `bf export` produces a versioned manifest with immutable contracts/decisions/profiles, permitted gate references, ownership history, outcome/cost JSONL, evidence manifests, retained artifacts, exact hashes, explicit missing/withheld records, and no usable secrets or reactivated grants/leases. | none | src/main.rs:29-108 has no export verb and no exportable objects exist beyond commands/operations. | §5 G2 row (backup/restore/export); no stage |
| R0346 | §23.2-import-adapter | missing | M0/M7 | L | Import earlier content only through a named source-format adapter: preserve raw bytes and original digest semantics, map Attempt/ModelJob to purpose-tagged Job history, resolve J's ExecutionBinding into a direct profile reference, never relabel a v1 payload as v3. | none | No import path on main; src/storage.rs:93-100 only applies 001_core.sql when schema_migrations is absent. | Stage 2 (recognize/migrate/import with provenance, partial) |
| R0347 | §23.2-import-paused | missing | M0/M7 | M | Import begins paused/proposed under new authority: stop the old overlapping allocator, reconcile existing PRs, resolve owners and missing dependencies, issue new grants, activate only after validation. | none | No paused-import state on main. Restorable partial: 9f03362:tests/hardening.rs:428 migration_preserves_old_records_and_does_not_enable_live (old sessions revoked, grants forced fixture-only). | Stage 2 (import paused, revoke obsolete sessions) |
| R0348 | §23.2-schema-recognition | partial | M0/M7 | M | Fingerprint every actual schema variant (hub.sqlite 001 and the separate bf.sqlite board schema), back up consistently before migrating, migrate forward preserving history, and refuse unknown schemas without deleting data. | src/storage.rs:93-100 checks only schema_migrations existence; src/board.rs:1091 and src/live.rs:22 open a second bf.sqlite with its own schema | No fingerprinting, no pre-migration backup, unknown schemas proceed silently; two writable databases remain. | Stage 2 |
| R0349 | §24.1-scripts-check | partial | M0/M7 | S | `scripts/check` covers Rust formatting/lint/tests, schema fixtures and web typecheck/test/build using pinned lockfiles. | scripts/check:12-19 (npm ci/typecheck/vitest, build-web --check, cargo fmt --check, clippy -D warnings, test --locked, build --locked); scripts/check:5-10 node/npm pins | No schema-fixture validation (no schemas/ on main); no demo run; browser journeys from 9f03362 (web/browser/workbench.spec.ts + playwright) were dropped from check. | §4 (local scripts/check) + Stage 1 (restore coverage) |
| R0350 | §24.1-demo-fixtures | missing | M0/M7 | L | `bf demo --fixture basic` and `bf demo --fixture interrupted_publish` run with fakes, no network calls and deterministic receipts, and the docs describe them truthfully. | none | src/main.rs:29-108 has no Demo verb; tests/cli_skeleton.rs:35 asserts `demo` stays hidden; docs/first_win.md:1-3 documents the nonexistent command and `fake: true` receipt (stale). Restorable from 9f03362:src/fixtures.rs, src/main.rs:20-70, tests/acceptance.rs:127-153, but requires the pruned jobs/delivery/forge modules. | Stage 1/3 |
| R0351 | §24.1-build-checkpoint | missing | M0/M7 | S | Maintain BUILD_CHECKPOINT.json with exact spec/source identities, completed packages, checks, known gaps and the next eligible package, updated every slice. | none | `git ls-tree 1ea7aa60` contains no BUILD_CHECKPOINT.json (unpushed local commit f0048725 adds one). 9f03362:BUILD_CHECKPOINT.json is restorable as a template, but its implemented_fragments describe pruned code and must be reset to main's truthful state. | Stage 1 |
| R0352 | §24.1-kit | partial | M0/M7 | S | The build agent can read the final spec, START_HERE.md, v3 schemas and examples from the repository. | docs/spec/BULLETFARM_FINAL_ENGINEERING_SPEC.md SHA-256 1d8064b4… matches /home/ubuntu/bullet/tips; docs/implementation-plan.md:16-18 records that schemas/backlog/validator are absent | START_HERE.md and the (1) provenance copy are not on GitHub main (only in unpushed f0048725); seven v3 schemas, nine examples, backlog.json and validator are absent everywhere (9f03362 had only schemas/task.schema.json + fixtures/protocol/task.json) and must be recreated under BF3-002 with a repository-owned validator. | Stage 1 (kit placement); BF3-002 for schemas |
| R0353 | §25-test-receipts | missing | M0/M7 | M | Each scenario test receipt records exact versions, fixture, input, observed failure/pass, producer and artifacts. | none | Tests on main emit pass/fail only; no receipt format. 9f03362:src/fixtures.rs:33 DemoReceipt is a historical starting point. | Stage 1 (checkpoints distinguish implemented/fixture/outstanding; partial) |
| R0354 | §25-coverage-map | partial | M0/M7 | M | Every one of the 80 AT/HF/CF scenarios is traceable to a named test or an explicit not-yet record on main. | tests/core.rs:30 http_auth_and_command_replay matches the AT-003 shape (same key/body -> same operation id) and denies anonymous reads (AT-005 shape); tests/cli_wiring.rs:97/114 cover stop; grep of src, tests, web/src for AT-0/HF/CF finds zero scenario IDs | 0 of 80 scenarios are named by any test on main. Restorable subjects: 9f03362:tests/acceptance.rs (at001-005, at012, at032, cf06, hf17) and tests/hardening.rs (at035/036 publication boundaries, cf05, migration), whose subjects were pruned. Needs a naming convention plus a generated coverage table in BUILD_CHECKPOINT.json. | Stage 1 (preserve every BF3/AT/HF/CF mapping) |
| R0355 | §24.1-ci-workflow | done | M0/M7 | S | Hosted CI runs scripts/check on every push/PR with pinned toolchain, dependency caching, bounded concurrency, timeouts, minimal permissions and no provider credentials. | .github/workflows/ci.yml:1-32 (push/PR triggers, contents:read, concurrency cancel-in-progress, 15-min timeout, SHA-pinned actions, toolchain 1.97.1, cargo+npm caches, node from web/.nvmrc, npm 10.9.8, bash scripts/check); last three runs SUCCESS including 1ea7aa60 | none for the workflow itself (coverage gaps tracked under §24.1-scripts-check) | §4 |
| R0356 | §24.1-branch-protection | done | M0/M7 | S | main requires the `check` status, an up-to-date branch, linear history and administrator enforcement; archive tags are immutable. | gh api repos/neverhuman/bulletfarm/branches/main/protection: contexts=[check], strict=true, enforce_admins=true, required_linear_history=true, allow_force_pushes=false, allow_deletions=false; ruleset 23674971 'Immutable migration archive tags' active | none (no native review requirement by design: shared identity, review is a process obligation) | Stage 0 / migration README PR A (lines 53-60) |
| R0357 | §24.1-review-protocol | partial | M0/M7 | S | Every PR carries bf-author, bf-claim and canonical BF3 acceptance mapping, passes local and hosted check, and is approved by a different-vendor `REVIEW: approve <exact head sha>` before rebase-merge. | AGENTS.md:37-50 documents the protocol; PR #1 body ends `bf-author: codex/codex-consolidation` / `bf-claim: c-49` with BF3 scope, and reviews contain `REVIEW: changes 15c8ce5d…`, `REVIEW: changes 3b42a643…`, `REVIEW: approve 3b42a643…` (claude-orch) matching the merged head | Not machine-enforced: no CI/merge check verifies the bf-author/bf-claim/BF3 lines or that a different-vendor approve names the merged head SHA; src/prs.rs lists only gh merge preconditions (tests/cli_prs.rs:196). | §4 |
| R0358 | §migration-PR-A | done | M0/M7 | S | Establish neverhuman/bulletfarm as the sole repository: preserve all six source histories and snapshots, import the frozen application tree, record identity exceptions, and close the superseded bf PR 12 after capture. | PR #1 merged 2026-09-18T20:19:29Z; branch archive/history and 1527 archive tags present; docs/migration/source-import.json records the import; bf PR 12 CLOSED 2026-09-18T18:29Z with tag archive/bf/pull/12/head present | none | Stage 0 / migration README PR A |
| R0359 | §migration-PR-B-rename | missing | M0/M7 | M | Rename the package and primary binary to `bulletfarm`, keep Rust library name `bf`, install `bf` as an alias, replace the legacy launcher with a retirement message, keep ~/.bf and BF_DATA_DIR, and update help, metadata, UI branding, badges and links. | none | Cargo.toml:2 name="bf" and Cargo.toml:36 [[bin]] name="bf"; src/main.rs:15 command name "bf"; web/src/App.tsx:117 brand "bf"; installed ~/.local/bin/bulletfarm is a symlink to the legacy `bullet` binary (rejects --version) while ~/.local/bin/bf is 0.1.0. | implementation-plan.md:14 / migration README PR B (lines 61-72) |
| R0360 | §migration-PR-B-pr-discovery | missing | M0/M7 | S | PR discovery maps exactly the five retiring identities (bf, bullet-farm, bullet-kernel, bullet-git, bullet-portal) to neverhuman/bulletfarm and deduplicates requests; unrelated repositories and historical note text keep their identities. | none | src/prs.rs has no canonicalization (grep bulletfarm/canonical/retir finds nothing); tests/cli_prs.rs:167 only normalizes origin URL forms; historical board rows would still generate requests to retired repos. | migration README PR B (lines 66-69) |
| R0361 | §migration-PR-B-identity-check | missing | M0/M7 | S | A CI identity check rejects retired repository URLs in active instructions/configuration, with explicit exceptions for historical records, the preserved specification and deliberate compatibility tests. | none | .github/workflows/ci.yml has no such step; retired names appear in docs/implementation-plan.md:122 (inventory sentence) and fixtures tests/cli_prs.rs:170-185, tests/tui.rs:103-177 that need an explicit exception list. | migration README PR B (lines 69-72) |
| R0362 | §migration-cutover-host | missing | M0/M7 | S | After PR B: install the verified build, update parent workspace instructions and active agent entry points, release old-path claims and acquire new-path claims, disable pushes from retired checkouts, and publish the source/PR migration index. | none | /home/ubuntu/bullet/AGENTS.md:1 still declares the product as the `bf/` checkout at github.com/neverhuman/bf; ~/.local/bin/bulletfarm -> bullet; retired local checkouts (bf/, bullet-farm/, …) remain writable. | migration README Cutover gates (lines 76-85) |
| R0363 | §migration-retirement | missing | M0/M7 | M | Archive the five retiring repositories, satisfy every deletion gate (fresh mirror integrity, checksum/restore drill, fresh checkout passes check, installed bulletfarm/bf parity, no duplicate PR requests, no new work), then delete exactly those five via operator-assisted delete_repo and verify only neverhuman/bulletfarm remains. | none | gh api shows bf, bullet-farm, bullet-kernel, bullet-git and bullet-portal all archived:false (zero open PRs on each); no restore-drill or installed-parity evidence recorded. | migration README Deletion gates (lines 86-110) |
| R0364 | §FU-codex-overcount | missing | M0/M7 | S | Discovery deduplicates Codex wrappers/children so one Codex session is one row with the correct session id and title. | none | src/identity.rs:20-22 classifies every process with comm==codex, exe ending /codex or cmdline containing @openai/codex, so a session's helper children each become rows; src/agents.rs:181-195 then hands distinct session_index rows to those processes in pid order, misattributing ids/titles (observed 18 rows vs 9 procs). tests/cli_agents.rs:29 covers only one codex process. Fix: collapse to the process-tree root via the ppid walk at src/identity.rs:40-60 and match session_index rows by cwd/updated_at, with a multi-process fixture. | §2 (dedupe wrappers/children) and §4 Discovery row; no stage |
| R0365 | §FU-transcript-paths | partial | M0/M7 | S | Transcript tails resolve for Codex, Cursor and Grok sessions, not only Claude. | src/live.rs:389-393 parses Codex/Cursor/Grok line shapes (unit tests at src/live.rs:548-553); src/live.rs:338-355 resolves a path only for Claude registry rows | src/agents.rs overlay_codex (154-195), overlay_grok (197-230) and Cursor classification never set transcript_path, so live.rs:339 returns None and tails are empty for those providers. Resolve ~/.codex sessions, Cursor and Grok transcript locations and add fixtures. | §4 Discovery row (unavailable transcript); no stage |
| R0366 | §FU-data-dir | missing | M0/M7 | S | The global `--data-dir` flag governs every verb, including board, claim, heartbeat, release, note, stop and the TUI. | none | src/main.rs:23 declares the global flag but src/main.rs:169-241 board/claim/heartbeat/release/note ignore `dir` and call board::cli_* which resolve src/board.rs:699-708 from BF_DATA_DIR/HOME only; src/main.rs:163 (TUI) and :251 (stop) use src/live.rs:50-58 the same way; only prs (main.rs:254) and the hub honour it. tests/cli_board.rs:205 covers HOME only. Fix: thread `dir` into Board/LiveSource constructors and add a --data-dir test. | no (Stage 2 moving mutations behind the hub API would subsume it) |

## g3-deferred (17 rows: deferred_by_plan 17)

| row_id | ID | Status | Milestone | Size | Requirement | Evidence on main | Gap | In prior plan |
|---|---|---|---|---|---|---|---|---|
| R0051 | AT-051 | deferred_by_plan | M8 | L | Channel replay: authenticated sender plus exact action/revision; duplicate messages do not duplicate commands (BF3-026). | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |
| R0128 | BF3-026-AC01 | deferred_by_plan | M8 | L | Verify Slack signatures/replay and mapped actor identity. | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |
| R0129 | BF3-026-AC02 | deferred_by_plan | M8 | L | No Slack-specific queue or approval bypass. | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |
| R0130 | BF3-026-AC03 | deferred_by_plan | M8 | L | Sensitive required approvals use the designated human path and exact subject. | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |
| R0131 | BF3-027-AC01 | deferred_by_plan | M8 | L | Unsupported Grok Bot/SMS APIs remain unsupported, not silently browser-automated. | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |
| R0132 | BF3-027-AC02 | deferred_by_plan | M8 | L | SMS defaults to opt-in notifications and authenticated links, not generic yes approvals. | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |
| R0133 | BF3-027-AC03 | deferred_by_plan | M8 | L | Same command and disclosure restrictions as CLI/web for thin clients. | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |
| R0134 | BF3-028-AC01 | deferred_by_plan | M8 | L | OpenJarvis engine boundary: compile/dependency/cancel/stream evidence collected in the actual environment. | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |
| R0135 | BF3-028-AC02 | deferred_by_plan | M8 | L | No broad Python/desktop/training/scheduler dependency adopted implicitly. | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |
| R0136 | BF3-028-AC03 | deferred_by_plan | M8 | L | Local quality, continued work after timeout and total cost measured before routing promotion. | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |
| R0137 | BF3-029-AC01 | deferred_by_plan | M8 | L | Architecture replacement/federation trial: one explicit hypothesis and experiment budget. | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |
| R0138 | BF3-029-AC02 | deferred_by_plan | M8 | L | Disjoint ownership does not imply atomic cross-repo behavior or global quota enforcement. | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |
| R0139 | BF3-029-AC03 | deferred_by_plan | M8 | L | A candidate architecture retains authority/evidence and exports; absent value ends the experiment. | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |
| R0140 | BF3-030-AC01 | deferred_by_plan | M8 | L | One targeted forge-native change with maintainer and conformance receipts. | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |
| R0141 | BF3-030-AC02 | deferred_by_plan | M8 | L | Ordinary Git/PR compatibility and canonical merge authority preserved. | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |
| R0142 | BF3-030-AC03 | deferred_by_plan | M8 | L | No new Git engine or full-platform rewrite follows solely from source access. | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |
| R0165 | HF15 | deferred_by_plan | M8 | L | Local engine timeout with continued server work: record unknown/continuing resource use and block unsafe budget release (BF3-028). | none | G3 package (spec §24.2); decide implement / reject with reason / no-need with evidence after BF3-022. | closure-plan.md §G3 |

## Obligations the first mapping pass missed (now rowed above where canonical; prose ones listed here)

| ID | Requirement | Size |
|---|---|---|
| BF3-007 (AC01-03, AT-012) | Adapter seam and deterministic fakes: probe/start_fresh/read_events/cancel/collect_result contract (§12.1), result-v3 payload (§12.1a), malformed/truncated frame negatives, distinct admission/end/output/cleanup events, no-network fixture proof. It is a hard dependency of BF3-008 and BF3-009, both of which the control-plane area lists. | M |
| BF3-008 (AC01-03, AT-013, AT-014, HF06, HF09, CF01) | Isolated workspaces and physical execution tracking: private Git/filesystem/process/network boundary, exact incarnation cleanup, survivor detection, honest occupancy under failed stop. HF07/HF08/CF01 in the received rows presuppose it. | L |
| BF3-010 (AC01-03, AT-026, HF02-HF05) | Certify one Codex CLI exec --json subscription profile in a qualified Linux OCI runner; fresh-session proof, cancellation/auth/quota/nested-usage fixtures, clean-room fallback. Live-gated by HOLD and the sandbox host blocker. | L |
| BF3-011 (AC01-03, AT-021, AT-030, AT-031, AT-033, AT-034, AT-044, HF11, CF02) | Seal candidates and run stage-specific independent checks (§15, §8.6 receipt rules): verifier identity/generation, discovery/assertion validation, prepublication vs CI-only distinction, forged-output and partial-artifact rejection. | L |
| BF3-012 (AC01-03, AT-015, AT-023, AT-024, AT-035-037) | Credential-separated safe import, durable effect intents (§16, §16.1), stable draft PR identity, lost-response/ambiguity/revocation/human-edit reconciliation, request_merge command kind. | L |
| BF3-013 (AC01-03) | First useful developer loop: bf demo --fixture basic and --fixture interrupted_publish (§24.1), then one installed browser-driven live task with interruption/restart/lost-publish recovery and recorded versions/costs/limits. | M |
| BF3-014 (AC01-03, AT-040) + §14.4 | Owner lanes, fair rotation cursor, tunable limits (4 team writers / 2 per owner / 2 verifier slots / 1 integration path / 8 pending PRs per repo), overlap reasons, external PR reconciliation. §14.4 belongs to control-plane and may be in the truncated tail. | M |
| BF3-015 (AC01-03, AT-038, AT-039) + §17 | Human takeover, return, submit-human and respecification: fence old writer, checkpoint disclosure, identical independent checks, allowance preserved across revisions, take/return command kinds. | M |
| BF3-016 (AC01-03, AT-027) | Certify a second coding-provider family through the same job/isolation/accounting contract; cross-provider checkpoint restart; no identity evasion on auth/quota failure. | L |
| BF3-017 (AC01-03, AT-025, HF12) + §10, §10.1, §10.2 | Foreman planning as a mission job, Fast/Standard/Deep bounded modes, plan-acceptance CAS, fresh task packets, scoped memory with no automatic audience widening. | L |
| BF3-018 (AC01-03, HF19) + §20.4 | Single React workbench on the shared typed API: Mine/Domain/Team filters, decision cards, paginated projections, bounded SSE with reconnect snapshot, slow-client bounds, zero-model monitoring, accessibility, TUI/CLI moved onto the same authority. | L |
| BF3-019 (AC01-03, AT-041, AT-042, CF04) + §15.3, §15.4 | Native integration on the current combined subject, assembled mission outcomes, known-revert invalidation of prerequisites, publication distinct from merge. | M |
| BF3-020 (AC01-03, AT-028, AT-029, AT-043, AT-047, HF10, CF08) + §17.2, §18.1 | Complete cost/window/route reporting: inclusive-root vs disjoint-leaf basis, late child usage attribution, attention windows and drain, advisory stall vs hard limits, human-time labelling. | M |
| BF3-021 (AC01-03, AT-045, AT-046, HF20, CF07) + §16.3, §16.4, §23.1, §23.2 | Consistent backup, paused disaster restore with publisher quiescence and authority rotation, retention/cleanup by exact identity, portable export/import, legacy digest-scheme import (CF07 is referenced by BF3-002-AC03 but its importer is unrowed). | M |
| BF3-022 (AC01-03, AT-052) + §7.2 + plan §4 performance workload | Two-owner/two-runner fault campaign, frozen release-build performance workload (10k tasks, 4 runners, 20 reads/s, 10 mutations/s, p95 targets, RSS), independent fresh-agent rebuild from the kit alone. | L |
| BF3-023..BF3-030 (G3; AT-048, AT-049, AT-050, AT-051, HF13, HF14, HF15, HF18) | Deferred packages: shadow evaluation/profile promotion (§22), Grok Build executor, one production pipeline (§21), signed Slack thin client, Grok Bot/SMS validation, OpenJarvis boundary (§19.3), replacement/federation test, one forge-native improvement. Each needs an implemented / rejected / no-need decision with evidence. | L |
| §12.2 / §12.3 | Three separate state dimensions (lifecycle, writing authority, occupancy) and the task state machine with orthogonal hold_reason (auth_required, dependency_invalid, budget_unavailable, review_required, resource_conflict, check_missing, outcome_unknown) and the separate publication attribute. Referenced by several control-plane rows but never listed as an obligation itself. | M |
| §16.2 | All-job restart behavior: acquire instance lock, inspect retained authority and unsettled effects, reconnect runner incarnations, fence expired work, reconcile before conflicting writes; reused PID/pathname is not identity. Only AT-008's 'across restart' touches it. | M |
| §20.1 CLI contract | Required verbs bf init, serve --local, connect, runner enroll/start, submit, plan, run --goal, chat, status --mine/--team, why, take, submit-human, return, revise, decide, pause, stop <mission>, cancel, report --window, export. Rows mention only `bf init`, the runner role and `bf serve --local`; src/main.rs:28-115 implements none of the listed verbs except doctor/serve (and stop is a pid-killer, not the mission stop). | L |
| §24.1 scripts/check scope + bf demo fixtures | scripts/check must cover schema fixtures (it does not: scripts/check:11-18 runs only web typecheck/test, build-web --check, fmt, clippy, cargo test, build) and the product must provide bf demo --fixture basic / interrupted_publish with deterministic receipts and no network. | S |
| §7.1 minimum process separation | Hub never executes candidate code, builds, tests or hooks; tool-using planners/reviewers are runner jobs; candidate execution has no hub/forge/check/signing credentials; trusted reporter outside the candidate process. Nothing rows this as a testable obligation (the proof-command runner in src/board.rs runs arbitrary commands in the CLI process today). | M |

## Audit snapshot (taken 2026-09-18T20:35Z, before bulletfarm#2 merged; kept as dated provenance, not current state)

Host blockers verified on xbabe2 at that time:

- No PR exists to merge: codex/complete-gap-plan is local-only (git branch -r shows only origin/main, origin/archive/history, origin/codex/consolidate-repository); it is 3 docs-only commits ahead of origin/main 1ea7aa60 and moved from f0048725 to c9dc7c60 while this audit ran, so another agent is writing to the same checkout — claim via `bf claim` before touching it.
- origin/codex/consolidate-repository is an unmerged remote product branch with no open PR; closure-plan §6.3 requires none. gh pr list and gh issue list on neverhuman/bulletfarm are both empty.
- GitHub token scopes are gist, read:org, repo only: no `workflow` scope (cannot push .github/workflows changes with the token; must push over SSH with ~/.ssh/id_ed25519_github_neverhuman_publish as AGENTS.md:49 says) and no `delete_repo` scope (closure-plan step C.5 retirement of bf/bullet-farm/bullet-kernel/bullet-git/bullet-portal needs operator-assisted auth).
- One shared GitHub identity `neverhuman` for every agent and vendor: 'different-vendor review' can only be a comment, never a native GitHub approval; main protection requires only the strict `check` status context, so process review is unenforced by the forge.
- Sandbox qualification (BF3-008/010, Stage 5) is blocked on this host: /proc/sys/kernel/apparmor_restrict_unprivileged_userns=1; `bwrap --unshare-user --unshare-pid` fails 'setting up uid map: Permission denied' and `bwrap --unshare-all` fails 'loopback: Failed RTM_NEWADDR: Operation not permitted' (bubblewrap 0.9.0). Docker 29.2.1 daemon is reachable (user in docker group) but is rootful and shared with a live container (veox-itest-db postgres, 127.0.0.1:5434) that must not be disturbed; using it as the OCI runner needs an explicit decision and an AppArmor/privilege change or a different Linux host.
- scripts/check exits 64 on the host shell: node v26.1.0 / npm 11.13.0 are active while web/.nvmrc pins 22.23.2 and check pins npm 10.9.8. ~/.nvm has v22.23.2 installed; every local proof must run under `nvm use 22.23.2` with npm 10.9.8 installed into it.
- Operating HOLD (AGENTS.md): no live provider calls, grants or enrollments may be created by agents. BF3-010, BF3-013 live pilot, Stage 7 dogfood, AT-026/AT-027 and every G1 exit that needs a real Codex run wait on the human-controlled enrollment/grant ceremony.
- Codex CLI on host is 0.155.0 (plan §Stage 5 audited 0.154.0); the pinned qualified binary must name 0.155.0 or whatever is actually tested. Other CLIs present: claude 2.1.277, cursor-agent 2026.09.10, grok 1.0.34.
- Secondary gh host 127.0.0.1:8787 (account jeryu) has an invalid token; harmless, but per plan §4 Jeryu credentials confer no GitHub authority and must not be used as a fallback.
- Migrating bf.sqlite into the hub (§7-d, M3) touches ~/.bf shared by every agent on this host; it requires quiescing all other agents' claims/heartbeats first, and the generated AGENT_CHAT.md symlink cutover must be preserved (AGENTS.md:31).
- Verified non-blocker: libsqlite3-sys 0.38.2 bundles SQLite 3.53.2 with source id 2026-06-03 19:12:13 d6e03d8c…, matching src/storage.rs:80-82, and the full Rust suite passes offline at this head.
