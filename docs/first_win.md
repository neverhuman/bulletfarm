# First win

1. `cargo run --locked --bin bf -- demo --fixture basic`
2. Read the receipt. `fake: true` is required. A fixture PASS is not live certification.
3. `cargo run --locked --bin bf` and press **Try the demo**.
4. A live executor stays dark until you supply a grant. `bf doctor` names the gap. Agents never write keys.
