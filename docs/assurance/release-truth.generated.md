<!-- Generated by: bullet-family check release --report --portable -->
<!-- Source: src/check/prerequisites.rs + src/check/truth/rows.rs + family.lock + repos.manifest.toml + docs/release.md + docs/assurance/product-gaps.md -->
<!-- Command: just release-truth -->
<!-- DO NOT EDIT BY HAND. -->

# Release truth

RELEASE DECISION: BLOCKED

| Field | Value |
| --- | --- |
| as_of | hub HEAD committer date not read; `docs/release.md` Last reviewed: 2026-08-25; no wall clock was read |
| source | `bullet-family 0.1.0` static release inventory, check report schema 2, tier RELEASE, 26 gates |
| scope | the 26 registered release gates of the four-repository Bullet Farm family, each mapped to its product gap G-id from `docs/assurance/product-gaps.md`; gap G12 is this inventory itself; nothing outside the catalog is counted |
| freshness | every input is identified by digest in the Freshness section; a gate row changes only when its catalog entry, its claim row, the register crosswalk, or a receipt changes |
| projection | PROJECTION — rendered from machine facts; this page holds no release authority |
| variant | PORTABLE — machine-local subjects, absolute paths, and mtimes are excluded; reproducible from a fresh hub-only checkout |

## Repository subjects

| Repository | Commit | Tree | Checkout |
| --- | --- | --- | --- |
| bullet-farm | UNKNOWN | UNKNOWN | not read (portable variant) |
| bullet-kernel | UNKNOWN | UNKNOWN | not read (portable variant) |
| bullet-git | UNKNOWN | UNKNOWN | not read (portable variant) |
| bullet-portal | UNKNOWN | UNKNOWN | not read (portable variant) |

## Family lock

| Field | Value |
| --- | --- |
| schema | 2 (diagnostic-only; every installer path refuses it by design) |
| tag | `v0.1.0-alpha.4` |
| binds current HEADs | UNKNOWN (portable variant does not read checkouts) |

## Five truths, never conflated

| Truth | Status | Source | Scope |
| --- | --- | --- | --- |
| Mechanical gates (fast) | NOT RUN (portable variant does not read machine-local check reports) | `.bullet-family/check-fast.json` written by `bullet-family check fast --json` | fixed fast command catalog; a pass here is not release readiness |
| Mechanical gates (required) | NOT RUN (portable variant does not read machine-local check reports) | `.bullet-family/check-required.json` written by `bullet-family check required --json` | fixed required command catalog; a pass here is not release readiness |
| Evidence completeness | 0 of 26 receipted | static negative inventory in `src/check/prerequisites.rs`; only `release.rust-msrv-1-95` admits an external receipt | the 26 registered release gates |
| Release review | HOLD — no receipt exists to review | this page; no reviewer record exists | human verification of receipted gates |
| Deployment match | N/A — pre-release; nothing is deployed | no deployment record exists | deployed SHA versus reviewed integration subject |
| Post-deploy survival | NOT ESTABLISHED | no deployment observation exists | required observations after a deployment |

## Gates (26 of 26 unproved)

1. **Nobody has restored a tagged backup and read the restored state back as an exact-subject release receipt.** — `release.backup-restore` BLOCKED
   - Product gap: G3, G9
   - Why it matters: A backup whose restore has never been observed protects nothing; the pointed-at bytes, not the database, are the recovery subject.
   - Acceptance: Run the tagged backup/restore suite from release bytes and register a signed receipt binding backup digest, restore epoch, and the exact four-repository subjects.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — no exact-subject backup and restore receipt is registered; exists but does not count: Kernel `798f0c8` `farm backup|restore` with an exact receipt and quarantined offline restore (COMPONENT_PROOF); restored state is not admitted for production use
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the backup/restore suite and its receipt shape are Kernel code (V1-S2); external predecessor: release bytes from signed tagged subjects (G9) and production restore admission (G3)
   - Next command: `cd ../bullet-kernel && cargo run --locked -p bullet -- farm backup --database /abs/ledger.sqlite --output /abs/snapshot.sqlite --receipt /abs/backup-receipt.json` — component backup with an exact receipt; `farm restore --backup … --receipt … --destination …` restores into quarantine only, and neither run is a tagged-bytes release receipt
   - Release-blocking: yes
2. **No checksum set exists for any release archive, because no release archive exists.** — `release.checksums` BLOCKED
   - Product gap: G9
   - Why it matters: Without checksums an installer cannot tell substituted bytes from released bytes.
   - Acceptance: Generate and re-read checksums for every exact archive in the five-target matrix and bind them in the signed release manifest.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — release archive checksums are absent; exists but does not count: hub `release verify` re-reads checksums only inside an already signed bundle (`352f963`, `ba09056`); no archive or package builder
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: a package builder and checksum generation over its archives are hub engineering (V1-S7); external predecessor: the five archives on macOS/Windows build platforms and the signed manifest that binds the checksums
   - Next command: NONE — no typed command exists yet; no package builder exists; `bullet-family release verify` only re-reads checksums inside an assembled signed bundle
   - Release-blocking: yes
3. **Nobody has crashed the tagged family at every SQLite/WAL/CAS/journal/generation boundary and read back either the prior or the whole next state.** — `release.fault-suite` BLOCKED
   - Product gap: G2, G3
   - Why it matters: Crash safety that was only reasoned about is not crash safety; one unobserved boundary can lose or duplicate an effect.
   - Acceptance: Run the tagged crash-boundary and recovery fault suite from release bytes and register its exact signed receipt.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the crash-boundary and recovery fault receipt is absent; exists but does not count: Kernel atomic lease/command/outbox, injected setup transaction boundaries (hub `94b6549`), and BulletGit prior-or-whole-next recovery (`274fd6d`, `f551736`) as COMPONENT_PROOF
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the crash-boundary and recovery fault suite is Kernel/BulletGit engineering (V1-S2/S3); external predecessor: the tagged release bytes the suite must run from
   - Next command: NONE — no typed command exists yet; `just model-check` covers only the two bounded formal protocols (lease/fence/reclaim, command/effect ambiguity), not the crash-boundary suite
   - Release-blocking: yes
4. **No GitHub App has integrated an exact Candidate into a protected test repository and read the result back.** — `release.forge.github-app` BLOCKED
   - Product gap: G7
   - Why it matters: GitHub is an effect adapter; until a lost response reconciles to UNKNOWN and read-back adopts the original OID, every GitHub effect is unproved.
   - Acceptance: Configure the GitHub App test repository with branch protection and register exact-subject dispatch, check, integration, read-back, and reconciliation receipts.
   - Evidence class: LIVE_PROOF
   - Current evidence: NONE — no protected GitHub App integration and reconciliation receipt is registered; exists but does not count: a specified, uncertified effect adapter (ADR 0002, ADR 0008); no App, test repository, or credential
   - Owner: EXTERNAL (needs operator credential, signer, service, or platform) — operator configures a GitHub App and a branch-protected test repository with separate delivery and attestation credentials
   - Next command: NONE — no typed command exists yet; no typed GitHub App command exists
   - Release-blocking: yes
5. **No protected Jeryu integration has run with restored operator authentication and produced a read-back receipt.** — `release.forge.jeryu` BLOCKED
   - Product gap: G6
   - Why it matters: Jeryu is the source forge; an integration that never read its own result back cannot claim protected-ref safety.
   - Acceptance: With operator-restored authentication run read-only probes, then one exact protected integration with UNKNOWN/read-back reconciliation, and register the receipt; never modify the running forge.
   - Evidence class: LIVE_PROOF
   - Current evidence: NONE — no protected Jeryu integration and reconciliation receipt is registered; exists but does not count: a local bare-forge component and a typed-quarantine Jeryu adapter; no authenticated read-back
   - Owner: EXTERNAL (needs operator credential, signer, service, or platform) — operator restores scoped authentication on the unmodified local forge and names a test repository for read-back
   - Next command: NONE — no typed command exists yet; the read-only probes that come first have no typed command yet; never modify the running forge
   - Release-blocking: yes
6. **The checked-in family.lock is still schema 2, which every installer path rejects by design.** — `release.installable-lock` BLOCKED
   - Product gap: G1
   - Why it matters: Installation from the hub cannot start until a lock carries authenticated Jeryu sources and signed exact subjects.
   - Acceptance: Publish immutable signed member tags, generate the schema-3 lock with Jeryu URL/slug, exact tree OIDs, lockfiles, and artifact digests, and commit it under a signed hub tag.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the checked-in family lock is not an installable signed schema-3 release lock; exists but does not count: schema-3 generation and strict verification code (`bullet-family lock generate|verify`), descriptor-relative setup, and the honest schema-2 refusal (`doctor` reports `UNSUPPORTED_SCHEMA`)
   - Owner: EXTERNAL (needs operator credential, signer, service, or platform) — operator publishes signed immutable member tags with an authenticated Jeryu URL/slug; the schema-3 lock can only be generated from those
   - Next command: `bullet-family lock generate --tag <signed-hub-tag>` — generates the schema-3 lock only from signed tagged members with authenticated sources; until they exist `bullet-family doctor --json` reports the schema-2 refusal, and that refusal is correct
   - Release-blocking: yes
7. **Nobody has run the signed prebuilt installer twice in a fresh HOME from tagged hub-only bytes.** — `release.installer-twice` BLOCKED
   - Product gap: G1
   - Why it matters: A source bootstrap that launches Cargo before admission is not an installer receipt; idempotence and exact clean member OIDs must be observed, not assumed.
   - Acceptance: From a signed prebuilt bullet-family binary and tagged bytes, run setup twice in a fresh HOME and register exact clean member OIDs, zero tracked changes, and no worktrees.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — no two-run fresh-HOME installer receipt from tagged hub-only bytes is registered; exists but does not count: two-run component fixture with local source transport and sealed Linux Cargo/Node/Bash/npm subjects (hub `94b6549`, `7efe2f3`); `scripts/setup.sh` stays a pre-admission source wrapper
   - Owner: EXTERNAL (needs operator credential, signer, service, or platform) — a signed prebuilt `bullet-family` from tagged bytes and a fresh-HOME replay through production Jeryu transport
   - Next command: NONE — no typed command exists yet; `scripts/setup.sh` launches Cargo before admission; running it twice is not the installer receipt
   - Release-blocking: yes
8. **No pinned Jankurai report reads 90 or better with zero caps and zero hard findings for the exact release subjects.** — `release.jankurai-90` BLOCKED
   - Product gap: G8
   - Why it matters: Hard findings and caps are release blockers; a machine-local or skip-green audit lane cannot substitute for the pinned receipt.
   - Acceptance: Run pinned Jankurai 1.6.11 with zero skips against the exact tagged subjects and register the score/cap/hard-finding receipt at 90 or better with zero caps and zero hard findings.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — Jankurai release evidence at score 90 with zero hard findings and caps is absent; exists but does not count: pinned local jankurai 1.6.11 lane that fails closed below its floor and ratchets upward only (`just audit`); no hosted checksum-pinned artifact
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: reduce hard findings and caps to zero and raise the score to 90 with the pinned local binary; external predecessor: a checksum-pinned hosted CI Jankurai artifact and the exact tagged subjects
   - Next command: `just audit` — pinned local lane with an upward-only floor; a local binary is not the CI artifact and this checkout is not the tagged subject
   - Release-blocking: yes
9. **No signed final release manifest binds the hub tag without embedding its own digest.** — `release.manifest-non-circular` BLOCKED
   - Product gap: G9
   - Why it matters: A manifest that includes its own digest cannot be checked; a manifest without a signature cannot be trusted.
   - Acceptance: Generate the final manifest binding hub tag, schema-3 lock, five byte-sorted target entries, and archive/SBOM/provenance digests, then sign it with the protected release key.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the final non-circular signed release manifest is absent; exists but does not count: hub `release verify` checks an exact non-circular signed five-target manifest against a preassembled fixture (`352f963`); no builder or signer
   - Owner: EXTERNAL (needs operator credential, signer, service, or platform) — the protected release signing key and its signer identity
   - Next command: `bullet-family release verify --bundle /abs/bundle --allowed-signers /abs/allowed_signers` — read-only verifier of an already assembled bundle; it re-reads a manifest and cannot produce or sign one
   - Release-blocking: yes
10. **None of the five release archives (Linux x86_64/aarch64, macOS x86_64/arm64, Windows x64) has been built with the Portal embedded.** — `release.package-matrix` BLOCKED
   - Product gap: G9
   - Why it matters: A release is bytes on every declared platform; the Vite preview lane and a separately built farmd are not package evidence.
   - Acceptance: Build and smoke all five archives from the exact tagged family with the Portal embedded in the Rust distribution and register each archive digest.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the five required platform archives with the embedded Portal are absent; exists but does not count: Portal `8272844` deterministic clean-commit bundle manifest and a separately built farmd; no archive, embedding, or package builder
   - Owner: EXTERNAL (needs operator credential, signer, service, or platform) — macOS x86_64/arm64 and Windows x64 build platforms plus the embedded Portal build; Linux alone is not the matrix
   - Next command: NONE — no typed command exists yet; no package builder or Rust embedding of the Portal bundle exists
   - Release-blocking: yes
11. **Only Linux containment has been observed live; no packaged non-Linux platform has been seen refusing real mutation.** — `release.platform-containment` BLOCKED
   - Product gap: G10
   - Why it matters: A platform without an equivalent containment backend must fail closed, and that refusal is itself a receipt to produce.
   - Acceptance: Register the Linux production containment receipt plus a fail-closed mutation-refusal receipt on every other packaged platform.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — platform containment or fail-closed refusal receipts are absent; exists but does not count: Linux user+net namespace, slirp4netns, nftables, and CONNECT-proxy isolation proofs (Kernel `d388733`, `ops/ci/egress.sh`); no non-Linux backend or refusal receipt
   - Owner: EXTERNAL (needs operator credential, signer, service, or platform) — native macOS/Windows containment backends or their fail-closed refusal receipts on packaged bytes
   - Next command: `cd ../bullet-kernel && bash ops/ci/egress.sh` — Linux half only: 3/3 isolation proofs on a host with unprivileged namespaces (exit 78 neutral otherwise); no non-Linux refusal receipt exists
   - Release-blocking: yes
12. **No signed build provenance statement exists for any archive.** — `release.provenance` BLOCKED
   - Product gap: G9
   - Why it matters: Without provenance a consumer cannot connect archive bytes to the hub tag, lock, and toolchains that produced them.
   - Acceptance: Produce provenance bound to the exact hub tag, lock, toolchains, and archive digests, sign it, and re-read it with the release verifier.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — signed build provenance for the package matrix is absent; exists but does not count: hub `release verify` re-reads provenance bytes inside a signed bundle; no provenance producer
   - Owner: EXTERNAL (needs operator credential, signer, service, or platform) — signed provenance needs the protected release key and archive digests from the package matrix
   - Next command: `bullet-family release verify --bundle /abs/bundle --allowed-signers /abs/allowed_signers` — re-reads provenance bytes inside an assembled signed bundle only
   - Release-blocking: yes
13. **No admitted Antigravity binary has produced a sealed PONG receipt through the live-conformance path; the path exists (Kernel `ba485d5`, `b4735da`, `0d848f6`; `docs/runbooks/live-conformance.md`) but every run so far refused at POLICY_LIVE_ADMISSION_DISABLED under the checked-in v1alpha1 policy, so the offline structured-output subset and the zero-spawn neutral refusal are the only Antigravity evidence.** — `release.provider.antigravity` BLOCKED
   - Product gap: G5
   - Why it matters: The bounded offline parser and the zero-spawn neutral refusal prove nothing about the live adapter, its credentials, quota, isolation, or patch output; only a sealed PONG receipt under an operator-ratified policy is LIVE_PROOF for this adapter.
   - Acceptance: After the operator act in `docs/runbooks/live-conformance.md` (authority keygen, a ratified v1alpha2 policy with generation 2 and an active provider-runner key, the `AGENT_CHAT.md` ratification line), run the real-mode lane and register the sealed exact adapter/version/profile receipt covering isolation, failure, quota, and patch conformance.
   - Evidence class: LIVE_PROOF
   - Current evidence: NONE — Antigravity release conformance evidence is absent; exists but does not count: Kernel `5badc85` bounded structured-output subset; the common policy→key→lease→admission→grant→egress→read-only-turn→canary→receipt path with the v1alpha2 loader mirror (`0d848f6`); every run refuses before spawn under the checked-in policy
   - Owner: EXTERNAL (needs operator credential, signer, service, or platform) — operator ratifies policy generation 2 with a provider-runner key and supplies the real profile and credentials; the Kernel loader already accepts v1alpha2 (`0d848f6`), so ratification is the only policy blocker and no agent flips `live_admission_enabled`
   - Next command: `cd ../bullet-kernel && BULLET_LIVE_REAL=1 BULLET_POLICY_PATH=/abs/bullet-data/policy/policy.json BULLET_LIVE_PROVIDERS=agy bash ops/ci/nightly.sh` — real mode refuses to start without an absolute operator-ratified policy path; under the checked-in v1alpha1 policy the lane exits 78 (neutral refusal) and spawns nothing
   - Release-blocking: yes
14. **No admitted Claude binary has produced a sealed PONG receipt through the live-conformance path; the path exists (Kernel `ba485d5`, `b4735da`, `0d848f6`; `docs/runbooks/live-conformance.md`) but every run so far refused at POLICY_LIVE_ADMISSION_DISABLED under the checked-in v1alpha1 policy, so the offline stream-JSON subset, the deep fake-process proof, and the zero-spawn neutral refusal are the only Claude evidence.** — `release.provider.claude` BLOCKED
   - Product gap: G5
   - Why it matters: The bounded offline parser and the zero-spawn neutral refusal prove nothing about the live adapter, its credentials, quota, isolation, or patch output; only a sealed PONG receipt under an operator-ratified policy is LIVE_PROOF for this adapter.
   - Acceptance: After the operator act in `docs/runbooks/live-conformance.md` (authority keygen, a ratified v1alpha2 policy with generation 2 and an active provider-runner key, the `AGENT_CHAT.md` ratification line), run the real-mode lane and register the sealed exact adapter/version/profile receipt covering isolation, failure, quota, and patch conformance.
   - Evidence class: LIVE_PROOF
   - Current evidence: NONE — Claude release conformance evidence is absent; exists but does not count: Kernel `c34d578` bounded stream-JSON subset and the only deep positive fake-process proof; the common policy→key→lease→admission→grant→egress→read-only-turn→canary→receipt path with the v1alpha2 loader mirror (`0d848f6`); every run refuses before spawn under the checked-in policy
   - Owner: EXTERNAL (needs operator credential, signer, service, or platform) — operator ratifies policy generation 2 with a provider-runner key and supplies the real profile and credentials; the Kernel loader already accepts v1alpha2 (`0d848f6`), so ratification is the only policy blocker and no agent flips `live_admission_enabled`
   - Next command: `cd ../bullet-kernel && BULLET_LIVE_REAL=1 BULLET_POLICY_PATH=/abs/bullet-data/policy/policy.json BULLET_LIVE_PROVIDERS=claude bash ops/ci/nightly.sh` — real mode refuses to start without an absolute operator-ratified policy path; under the checked-in v1alpha1 policy the lane exits 78 (neutral refusal) and spawns nothing
   - Release-blocking: yes
15. **No admitted Codex binary has produced a sealed PONG receipt through the live-conformance path; the path exists (Kernel `ba485d5`, `b4735da`, `0d848f6`; `docs/runbooks/live-conformance.md`) but every run so far refused at POLICY_LIVE_ADMISSION_DISABLED under the checked-in v1alpha1 policy, so the offline App Server JSONL subset and the zero-spawn neutral refusal are the only Codex evidence.** — `release.provider.codex` BLOCKED
   - Product gap: G5
   - Why it matters: The bounded offline parser and the zero-spawn neutral refusal prove nothing about the live adapter, its credentials, quota, isolation, or patch output; only a sealed PONG receipt under an operator-ratified policy is LIVE_PROOF for this adapter.
   - Acceptance: After the operator act in `docs/runbooks/live-conformance.md` (authority keygen, a ratified v1alpha2 policy with generation 2 and an active provider-runner key, the `AGENT_CHAT.md` ratification line), run the real-mode lane and register the sealed exact adapter/version/profile receipt covering isolation, failure, quota, and patch conformance.
   - Evidence class: LIVE_PROOF
   - Current evidence: NONE — Codex release conformance evidence is absent; exists but does not count: Kernel `ca376e4` bounded App Server JSONL subset; the common policy→key→lease→admission→grant→egress→read-only-turn→canary→receipt path with the v1alpha2 loader mirror (`0d848f6`); every run refuses before spawn under the checked-in policy
   - Owner: EXTERNAL (needs operator credential, signer, service, or platform) — operator ratifies policy generation 2 with a provider-runner key and supplies the real profile and credentials; the Kernel loader already accepts v1alpha2 (`0d848f6`), so ratification is the only policy blocker and no agent flips `live_admission_enabled`
   - Next command: `cd ../bullet-kernel && BULLET_LIVE_REAL=1 BULLET_POLICY_PATH=/abs/bullet-data/policy/policy.json BULLET_LIVE_PROVIDERS=codex bash ops/ci/nightly.sh` — real mode refuses to start without an absolute operator-ratified policy path; under the checked-in v1alpha1 policy the lane exits 78 (neutral refusal) and spawns nothing
   - Release-blocking: yes
16. **No admitted Cursor binary has produced a sealed PONG receipt through the live-conformance path; the path exists (Kernel `ba485d5`, `b4735da`, `0d848f6`; `docs/runbooks/live-conformance.md`) but every run so far refused at POLICY_LIVE_ADMISSION_DISABLED under the checked-in v1alpha1 policy, so the offline ACP subset and the zero-spawn neutral refusal are the only Cursor evidence.** — `release.provider.cursor` BLOCKED
   - Product gap: G5
   - Why it matters: The bounded offline parser and the zero-spawn neutral refusal prove nothing about the live adapter, its credentials, quota, isolation, or patch output; only a sealed PONG receipt under an operator-ratified policy is LIVE_PROOF for this adapter.
   - Acceptance: After the operator act in `docs/runbooks/live-conformance.md` (authority keygen, a ratified v1alpha2 policy with generation 2 and an active provider-runner key, the `AGENT_CHAT.md` ratification line), run the real-mode lane and register the sealed exact adapter/version/profile receipt covering isolation, failure, quota, and patch conformance.
   - Evidence class: LIVE_PROOF
   - Current evidence: NONE — Cursor release conformance evidence is absent; exists but does not count: Kernel `ea89929` bounded ACP subset; the common policy→key→lease→admission→grant→egress→read-only-turn→canary→receipt path with the v1alpha2 loader mirror (`0d848f6`); every run refuses before spawn under the checked-in policy
   - Owner: EXTERNAL (needs operator credential, signer, service, or platform) — operator ratifies policy generation 2 with a provider-runner key and supplies the real profile and credentials; the Kernel loader already accepts v1alpha2 (`0d848f6`), so ratification is the only policy blocker and no agent flips `live_admission_enabled`
   - Next command: `cd ../bullet-kernel && BULLET_LIVE_REAL=1 BULLET_POLICY_PATH=/abs/bullet-data/policy/policy.json BULLET_LIVE_PROVIDERS=cursor bash ops/ci/nightly.sh` — real mode refuses to start without an absolute operator-ratified policy path; under the checked-in v1alpha1 policy the lane exits 78 (neutral refusal) and spawns nothing
   - Release-blocking: yes
17. **The receipt verifier has only ever checked fixtures; no external allowed-signers policy or real signed release receipt has been provisioned.** — `release.receipt-contracts` BLOCKED
   - Product gap: G9
   - Why it matters: A verifier without a provisioned signer policy and a real receipt is a component, not release evidence.
   - Acceptance: Provision the external signer policy, trusted-time observation, kind-specific semantic verifier, and exact tagged receipts, then register their verification.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the strict receipt verifier exists, but no independently provisioned allowed-signers policy or actual signed release receipt is registered; exists but does not count: strict canonical receipt/policy verifier with exact OpenSSH signer/namespace/interval checks (hub `143f8b9`) plus the MSRV receipt-admission path (`d762f86`); only fixtures have ever been checked
   - Owner: EXTERNAL (needs operator credential, signer, service, or platform) — an independently provisioned allowed-signers policy, a trusted-time observation, and a real signed release receipt from tagged bytes
   - Next command: `bullet-family release receipt-verify --receipt /abs/receipt --signature /abs/signature --policy /abs/policy` — strict verifier that has only ever seen fixtures; a fixture pass is component evidence
   - Release-blocking: yes
18. **No admitted Rust 1.95 build-and-test receipt has been supplied through the root-owned `/etc/bullet-farm/release-msrv-1-95-admission.toml` descriptor; the admission path exists (hub `d762f86`) and nothing has been admitted through it.** — `release.rust-msrv-1-95` BLOCKED
   - Product gap: G9
   - Why it matters: MSRV is a release promise; a local toolchain match today is not a tagged-bytes build receipt, and a self-selected signer or a generic result digest cannot clear the gate.
   - Acceptance: Provision the root-owned descriptor, policy, and evidence directory with distinct source-tag, build-attestor, and trusted-time Ed25519 roots, then supply the receipt binding the schema-3 lock, clean signed subjects, dependency-lock digests, exact Rust 1.95 rustc/cargo bytes, and zero-skip build/test observations for all three Rust workspaces.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — an exact release build receipt for Rust 1.95 is absent; exists but does not count: the semantic receipt-admission path for exactly this gate (`d762f86`; absent → BLOCKED, rejected → FAIL, admitted → PASS) and local lanes that already build under rustc 1.95.0 (`rust-toolchain.toml`, `scripts/ci-doctor.sh`)
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the admission path and the 1.95 lane are hub code; every local lane already runs under rustc 1.95.0; external predecessor: a root-owned descriptor with three distinct signer roots, an independently signed time observation, and the schema-3 lock the receipt must bind
   - Next command: `bullet-family check release --json` — reads only the fixed `/etc/bullet-farm/release-msrv-1-95-admission.toml` descriptor (root-owned; environment variables and repository files cannot redirect it); this one gate becomes PASS only from an admitted receipt
   - Release-blocking: yes
19. **Nobody has built and tested the exact tagged family with pinned Rust 1.97.1 and registered the receipt.** — `release.rust-pinned-1-97-1` BLOCKED
   - Product gap: G9
   - Why it matters: The pinned toolchain is the second required build; one toolchain receipt never covers the other.
   - Acceptance: Build and test the exact tagged family with the admitted pinned Rust 1.97.1 toolchain under cargo --locked and register the receipt.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — an exact release build receipt for pinned Rust 1.97.1 is absent; exists but does not count: nothing beyond the MSRV admission pattern; `scripts/ci-doctor.sh` accepts rustc 1.95.0 only and no 1.97.1 lane or admission path exists
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: a pinned 1.97.1 lane and its admission path are ordinary hub engineering; external predecessor: the exact tagged family bytes and the same signer/time roots as the MSRV receipt
   - Next command: NONE — no typed command exists yet; no 1.97.1 lane exists; `scripts/ci-doctor.sh` refuses any rustc other than 1.95.0
   - Release-blocking: yes
20. **No software bill of materials exists for any release archive.** — `release.sbom` BLOCKED
   - Product gap: G9
   - Why it matters: Consumers cannot audit or respond to a vulnerability in bytes whose contents were never enumerated.
   - Acceptance: Generate and validate an SBOM for every exact release archive and bind each digest in the signed manifest.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — software bills of materials for the package matrix are absent; exists but does not count: hub `release verify` re-reads SBOM bytes inside a signed bundle; no archive, SBOM generator, or semantic SBOM validation
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: SBOM generation and validation over built archives; external predecessor: the five archives of the package matrix and the signed manifest that binds each SBOM digest
   - Next command: NONE — no typed command exists yet; no package builder or admitted SBOM generator exists
   - Release-blocking: yes
21. **No pinned dependency scan receipt exists for the exact release lockfiles.** — `release.scan.dependency` BLOCKED
   - Product gap: G8, G9
   - Why it matters: A scan run on a working tree at some earlier commit says nothing about the tagged lockfiles.
   - Acceptance: Run the admitted cargo-deny 0.19.8 against the exact tagged lockfiles and register its receipt.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the pinned dependency scan receipt is absent; exists but does not count: `cargo deny check bans` under the pinned cargo-deny 0.19.8 in `just security` on this checkout; advisories and tagged lockfiles are not covered
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the pinned cargo-deny lane over this checkout, extended to advisories; external predecessor: the exact tagged lockfiles of all three Rust workspaces
   - Next command: `just security` — runs gitleaks, `cargo deny check bans`, and zizmor on this checkout only; not the tagged lockfiles
   - Release-blocking: yes
22. **No pinned license policy scan receipt exists for the exact release artifacts.** — `release.scan.license` BLOCKED
   - Product gap: G8, G9
   - Why it matters: License violations discovered after tagging invalidate the release bytes.
   - Acceptance: Run the admitted license scanner against the exact archives and SBOMs and register its receipt.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the pinned license policy scan receipt is absent; exists but does not count: no license scan is admitted; `just security` runs `cargo deny check bans` only
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: a pinned license policy scan is local engineering; external predecessor: the exact tagged archives and SBOMs it must run against
   - Next command: NONE — no typed command exists yet; `just security` runs `cargo deny check bans` only; no license scanner is admitted yet
   - Release-blocking: yes
23. **No pinned secret scan receipt exists for the exact tagged trees.** — `release.scan.secret` BLOCKED
   - Product gap: G8, G9
   - Why it matters: A canary or credential in tagged bytes is unrecoverable once published.
   - Acceptance: Run the admitted gitleaks 8.21.2 against every exact tagged tree and register its receipt.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the pinned secret scan receipt is absent; exists but does not count: `gitleaks detect --source . --no-git --redact` under the pinned gitleaks 8.21.2 in `just security` on this working tree
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the pinned gitleaks lane over this working tree; external predecessor: every exact tagged tree of the four repositories
   - Next command: `just security` — gitleaks over this working tree only (`--no-git`); the tagged trees of all four repositories remain
   - Release-blocking: yes
24. **No pinned workflow policy scan receipt exists for the exact workflow bytes.** — `release.scan.workflow` BLOCKED
   - Product gap: G8, G9
   - Why it matters: Hosted workflows are a supply-chain surface; unpinned or over-permissioned steps break provenance.
   - Acceptance: Run the admitted zizmor 1.25.2 against the exact workflow bytes and register its receipt.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — the pinned workflow policy scan receipt is absent; exists but does not count: `zizmor .` under the pinned zizmor 1.25.2 in `just security` on this checkout
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: the pinned zizmor lane over this checkout; external predecessor: the exact tagged workflow bytes
   - Next command: `just security` — zizmor over this checkout's workflows only; not the tagged workflow bytes
   - Release-blocking: yes
25. **Nothing in the package matrix carries a signature from a protected release key.** — `release.signatures` BLOCKED
   - Product gap: G9
   - Why it matters: An unsigned archive, checksum set, SBOM, or manifest can be replaced by anyone who can write to the download path.
   - Acceptance: Sign every archive, checksum set, SBOM, provenance statement, and the final manifest with admitted release keys and re-read them with the release verifier.
   - Evidence class: RELEASE_PROOF
   - Current evidence: NONE — verified signatures for the package matrix are absent; exists but does not count: hub `release verify` checks detached signatures and exact Ed25519 signer status inside an assembled bundle; nothing signs
   - Owner: EXTERNAL (needs operator credential, signer, service, or platform) — protected release keys and a signer identity
   - Next command: `bullet-family release verify --bundle /abs/bundle --allowed-signers /abs/allowed_signers` — verifies detached signatures inside an assembled bundle; it signs nothing
   - Release-blocking: yes
26. **Nobody has produced a signed five-plane TRANSACTION_PROOF from `just demo`; the demo still ends in synthetic success.** — `release.transaction-demo` BLOCKED
   - Product gap: G2
   - Why it matters: The offline transaction is the baseline every live and release gate builds on; synthetic success renamed is still synthetic.
   - Acceptance: Run the non-synthetic tagged demo through real child boundaries and the protected local forge simulator, ending in one signed TRANSACTION_PROOF covering authority, runner death/salvage, independent verification, ambiguous-effect reconciliation, protected integration, preservation, and truthful projection.
   - Evidence class: TRANSACTION_PROOF
   - Current evidence: NONE — the exact offline five-plane transaction demo receipt is absent; exists but does not count: `just demo` runs the Kernel simulator demo and prints synthetic receipts; atomic lease/command/outbox, fail-closed `bullet-gitd`, the fixture E2 verifier, and Portal PENDING→UNKNOWN are COMPONENT_PROOF
   - Owner: LOCAL (closable offline) — Kernel + BulletGit + Portal engineering (V1-S4); the TRANSACTION_PROOF is credential-free and offline
   - Next command: `just demo` — today ends in synthetic success from the simulator; the receipt must replace that success, not rename it
   - Release-blocking: yes

## Product-gap crosswalk

Every G-id of `docs/assurance/product-gaps.md` is answered exactly once: by the gate rows that name it, by the ungated section below, or by this inventory. Titles are compiled; the crosswalk rows are read from the register and compared here.

| Gap | Title | Answered by |
| --- | --- | --- |
| G1 | Hub-only signed install | `release.installable-lock`, `release.installer-twice` |
| G2 | Connected five-plane transaction | `release.fault-suite`, `release.transaction-demo` |
| G3 | Production Kernel write path | `release.backup-restore`, `release.fault-suite` |
| G4 | Production BulletGit write path | ungated — see “Blocking but not a release gate” below |
| G5 | Live provider conformance | `release.provider.antigravity`, `release.provider.claude`, `release.provider.codex`, `release.provider.cursor` |
| G6 | Jeryu live effect | `release.forge.jeryu` |
| G7 | GitHub live effect | `release.forge.github-app` |
| G8 | Security release floor | `release.jankurai-90`, `release.scan.dependency`, `release.scan.license`, `release.scan.secret`, `release.scan.workflow` |
| G9 | Signed five-target release | `release.backup-restore`, `release.checksums`, `release.manifest-non-circular`, `release.package-matrix`, `release.provenance`, `release.receipt-contracts`, `release.rust-msrv-1-95`, `release.rust-pinned-1-97-1`, `release.sbom`, `release.scan.dependency`, `release.scan.license`, `release.scan.secret`, `release.scan.workflow`, `release.signatures` |
| G10 | Non-Linux containment | `release.platform-containment` |
| G11 | Evolutionary runtime | ungated — see “Blocking but not a release gate” below |
| G12 | Family `check release` | this inventory — 26 gates, 0 receipted (`bullet-family check release --json`) |
| G13 | Portal product surfaces | ungated — see “Blocking but not a release gate” below |
| G14 | farmd production API | ungated — see “Blocking but not a release gate” below |
| G15 | Cognitive persistence | ungated — see “Blocking but not a release gate” below |

Agreement with `docs/assurance/product-gaps.md`: YES — all 26 crosswalk rows and the G-id list agree

## Blocking but not a release gate

Product gaps with no `release.*` id. Each row says which gate it blocks through; none can be receipted here, and each is listed so that no G-id is invisible to machine output.

1. **Public BulletGit `clone` still answers AUTHORITY_CONTRACT_UNAVAILABLE, and no immutable `bullet-wire` tag has been published for consumers to pin.** — G4 Production BulletGit write path
   - Why it matters: Without positive online authority and a published wire tag no Candidate can be written by the production path, so the five-plane transaction (G2) cannot start.
   - Acceptance: Publish the immutable `bullet-wire` tag, land Kernel online reservation/settlement and BulletGit positive online authority, and read one exact Candidate back through the public path inside the G2 transaction.
   - Evidence class: TRANSACTION_PROOF
   - Current evidence: COMPONENT only — dissociate clone, hostile-git, generations, preservation, and honest cleanup UNKNOWN (BulletGit `236f4ef`); no positive online authority and no published wire tag
   - Owner: LOCAL-then-EXTERNAL (closable offline only up to an external predecessor) — offline part: Kernel online reservation/settlement and BulletGit positive authority (V1-S3); external predecessor: operator publishes the immutable `bullet-wire` tag
   - Next command: NONE — no typed command exists yet; `just contract` proves generated wire identity, not a published tag
   - Release-blocking: yes — through G2 (`release.transaction-demo`); no receipt here can count on its own
2. **Self-tuning optimization and evolutionary campaigns are explicitly post-V1; `evolutionary_authority=false` is the committed V1 policy.** — G11 Evolutionary runtime
   - Why it matters: The frozen V1 scope requires the minimum typed cognitive plane, not an authority-bearing evolutionary runtime. Treating a preview diagnostic as a GA gate would fork that contract.
   - Acceptance: Keep evolutionary authority disabled through V1. A later release may implement recipes, archive, study, canary, and promotion behind the evolutionary-control contract; the policy bit changes last, by operator ratification.
   - Evidence class: COMPONENT_PROOF
   - Current evidence: design only — `docs/architecture/evolutionary-control.md` and policy `route_policy.evolutionary_authority=false`; no recipe, archive, study, canary, or promotion code path
   - Owner: LOCAL (closable offline) — post-V1 evolutionary-control engineering; never by flipping the policy bit first
   - Next command: NONE — no typed command exists yet; do not start a V1 campaign; the typed durable study and canary surfaces do not exist
   - Release-blocking: no for V1 — self-tuning optimization is post-V1; `linux-preview` surfaces an extra evolution diagnostic that cannot alter the canonical 26-gate GA contract
3. **Six of fifteen Portal spec surfaces (Cognitive Router, Fusion Lab, Quota/Capacity, Struggle, Behavior, Workspace Hygiene) have no durable ledger subject and render explicit UNKNOWN, and the Portal is not packaged or embedded.** — G13 Portal product surfaces
   - Why it matters: A projection without a ledger subject cannot be truthful, and a Portal served from a Vite preview is not the released product surface.
   - Acceptance: After G2/G3, add the missing ledger subjects, project all fifteen surfaces with watermark-bound truth, and embed the built Portal in the Rust distribution.
   - Evidence class: TRANSACTION_PROOF
   - Current evidence: COMPONENT only — the projected surfaces (Portal `95108e3`: unit, mocked-browser, and real-farmd lanes), CSRF/202, PENDING→UNKNOWN, SSE STALE; seven surfaces stay UNKNOWN and no packaged runtime exists
   - Owner: LOCAL (closable offline) — Portal + farmd owners after G2/G3 (V1-S5)
   - Next command: NONE — no typed command exists yet; Portal `npm test` lanes are component evidence; no packaged Portal command exists
   - Release-blocking: yes — through G2 (truthful projection plane of `release.transaction-demo`) and G9 (embedded Portal in `release.package-matrix`)
4. **farmd's public surface is the authenticated command/snapshot/SSE subset plus six read-only projections (including revision-one Context Lineage), not the designed control plane; no signed dispatch settles a public command beyond PENDING, UNKNOWN, or FAILED.** — G14 farmd production API
   - Why it matters: Until signed dispatch and the missing ledger subjects exist, every public command can only settle PENDING to UNKNOWN or FAILED, so a transaction cannot finish through the API.
   - Acceptance: After signed internal lease transport and the missing ledger subjects, serve the control-plane routes with authenticated 202 commands that reconcile past PENDING to their applied and independently checked states and read back exactly.
   - Evidence class: TRANSACTION_PROOF
   - Current evidence: COMPONENT only — loopback origin, no-wildcard CORS, command 202, ready/outbox/missions plus Fleet/Session/Merge/Quality/Audit snapshots (Kernel `529bad1`); no signed dispatch
   - Owner: LOCAL (closable offline) — Kernel API after signed dispatch (V1-S5)
   - Next command: NONE — no typed command exists yet; `bullet-farmd` serves the committed subset only
   - Release-blocking: yes — through G2 (`release.transaction-demo`); no receipt here can count on its own
5. **CognitiveTask, SelectionGroup, Role, and Fusion are wire IDs or design, not durable scheduler objects; no routing or fusion decision can be replayed from persisted inputs.** — G15 Cognitive persistence
   - Why it matters: Provider selection that cannot be replayed cannot be audited, and a transaction whose routing provenance is not durable cannot be reconstructed after a crash.
   - Acceptance: After G2, persist the cognitive objects in the Kernel ledger (V1-S6) and replay one routing/fusion decision from persisted inputs with an exact receipt.
   - Evidence class: COMPONENT_PROOF
   - Current evidence: design and wire shapes only — routing provenance records and the offline provider parsers; no durable scheduler object
   - Owner: LOCAL (closable offline) — Kernel V1-S6 after G2
   - Next command: NONE — no typed command exists yet; cognitive objects have no CLI surface
   - Release-blocking: yes — through G2 (`release.transaction-demo`); no receipt here can count on its own

## Excluded from this decision

- Synthetic receipts, including the `required.demo-component` simulator lane: SYNTHETIC_PROOF never counts toward a release gate.
- Simulator runs, fixture-only policies, and the fixture-only live-enabled v1alpha2 policy: none is an operator ratification.
- Prose: `docs/release.md`, `docs/assurance/product-gaps.md`, `docs/assurance/v1-closure-plan.md`, ADRs, changelog, and coordination chat describe evidence; they are not evidence.
- Component receipts named in `docs/release.md` and in the “exists but does not count” text above: COMPONENT_PROOF for one crate or surface, never transaction, live, or release evidence.
- The archived 2026-08-24 live demonstration receipt at the family root `.l7-bundle/`: incident evidence only (`transaction_gate_eligible: false`); it is not counted.
- Any mechanical gate result recorded against subjects other than the current HEADs (STALE above).
- Reviewer checkmarks, browser-local marks, exit codes, model statements, pushed branches, and pull requests.

## Freshness

| Input | Path | Identity | mtime (unix seconds) |
| --- | --- | --- | --- |
| family lock | `family.lock` | blake3:2b9fdced1effaaf7d0e9eefe5381b0e844bd5194fd412963f536f83bbb26d0a5 | excluded (portable variant) |
| hub manifest | `repos.manifest.toml` | blake3:46ed9d45fe7aa08d5b2d0574f176d4368d236873183f1ec77e7345cd93717159 | excluded (portable variant) |
| release index | `docs/release.md` | `Last reviewed: 2026-08-25` line only | excluded (portable variant) |
| product-gap register | `docs/assurance/product-gaps.md` | blake3:b7d40801138e15937cbf8696fbf0e5fc830e956e6c68ddba11344f4636f37a37 over the 26 `release.*` crosswalk rows + G-id list only | excluded (portable variant) |
| fast check report | `.bullet-family/check-fast.json` | absent | excluded (portable variant) |
| required check report | `.bullet-family/check-required.json` | absent | excluded (portable variant) |
| gate catalog | `src/check/prerequisites.rs` (compiled into `bullet-family 0.1.0`) | check report schema 2 | — |
| claim rows | `src/check/truth/rows.rs` + `rows/gates.rs` + `rows/ungated.rs` (compiled into `bullet-family 0.1.0`) | 26 gate rows + 5 ungated rows over 15 G-ids | — |
